# WORKLOG — рабочий поток между записями журнала STATE.md

Не источник истины и не замена журналу STATE.md (§2 HANDOFF.md). Здесь —
то, что происходит МЕЖДУ его записями: короткие пометки «то сделал, это
понял, то запланировал», без прозы и обоснований, без цели пережить
века. Как только что-то здесь дозревает до настоящего решения — оно
уезжает в STATE.md формальной записью журнала, а не остаётся только
тут. Этот файл может позже подрезаться (старые даты — не архив,
которым дорожат); STATE.md — может.

Пишется в КОНЕЦ (append, не в начало, в отличие от журнала STATE.md).
Читается с конца — `tail`, вверх до даты последней записи в журнале
STATE.md, дальше уже покрыто им напрямую. Формат: `## ГГГГ-ММ-ДД`,
внутри — `ЧЧ:ММ — короткая строка`, без дальнейшей вложенности.

При расхождении с STATE.md — STATE.md главнее (§2 HANDOFF.md): запись
здесь просто не была ещё вовремя туда перенесена, это не альтернативная
версия истины.

---

## 2026-07-21

- 10:05 — Предложение Влада: завести этот файл, повод — «лёгкая
  несостыковка при смене диалогов». Согласен, завожу, встраиваю чтение
  в HANDOFF.md §0 (хвост, до даты последней записи журнала STATE.md) и
  в загрузочный набор §1.
- 10:05 — Влад подтвердил: миграция `_context` (STATE.md «Позже»)
  закрыта целиком. Заодно поймано — заголовок того пункта был
  устаревшим («план не начат» при факте, что стадии 0-6 сделаны и
  проверены живьём 2026-07-17) — поправлено на месте.
- 10:10 — Карточка объекта (план Chat пп.4-7) отложена сознательно —
  до разговора о дизайне страниц и их поведении, который был в
  ДРУГОМ прошлом диалоге, не в этом (Влад упомянул, не пересказал).
  Переходим к выборкам — прерванный разговор с прошлой сессии,
  оставленный ровно на вопросе «какими полями описывается первая
  выборка» (STATE.md, «Позже», раздел про сортировку/фильтр/поиск).
- 12:40 — Долгий концептуальный разговор про слой представления, ничего
  ещё не закоммичено, ход мысли легко потерять — фиксирую нить целиком.
  Отправная точка — моё предложение `model_selections` было НЕВЕРНЫМ:
  `model_` — этаж ДНК (база), выборка/отчёт — этаж РНК (презентация),
  смешивать нельзя. Если понадобится невидимая служебная таблица для
  презентации — новый префикс `presentation_`, не `model_`.
  Влад задал форму: `select_` — настоящий Postgres VIEW из ОДНОЙ
  таблицы (админ выбирает поля и фильтр через конфигуратор, никакого
  текстового DSL не нужно — Postgres сам исполнитель); `report_` —
  работает в рамках дерева (все связанные таблицы), объединяет
  несколько выборок, «уже скорее функция»; страницы — организующий
  слой над report_/applet, с параметрами показа и CSS.
  Проверил код: `configurator_create_view()` (07-17) — рабочий
  прототип именно этого пути, но узкий (жёстко `id, data_name`,
  фильтр только `= 1`) — для `select_` нужно обобщение: N полей,
  фильтр по конкретному значению.
  Прочитан `О_словарях.md` — ключевая мысль: словарь может быть
  ПАРАМЕТРИЗОВАННОЙ проекцией/срезом контекстной таблицы
  (`reagents_for_material(material_id)`), не только `id → name`.
  Разобран богатый пример заказчика (скважина по элементу присадки +
  % перетоков + диапазон глубин/температур + технолог) — не одна
  способность, а минимум шесть, по возрастанию сложности: (1) равенство
  по своему словарному полю, (2) диапазон (BETWEEN, новый тип условия),
  (3) каскадный/производный словарь — открытый вопрос: настоящая
  dep_-иерархия (цех→смена→мастер→бригада) или наблюдаемая только по
  фактам корреляция? Ответ определяет механизм (record_children() уже
  умеет первое, второе нужно проектировать), (4) глубокий межтабличный
  фильтр через контекстные таблицы (реагент→состав→материал→
  цементирование→ступень→скважина) — «есть ли СРЕДИ ПОТОМКОВ хоть
  один, кто удовлетворяет условию», новая операция, никогда раньше не
  называлась, (5) значение из отдельной присоединённой таблицы
  (АКЦ, % перетоков) — тот же примитив, что (4), для колонки, не
  условия, (6) агрегаты — сознательно последними.
  Пп.4-5 — тот же примитив, что «одолженные колонки» из разговора про
  выборки (многоуровневый обход dep_, вверх и вниз) — решать один раз.
  Поймал и признал свою ошибку: предлагал прикрутить живой фильтр
  («технолог над списком») к `record_table_view()` — это админский
  слой (Карточка объекта), заказчик туда не заходит по уже принятой
  доктрине. Сам механизм фильтра (запрос + WHERE) — ОДНО общее место в
  ядре, не два параллельных; но ПРОЯВЛЯЕТСЯ он первым в слое
  представления (там повод), в админку приходит рекурсивно и почти
  бесплатно после.
  Влад скорректировал курс: не проектировать инфраструктуру заранее
  (каскады, многоуровневый обход, живой AJAX) — сначала понять два
  простых случая по отдельности: (1) простой VIEW без параметров —
  юзкейсы: «скважины в работе», «скважины технолога Иванова»,
  обобщённый гибридный словарь (не только id+data_name), зафиксированное
  на ТЗ-2 условие («давление отклонилось больше чем на X»); (2) я
  сначала неверно понял это как `calc_`/`formula_parse` — Влад поправил:
  имелась в виду не формула, а PARAMETERIZED ФУНКЦИЯ Postgres.
  Присланная от Chat справка подтвердила и уточнила: VIEW параметров не
  принимает вообще; либо (а) фильтровать VIEW снаружи (`SELECT * FROM
  view WHERE x = $1` — WHERE добавляет вызывающий код, сама форма
  выборки не меняется), либо (б) `CREATE FUNCTION ... RETURNS TABLE` —
  настоящая параметризованная табличная функция Postgres, либо (в)
  session-переменная (`current_setting`/`set_config`) — своя же
  справка предупреждает про пул соединений, для GPDP решили НЕ брать
  этот путь (скрытое глобальное состояние хуже явного параметра
  функции, обсуждать не стали подробно).
  Красиво сошлось: `select_` = VIEW (форма (а) уже покрывает даже
  живую фильтрацию списка — WHERE снаружи, без параметров у самого
  VIEW), `report_` = `RETURNS TABLE`-функция (форма (б)) — это ровно
  то, что Влад предсказал словами ещё в первом сообщении про report_
  («он уже скорее будет функцией»), до всякой технической справки.
  НЕ решено: принимать ли предложенные Chat альтернативные префиксы
  (`view_`/`filter_`/`query_`) — склоняюсь оставить `select_`/`report_`,
  но не решал за Влада.
  Следующий конкретный шаг, ещё не начат: обобщение
  `configurator_create_view()` под `select_` — произвольные N колонок,
  фильтр по конкретному значению вместо жёсткого `= 1`.
- 12:50 — Backend + форма select_ собраны целиком (коммиты d94f3cd,
  67ad473, STATE.md журнал (10)-(11)) — паспорт presentation_selections,
  selection_parse/snapshot_build_selections, configurator_create_
  selection(), визуальная форма (источник → чекбоксы полей → каскадный
  фильтр). Находка по дороге: filterable по ошибке ссылался на links_
  (integer[], множественное) вместо link_ (int, единственное) — три
  места, поймано и исправлено до коммита.
- presentation_selections создана на gpdp_test живьём. Первая попытка
  Влада упала — запустил bootstrap_postgres.sql целиком, а не только
  новый кусок (`relation model_registry уже существует`). Починено:
  IF NOT EXISTS в основном файле + отдельный самостоятельный
  current/add_presentation_selections.sql (15b3a05) — вторая попытка
  этим отдельным файлом прошла чисто. Ждём первого живого прогона
  самой формы (select_wells_in_progress, тот самый скучный пример —
  источник well, поля номер/месторождение/технолог/дата,
  фильтр voc_status = «В работе»).
- Влад: «Новая выборка показывает одни словари». Причина — скопировал
  фильтр по data_name из configurator_link_target_items() туда, где
  он не годится: well не несёт data_name (у него data_number),
  проходили только таблицы, у которых data_name случайно есть — по
  факту словари. Убрано, источником теперь любая таблица (32a8f87).
- Влад: сделал две выборки, работает. Два новых замечания — словари
  засоряют список конфигуратора, select_ падал в «Главные» вперемешку
  с обычными таблицами (не было своей категории). table_group()
  получил категорию 'selection' (по object_type='view'), словари и
  системные таблицы теперь в <details> (свёрнуты по умолчанию, без
  JS) — 6759e4b.
- Влад: «Главные» вводило в заблуждение (звучит как «одна
  привилегированная», совпадает со служебной main из smoke_test.php;
  на деле — просто «вершина своего дерева», корней может быть
  несколько независимых). Переименовано 'main'→'root' и в коде, и на
  экране («Корневые таблицы»); все четыре раздела теперь свёрнуты по
  умолчанию одинаково — 94cda47. Вопрос про выпадающее меню вместо
  <details> — ответил в чате, не стал делать молча: рекомендовал
  оставить <details> (ноль JS, несколько разделов можно держать
  открытыми одновременно), не решал за Влада.
