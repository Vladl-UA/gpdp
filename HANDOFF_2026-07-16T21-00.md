Задача: обнови два AI-документа (`ARCHITECTURE_SNAPSHOT_AI.md`, `STATE_INDEX_AI.md`) под текущий `main` — не точечно, полная пересборка, слишком много всего накопилось с прошлого раза (`source_main_commit: 3d1af2c` у обоих). `FUNCTION_PASSPORTS_AI.md` НЕ трогать — он уже пересобран и свежий (`generated_at_commit: eaae58b`), проверен построчно вручную (номера строк и `called_by` сверены с кодом), расхождений не найдено.

Что нового с последнего обновления (для ориентира, не исчерпывающий список — сверься с `main` сам):

* **Переезд GPDP с MySQL на PostgreSQL целиком** (STATE.md «Сейчас» п.9, полный катор, не dual-driver) — самый крупный кусок:
  * `db.php` переписан заново: `db_select`/`db_execute` теперь на `pg_query_params` вместо `mysqli_*`; три новые функции — `db_connect`, `db_close`, `db_placeholders`.
  * `core.php::snapshot_build_structure` — `SHOW TABLES`/`SHOW COLUMNS` заменены на `information_schema`/`pg_catalog`.
  * `core.php::record_save` — обратные кавычки убраны, INSERT-ветка получила `RETURNING id`.
  * `configurator.php` — DDL-билдер (`configurator_create_table`) переписан под Postgres-синтаксис (`GENERATED ALWAYS AS IDENTITY`, без `ENGINE=`/`CHARSET=`); ещё пять функций (`configurator_register_link/formula`, `configurator_adopt_field/table`) получили `RETURNING id` там, где читают `['id']`.
  * `labels.php::labels_save_label` — `ON DUPLICATE KEY UPDATE` → `ON CONFLICT ... DO UPDATE`.
  * `helpers.php::admin_db_connect` — тело переписано, делегирует в `db_connect()`.
  * Тип параметра соединения сменился с `mysqli` на `PgSql\Connection` примерно в 30 сигнатурах по `core.php`/`configurator.php`/`helpers.php`/`labels.php`/`tools_bulk_import.php` — механическая замена, без изменения логики в большинстве случаев (детали и честная граница проверки — HANDOFF_2026-07-16T18-00.md, уже в `main`, я сверял его прошлый паспорт-заход по этому файлу построчно, разошедшихся строк не нашёл).
  * `tools_bulk_import.php` — `mysqli_begin_transaction`/`mysqli_rollback` (dry-run транзакция) → `pg_query($db, 'BEGIN')`/`pg_query($db, 'ROLLBACK')`. Эта функция не описана в паспортах вообще — файл целиком не покрыт (там только пять из перечисленных ранее вспомогательных функций, а транзакционный блок — top-level код `tools_bulk_import.php`, не отдельная функция).
  * `entities.php::ent_bul` — `default_type` `tinyint(1)` → `smallint` (Postgres не имеет `tinyint`; `boolean` сознательно НЕ выбран — см. комментарий в самом файле). `allowed_types` у `ent_voc`/`ent_ltext`/`ent_int`/`ent_link` вычищены от несуществующих в Postgres `mediumint`/`mediumtext`/`longtext`.
* **`calc_`: шаг 3 архивного плана** (коммит `a99c9e0`, был ДО начала переезда, но после `3d1af2c`) — экран конфигуратора для типа «Вычисляемое (формула)»: `configurator_parse_field` принимает и проверяет формулу, `configurator_register_formula` пишет в `model_formulas`.
* **Новые файлы, не только код**: `bootstrap_postgres.sql` (DDL четырёх системных `model_*`-таблиц под Postgres — реконструкция, не копия оригинала, см. комментарий в самом файле), `import_dictionaries.json`/`import_tz2.json` (данные для `tools_bulk_import.php`, не архитектура, но `ARCHITECTURE_SNAPSHOT_AI.md` может упомянуть их как пример использования конвейера), `STRUCTURE_PROPOSAL_FORMAT.md` (справочный документ для описания ещё не созданных таблиц — пара к `BULK_IMPORT_FORMAT.md`, который уже был известен паспортам).
* **Дерево Лист1-4 ТЗ-2 пересоздано на Postgres целиком** через конфигуратор — шесть новых предметных таблиц (`material_test`, `test_reagent`, `akc_by_material`, `productive_horizon`, `akc_by_horizon` плюс словарь `voc_fluid_type`). Это не код, но `STATE_INDEX_AI.md` про это не знает вообще — весь журнал переезда (`STATE.md`, 11 коммитов с `3d1af2c`) в индексе не отражён.

Проверь свежесть каждого документа сам через историю файла в `main` (тот же механизм, что мы уже используем — `git log <commit>..HEAD -- <область>`), не полагайся на этот список как на исчерпывающий — просто ориентир, откуда искать. Область для `ARCHITECTURE_SNAPSHOT_AI.md` — `*.php`; для `STATE_INDEX_AI.md` — только `STATE.md`.
