Задача: обнови все три AI-документа (`FUNCTION_PASSPORTS_AI.md`, `ARCHITECTURE_SNAPSHOT_AI.md`, `STATE_INDEX_AI.md`) под текущий `main` — полная пересборка, не точечно. Оба снапшот-документа сейчас на `source_main_commit: eaae58b`, с тех пор 10 коммитов по `*.php` и 9 по `STATE.md` — накопилось целое новое направление, не мелкие правки.

Что нового с `eaae58b` (для ориентира, не исчерпывающий список — сверься с `main` сам, `git log eaae58b..HEAD --oneline -- '*.php'` и `-- STATE.md`):

* **`links_` — новая сущность целиком** (многозначная ссылка на словарь, 0..N, `entities.php`): `ent_links`, `links_handler` (read/new/edit/validate), `links_array_parse`/`links_array_build` (Postgres `integer[]` ↔ PHP-массив, `pg_*` всегда отдаёт массивы текстом). Затронула не только `entities.php`:
  * `core.php::record_view_row` — больше не форсит `(string)`-каст значения ячейки, список проходит как есть.
  * `core.php::snapshot_build_dictionaries` — Проход 1б распознаёт `links_`, не только `link_` (был реальный fatal на этом месте, нашлось живьём).
  * `render.php::render_choice`/`render_value`/`render_record_table` — виджет `select_multiple`, раскладка списка через `<br>` (в столбик, не через запятую — по прямому требованию Влада, чтобы таблица не растягивалась вширь).
  * `configurator.php` — три места, ранее жёстко завязанные на литеральную строку `'link'` (проверка обязательности цели, JS-переключатель видимости дважды), расширены на `'links'`.
* **`configurator_create_view()` — гибридный словарь (Postgres `VIEW`)**, новая ветка `configurator_validate_spec` (`'view_filtered'`), четвёртый вариант в форме `new_table`. `configurator_delete_table` — развилка `DROP TABLE`/`DROP VIEW` по `object_type` (новое поле в `snapshot_build_structure`, читает `information_schema.tables` вместо `pg_catalog.pg_tables` — теперь видит `VIEW`, не только `BASE TABLE`).
* **Рефакторинг `configurator.php`** (до `links_`/вьюх, но после предыдущей пересборки): три новых общих кирпича — `configurator_with_lock`, `configurator_register_element`, `configurator_is_managed`. Восемь функций переведены на них, поведение не менялось.
* **`db.php::db_query_count()`** — новая функция, счётчик фактически выполненных запросов (замена MySQL `SHOW SESSION STATUS LIKE 'Questions'` для N+1-проверки в `smoke_test.php`, у Postgres прямого аналога нет).
* **`core.php::record_delete`** — убран `LIMIT` из `DELETE` (MySQL-расширение, в Postgres синтаксиса нет вовсе — нашлось смоуком, не на этапе написания).
* **`smoke_test.php`** — полностью переведён на Postgres (был сознательно не тронут на предыдущей пересборке): `mysqli_*` убраны, `UPDATE...JOIN` → `UPDATE...FROM`, фикстуры `voc_smoke`/`main` заведены. 70/70 проверок зелёные живьём. Не знаю, входит ли этот файл в паспорта вообще (проверь сам, что он там значился как «пять вспомогательных функций»).

Проверь свежесть каждого документа сам через историю файла в `main`, не полагайся на список выше как на исчерпывающий — просто ориентир, откуда искать. Область для `ARCHITECTURE_SNAPSHOT_AI.md`/`FUNCTION_PASSPORTS_AI.md` — `*.php`; для `STATE_INDEX_AI.md` — только `STATE.md`.
