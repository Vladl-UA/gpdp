Задача: обнови все три AI-документа (`FUNCTION_PASSPORTS_AI.md`, `ARCHITECTURE_SNAPSHOT_AI.md`, `STATE_INDEX_AI.md`) под текущий `main` — полная пересборка, накопилось много и по коду, и по журналу. Оба снапшот-документа сейчас на `source_main_commit: 843f89d`, с тех пор 24 коммита по `*.php` и 15 по `STATE.md` — не мелкие правки, а два больших направления работы плюс несколько независимых кусков поменьше.

Ориентир (не исчерпывающий список — сверься с `main` сам, `git log 843f89d..HEAD --oneline -- '*.php'` и `-- STATE.md`):

**Направление 1 — гибридные словари и `links_`** (первая половина дня):
* `links_` — новая сущность целиком (уже было частично отражено в прошлой пересборке на другом коммите, проверь актуальность): многозначная ссылка на словарь, `entities.php::ent_links`/`links_handler`, Postgres `integer[]` ↔ PHP-массив.
* `configurator_create_view()` — гибридный словарь на Postgres `VIEW`, фильтр по одной `bul_`-колонке источника. `snapshot_build_structure()` теперь читает `information_schema.tables` (видит `VIEW`, не только `BASE TABLE`), добавлено поле `object_type`. `configurator_delete_table()` — развилка `DROP TABLE`/`DROP VIEW`.
* `core.php::record_delete_check_usage()` — новая проверка использования перед удалением словарной позиции (реферальная целостность на уровне GPDP, не БД — Postgres не умеет `REFERENCES` на элемент массива).
* `db.php::db_query_count()` — счётчик запросов (замена MySQL `SHOW SESSION STATUS` в `smoke_test.php`).

**Направление 2 — единый вход `index.php` через `_context`** (вторая половина дня, самое крупное изменение):
* `core.php::REQUEST_CONTEXTS`/`request_context()` — контекст запроса определяется РАНЬШЕ `snapshot_init()`. Константа дважды меняла форму за день — сейчас ассоциативный массив (`icon`/`label`/`href` на каждый ключ), не плоский список.
* `configurator.php`/`labels.php` — из самостоятельных скриптов стали чистыми библиотеками: `configurator_dispatch()`/`labels_dispatch()`, `require_once` вместо `require`, при прямом обращении — `redirect` на `index.php?_context=...`, никакой собственной логики диспетчера вне `require`.
* Весь HTML обоих файлов переехал в `render.php` (`render_configurator_*`/`render_labels_*`) — ни одного `echo` с HTML-тегом больше не осталось ни в `configurator.php`, ни в `labels.php`.
* `labels_registry_id`/`labels_save_label` переименованы в `model_label_registry_id`/`model_label_save`.
* `render_admin_page_open()` сменил сигнатуру дважды за день — сейчас `(string $title, string $current_context, string $extra_head = '')`, строит системное меню сама через `render_context_menu()` из `REQUEST_CONTEXTS`.
* `ARCHITECTURE.md` уже обновлён отдельно (§6, диаграмма вызовов, `ENTITY_RESERVED_IDS`) — сверься с ним, не только с кодом.

**Меньшие, но реальные правки по пути**: `record_delete()` — убран MySQL-специфичный `LIMIT`; `render.php` — обширная переработка вёрстки карточки объекта (`render_record_compact`, `.rel-wrap`, капнутая глубина вложенности, hover-меню действий вместо иконок) — часть сделана мной, часть Chat отдельно (проверь коммиты по `style.css`/`render.php` за сегодня целиком, не только мои).

Проверь свежесть каждого документа сам через историю файла в `main`, список выше — ориентир, откуда искать, не подменяет собственную проверку.
