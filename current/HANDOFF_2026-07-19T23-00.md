Задача: пересобери все три AI-документа (`FUNCTION_PASSPORTS_AI.md`, `ARCHITECTURE_SNAPSHOT_AI.md`, `STATE_INDEX_AI.md`) под текущий `main`. Оба снапшот-документа собраны на `6e02a5f`/`3dea6aa`, индекс журнала — на `73c769e`; с тех пор девять коммитов по `*.php` и десять по `STATE.md`.

Проверь свежесть каждого документа сам, механически, а не по полю внутри документа: `git log $(git log -1 --format=%H -- "<файл>")..HEAD --oneline -- '*.php'` (для индекса журнала — `-- STATE.md`). Список ниже — ориентир, откуда искать, он не подменяет собственную сверку с `main`.

**ВАЖНО, про номера строк.** Твои ссылки на строки устойчиво уезжают на +2 во всех файлах — рабочая гипотеза: содержимое читается через обёртку код-фенса. Прежде чем зафиксировать любой номер строки, проверь его дважды двумя независимыми способами (например, `grep -n` по сигнатуре и отдельно чтение окрестности), и сверь, что найденная строка действительно содержит то, что ты о ней утверждаешь.

**Направление 1 — транзакции структурных операций** (коммиты `b1b3f3e`, `d08396f`, `08f2dfd`):
* `db.php` — три новые функции: `db_transaction_begin()`, `db_transaction_commit()`, `db_transaction_rollback()`. Тонкие обёртки над `db_execute()`, честный результат, без вложенности и SAVEPOINT.
* `configurator.php::configurator_with_lock()` — **сменила и сигнатуру, и контракт**. Четвёртый параметр `array $application`. Внутри теперь: `BEGIN → тело → snapshot_build() → COMMIT → snapshot_save()`. Каркас сам владеет пересборкой и публикацией снапшота; тело операции отвечает только за DDL и реестр.
* `configurator.php::configurator_refresh()` — **удалена** как дубль (её работу забрал каркас).
* Девять тел структурных операций больше не собирают и не публикуют снапшот; пять сообщений об ошибках переписаны — состояния «создано в БД, но не зарегистрировано» больше не бывает, оно откатывается.
* `tools_bulk_import.php` — сырые `pg_query($db, 'BEGIN'/'ROLLBACK')` заменены на примитивы `db.php`, отказ `BEGIN` проверяется; докблок про `auto_increment у InnoDB` поправлен на `IDENTITY`.
* `smoke_test.php` — новая секция 0.

**Направление 2 — тег `option` вернулся в свой слой** (коммиты `0b1c711`, `d02a2fc`, `e242281`):
* `render.php::render_options()` — новая функция, единственный источник тега в проекте. Принимает список `['value' => .., 'label' => ..]`, `$current` может быть скаляром или массивом (`links_`).
* `render.php::render_configurator_new_table()` и рендер формы правки таблицы — **сменили сигнатуры**: параметры с готовым HTML стали массивами (`$type_items`, `$parent_items`, `$link_target_items`, `$dict_items`).
* `configurator.php` — двенадцать мест сборки `<option>` заменены на подготовку списков.
* Два цикла внутри `render_field()` сведены к `render_options()`.
* `smoke_test.php` — новая секция 6а.

**Направление 3 — зависший lock** (коммит `90b7184`):
* `core.php::snapshot_lock_force_release()` — **удалена**, на её месте `snapshot_lock_release_stale(PgSql\Connection, array): array`. Порядок: пересобрать снапшот → записать → и только потом удалить файл лока. Если модель не собирается, лок остаётся на месте.
* `render.php` — две новые функции: `render_duration()` (секунды → человеческая строка) и `render_lock_state()` (блок «Блокировка схемы» для страницы диагностики).
* `configurator.php` — новый маршрут `release_lock`, вынесен отдельно от `$repair_actions`.
* `index.php` — ответ 503 теперь показывает возраст лока.
* `smoke_test.php` — новая секция 8а.

**Не мной, но тоже не отражено** (коммиты `fd3842f`, `c9a34ec`, предыдущая сессия): `configurator_register_element()` возвращает честный результат вместо голого `int`, все семь точек вызова его проверяют; CSS вынесен из `labels.php` в `style.css`; убраны обратные кавычки в `COUNT` (синтаксис MySQL, Postgres их отвергал).

**По журналу** (для `STATE_INDEX_AI.md`): десять коммитов в `STATE.md`, из них три новые записи за 07-19 (транзакции, `option`, lock) и одна за 07-18 (вечер) про ревизию Chat. Плюс раздел «Сейчас» снова пуст — все пункты закрыты.
