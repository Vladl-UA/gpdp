Задача: почини `FUNCTION_PASSPORTS_AI.md` и допиши все три AI-документа под текущий `main`. Это не претензия к качеству — то, что ты собрал, проверено и верное; проблема в охвате, и она механическая.

**ВАЖНО, про номера строк.** Твои ссылки на строки исторически уезжали на +2 во всех файлах — рабочая гипотеза: содержимое читается через обёртку код-фенса. В этой пересборке смещения НЕ было, я сверял выборочно (`db_transaction_begin` 204/204, `db_select` 120/120, `configurator_with_lock` 479/479, `render_options` 142/142) — дельта ноль везде. Инструкция остаётся как профилактика, а не как упрёк: прежде чем зафиксировать любой номер строки, проверь его дважды двумя независимыми способами (`grep -n` по сигнатуре и отдельно чтение окрестности) и убедись, что найденная строка содержит именно то, что ты о ней утверждаешь.

## 1. Главное: пересборка потеряла 71 паспорт

Посчитано механически, поимённой сверкой двух версий файла:

```
6e02a5f  146 паспортов, 172 строки   ← до пересборки
c0f6320   82 паспорта,  106 строк    ← «rebuild_type: full»
```

Потеряно 71, добавлено 7. Документ был собран заново с нуля вместо обновления, и всё, чего не касался предыдущий тикет, в новую версию не попало. Мой тикет перечислял три направления и просил сверяться с `main`, но слов «сохрани уже описанное» в нём не было — вина на формулировке задачи, не на исполнении.

Восстановить нужно следующее (полный список, сгруппирован по файлам). Каждый паспорт **проверь на актуальность против текущего `main`** — часть могла измениться за прошедшие коммиты, слепое копирование из старой версии не годится:

**core.php (42):** `action_mode`, `entities`, `entity_id_valid`, `entity_registry_load`, `field_data`, `field_exec`, `field_parse`, `field_prefix`, `formula_parse`, `model_diagnose`, `record_children`, `record_delete_check_usage`, `record_fetch`, `record_form_view`, `record_label`, `record_list`, `record_parent_candidates`, `record_parent_relation`, `record_reparent`, `record_reparent_view`, `record_result`, `record_save`, `record_table_view`, `record_tree`, `record_view_columns`, `record_view_row`, `schema_view`, `snapshot_build`, `snapshot_build_dictionaries`, `snapshot_build_formulas`, `snapshot_build_links`, `snapshot_build_presentation`, `snapshot_build_registry`, `snapshot_build_relations`, `snapshot_build_structure`, `snapshot_last_error`, `snapshot_load`, `snapshot_save`, `snapshot_templates`, `snapshot_validate`, `table_group`

**entities.php (27):** `ent_bul`, `ent_calc`, `ent_data`, `ent_date`, `ent_dec`, `ent_footnote`, `ent_int`, `ent_link`, `ent_links`, `ent_ltext`, `ent_time`, `ent_voc`, `ent_year` и парные им `bul_handler`, `calc_handler`, `data_handler`, `date_handler`, `dec_handler`, `footnote_handler`, `int_handler`, `links_handler`, `ltext_handler`, `time_handler`, `voc_handler`, `year_handler`, плюс `formula_eval`, `links_array_build`, `links_array_parse`

**render.php (1):** `render_reparent_form`
**config.php (1):** `config`

Старая версия целиком доступна: `git show 6e02a5f:FUNCTION_PASSPORTS_AI.md`.

Заодно поправь шапку: пока покрытие неполное, `rebuild_type: full` вводит в заблуждение — следующая сессия, доверившись слову `full`, решит, что `field_parse` не существует. Либо `full` после восстановления, либо `partial` со строкой о том, какие файлы покрыты целиком, а какие частично.

## 2. Чего не хватает во всех трёх: стадия 5 дорожной карты единого входа

Коммиты `d9b6e00`, `e210079` — легли после твоей сборки, знать о них ты не мог.

`index.php` был последней точкой входа, рождавшей HTML сама (13 мест из 20 `echo`). Теперь ноль: все двадцать... точнее, четырнадцать оставшихся `echo` идут через `render_*`. **HTML в проекте не рождается больше нигде, кроме `render.php`** — это теперь проверяемый факт, а не обещание в докблоке.

Новые функции в `render.php`: `render_data_home()` (домашний список корневых таблиц), `render_save_errors()` (отказ записи, 422, перечень причин), `render_page_heading()`, `render_link_line()` (третий параметр — необязательный CSS-класс), `render_parent_context()`. У `render_link_line()` четыре потребителя — «Отмена» дважды, «Добавить запись», «← К списку таблиц».

Попутно: `render_admin_page_close()` существовала давно и не использовалась — три места закрывали страницу руками, теперь зовут её. Последний `echo` с HTML в `labels.php` тоже закрыт.

В `smoke_test.php` — статический сторож на весь проект (не на один файл, как было для SQL в `index.php`): любой `echo '<'` или heredoc с HTML вне `render.php` валит смоук с именем нарушителя.

Важно для `ARCHITECTURE_SNAPSHOT_AI.md`: инвариант «HTML рождается только в `render.php`» из §3/§12 теперь выполняется без исключений во всех тринадцати файлах. До стадии 5 он не выполнялся.

## 3. Для STATE_INDEX_AI.md: две записи журнала

`07-19 (ночь, 2)` — обзор всего кода на соответствие архитектуре, тринадцать механических проверок против §1, §3, §10, §12, §13. Зафиксированы шесть находок, ждущих очереди: три зашитых решения о представлении (`RENDER_COMPACT_MAX_COLS = 4`, порог ширины `<= 7`, слияние сиблингов по наличию внуков); пустые заготовки «на будущее» (`table_group()` документирует возврат `'report'`, которого в теле нет; полка «Отчёты» на трёх страницах); четвёртая копия `<option>` в `render_reparent_form`; дублирующийся JS обеих конфигураторных форм; неэкранированный `render_admin_flash()`; обход `db.php` сырыми `pg_query` в `snapshot_build_structure()`.

`07-19 (ночь, 3)` — стадия 5, описана выше.

## 4. Общее

Проверь свежесть каждого документа сам, механически: `git log $(git log -1 --format=%H -- "<файл>")..HEAD --oneline -- '*.php'` (для индекса журнала — `-- STATE.md`). Поля внутри документов — подсказка, никогда не тест на свежесть; ты сам это правило записал в шапку `ARCHITECTURE_SNAPSHOT_AI.md`, оно верное.

Список выше — ориентир, откуда искать. Он не подменяет собственную сверку с `main`.
