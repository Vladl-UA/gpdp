# GPDP/RNA — Текущее состояние и дорожная карта

Обновляется при завершении шага или пересмотре решения.
Инварианты — в ARCHITECTURE.md; здесь — «где мы» и «что дальше».

---

## Как начинать новую ветку разработки

Первая строка сессии:

```
Работаем по ARCHITECTURE.md. Текущая задача — пункт N из раздела «Сейчас».
```

Этого достаточно; пересказ истории не требуется.

## Сейчас (база, в порядке выполнения)

**0. Слой абстракции над вызовами БД — ВЫПОЛНЕНО ЦЕЛИКОМ 2026-07-14.**
`db.php` (`db_select`/`db_execute`) — все прямые вызовы `mysqli_*` по
проекту переведены, кроме сознательно оставленных (управление
соединением: `connect`/`close`/`set_charset`; одна интроспекция схемы
`SHOW TABLES`/`SHOW COLUMNS`, другой формат возврата). Разбито по
файлам: `core.php`+`configurator.php` — Sonnet, семь коммитов
(`40e40a0`…`a1c8144`). `helpers.php`+`labels.php` — Chat, первая
настоящая делегированная работа по `AI_CONTEXT_PROTOCOL.md`, два
коммита (`afa0cab`, `2c15076`), проверено `diff`'ом построчно перед
принятием — текст SQL не тронут, поведение при ошибке явно
откомментировано там, где менялось. `index.php` не требовал правки
(только разрешённый `mysqli_close`). Смоук зелёный целиком после
каждого шага, включая финальную проверку после работы Chat (раздел 7
проверяет именно код `helpers.php`, который он переписал). НЕ
расширять до полноценной СУБД-абстракции без нового решения — границы
масштаба см. журнал 2026-07-13.

Пункты 1–7 первоначального списка **выполнены**: смоук в среде разработки
(37 проверок на живой MariaDB), холодный старт и полный CRUD-цикл
(new → save → PRG → edit → delete, кириллица насквозь) **подтверждены
на целевом localhost** (Debian, gpdp_test) 2026-07-03. Холодный старт
закрыт: точка входа — `snapshot_init()`, единственная дверь сборки.
Остаток:

1. **Реестр модели и словари** (доктрина — О_словарях.md; инварианты —
   §16–17). Имя семейства служебных таблиц: `model_*` (рабочее;
   альтернатива `datamodel_*` — подтвердить до выполнения DDL). Подшаги:

   **(а0) Реестр — ВЫПОЛНЕНО 2026-07-05.** Раскол subscr →
   model_registry + model_labels, на целевом localhost.

   ```sql
   CREATE TABLE model_registry (
     id           INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
     data_kind    VARCHAR(16)  NOT NULL,   -- 'table' | 'field'
     data_owner   VARCHAR(64)  NULL,       -- NULL для kind='table'
     data_element VARCHAR(64)  NOT NULL,
     active       TINYINT(1)   NOT NULL DEFAULT 1,
     UNIQUE KEY uq_address (data_kind, data_owner, data_element)
   ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

   CREATE TABLE model_labels (
     dep_model_registry INT UNSIGNED NOT NULL PRIMARY KEY,
     data_short         VARCHAR(64)  NULL,
     data_full          VARCHAR(255) NULL,
     CONSTRAINT fk_labels_registry FOREIGN KEY (dep_model_registry)
       REFERENCES model_registry(id) ON DELETE CASCADE
   ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;
   ```

   Отличие от первого черновика DDL: **у model_labels нет суррогатного
   `id`** — отношение к реестру строго 1:1 («подпись не живёт без
   элемента», не более одной на элемент сегодня), поэтому
   `dep_model_registry` сам стал PRIMARY KEY — инвариант 1:1 обеспечен
   схемой, не дисциплиной кода. Правило кардинальности — см. журнал:
   решать по факту связи каждый раз заново, не шаблоном.

   Выполнено: сборка снапшота (`SUBSCR_TABLE` упразднён →
   `MODEL_REGISTRY_TABLE`/`MODEL_LABELS_TABLE`; новые
   `snapshot_build_registry()`/`snapshot_build_presentation()`,
   presentation-карта вложенная `[kind][owner][element]`; новая
   `snapshot_refresh_model()` симметрично presentation); три потребителя
   (`field_data` в core.php, шапка+заголовок в index.php, `render.php`);
   `active` — в `STRUCTURAL_FIELD_NAMES`; служебные таблицы `model_*`
   исключены из структурного сканера целиком (см. журнал).
   `subscr` — данные перенесены, физически ещё не удалена (страховка).

   **(а1) Адресный словарь — ВЫПОЛНЕНО 2026-07-08.** Резолвер
   `snapshot_build_dictionaries()` — чистая функция над уже собранной
   структурой (ноль SHOW): `voc_x` существует → склад (`source_table =
   voc_x`); иначе `x` с `data_name` → адрес (`source_table = x`,
   `label_from = x`, наследование подписи — хук, показ в presentation
   отложен); иначе fail-fast («нужен паспорт словаря» / «неизвестный
   адрес словаря»). Результат — `model.dictionaries[voc_x]`, ключ по
   имени поля (одно имя = один смысл на всю модель — граница журнала
   07-07, зафиксирована в докблоке функции). Врезка в `snapshot_build`:
   fail-fast тем же путём, что `unknown_fields`, через
   `snapshot_last_error`. `field_data` инъектит скомпилированный
   `source` в пакет; `voc_handler` (entities.php) читает `$field
   ['source']` вместо вычисления имени таблицы из `$field['raw']` —
   единственная содержательная строка правки в хендлере. `lookup_options`
   не тронут (не требовалось — сигнатура и так принимала доверенное имя).
   Смоук: секция 1а (`smoke_test.php`) — 3 проверки на живом снапшоте +
   лестница целиком на синтетической структуре (склад / адрес /
   «нужен паспорт» / «неизвестный адрес»), резолвер БД не касается.
   Все 10 секций смоука зелёные на целевом localhost 2026-07-08.
   Границы согласования (журнал 07-07) удержаны без отступлений:
   резолвер остаётся конвенционным/временным, не паспортной доктриной;
   fail-fast на неразрешимый `voc_x` — временная жёсткость под гарантию
   конфигуратора, НЕ отмена принципа локальной деградации для будущих
   ошибок отдельных паспортов/формул (стратегический инвариант, не
   реализован — просто не должен быть забыт при (а2)); склад/дескриптор
   не различаются, пока нет таблиц-описателей — граница (а2).
   Побочная находка при закрытии — см. журнал 07-08: миграция (а0)
   на presentation-слой оказалась неполной, вскрылось только сейчас,
   потому что смоук впервые пересобрал снапшот текущим ядром вместо
   подхвата ископаемого файла с диска (см. журнал).

   **(а2) Составная подпись словаря — ВЫПОЛНЕНО 2026-07-08** (уровень 2,
   §16.1 «маяк описателя»/§16.3 «шаблон склейки»). Финальный контракт
   после трёх пересмотров в разговоре 07-08 (эволюция решения — см.
   журнал: отдельная таблица-описатель → order+glue колонки → шаблон
   с фигурными плейсхолдерами, по образцу KDE HTML Clock placeholders):
   - новая колонка `model_labels.data_label_template VARCHAR(255) NULL`,
     на строке `data_kind='table'` целевой таблицы (не на строках полей —
     снимает омонимию: `data_number` сохраняет свою обычную подпись
     в собственной форме, шаблон лишь ССЫЛАЕТСЯ на него по имени);
   - эталон: `well` получает `data_label_template = '{voc_area} №{data_number}'`
     → «Мамуринская №31»;
   - маяк — сам факт непустого шаблона, отдельного флага не нужно;
     резолвер (а1) проверяет шаблон ПЕРЕД тем, как объявить fail-fast
     «нужен паспорт словаря» на ступенях склада и адреса — расширение
     существующей лестницы, не переписывание;
   - плейсхолдеры `{имя_поля}` — ТОЛЬКО поля той же строки источника
     (`well.voc_area`, `well.data_number`); ссылки через `rel_`/`dep_`
     в другие таблицы — сознательно вне объёма первой версии;
   - компилятор: `preg_match_all` по шаблону при сборке снапшота,
     whitelist-сверка каждого токена со снапшотом (fail-fast на
     несуществующее поле — как «нужен паспорт», та же дисциплина);
     `voc_`-токены резолвятся рекурсивно через уже разрешённый
     источник, циклы (шаблон А ссылается на словарь Б с собственным
     шаблоном, обход возвращается в А) — fail-fast сборки с перечнем
     цепочки (§16.5);
   - исполнение — БЕЗ SQL JOIN (§16.3: «текст SQL в данных не хранится
     и не исполняется»): `lookup_*` читает `SELECT *` источника,
     собирает строку в PHP по плану, `voc_`-токены — через уже
     существующий `lookup_options()` (переиспользование request-level
     кэша helpers.php, ноль нового кэширующего слоя).
   Честная оговорка (не прятать): `model_labels` — presentation-слой
   (§17), контракт которого был «готовая строка для показа». Шаблон —
   уже не готовая строка, а инструкция сборки, исполняемая lookup_*;
   технически укладывается в «обычные строки обычной таблицы» (§16.6),
   но концептуально presentation-слой начинает нести и модельную логику.
   Явная граница с (а1): различение склад/проекция для существующей
   `voc_x` — часть этого шага (проверка шаблона встраивается в
   резолвер), не молчаливое расширение (а1) задним числом.
   Наследование подписи (`label_from`, хук уже есть в а1) — про подпись
   САМОГО поля voc_x в его форме, не про сборку значений опций; не
   пересекается с шаблоном, показ в presentation по-прежнему отложен.

   **(а3) Секция смоука по лестнице целиком**: склад / адресный /
   паспортный / конкатенация / словарь-в-label (JOIN) / осиротевший
   паспорт / цикл / «нужен паспорт».
2. **Семейство времени — ВЫПОЛНЕНО 2026-07-05**: `ent_date`, `ent_year`,
   `ent_time` живы на целевом localhost, ядро (core.php) не тронуто ни
   строчкой. Честная поправка к исходному плану: composite-input
   (day+month+year отдельными ключами POST) НЕ понадобился — каждая из
   трёх сущностей нашла ровно одно нативное значение и один нативный
   HTML-виджет (`<input type="date/number/time">`), сборки из частей
   не потребовалось вообще. Composite-механика (конвенция `field__part`,
   разбор в record_save) откладывается до первой сущности, у которой
   действительно нет родного виджета — кандидат `ent_dur` (сроки
   схватывания «6-40», MySQL TIME физически годится, HTML type="time"
   — нет, это «время суток», не «сколько прошло»). `ent_year`/`ent_time`
   построены без текущего предметного поля, по прямой директиве
   «типы под всё, что поддерживает БД» — не спекуляция, осознанная
   политика полноты семейства (тот же приём, что с `footnote`/`ltext`).
   `ent_year` хранится как `smallint`, не MySQL `YEAR` — последний узок
   (1901–2155) без реальной причины его придерживаться; validate
   проверяет границы ВЫБРАННОГО типа, не выдуманную календарную
   семантику. Найден и исправлен реальный класс бага: `nullable=true`
   в паспорте не означает, что validate обязан принимать пустую строку
   как валидное значение для DATE/YEAR/TIME (в отличие от varchar,
   где '' законно) — пустая строка конвертируется в NULL по ЖИВОЙ
   схеме колонки (`field['schema']['nullable']`), не по дефолту паспорта.
   Первый предметный кейс: дата в ТЗ-2 лежит Excel-сериалом (39604) —
   конвертация при построении тестовых данных, ещё не выполнена.
3. **Зависимые таблицы (`rel_`/`dep_`) — ВЫПОЛНЕНО 2026-07-08.** Несущая
   конструкция референсной задачи. Семантика прояснена по легаси-дампу
   `tt_utf8_fixed.sql` (археология смысла, не образец кода):
   - **`dep_<table>`** = «dependent on» — FK на строке-потомке →
     непосредственный родитель, ставит ссылающуюся таблицу в подчинение
     (`bufers.dep_material → material`, `material.dep_steps → steps`,
     `steps.dep_main → main`). Это и есть «ссылка на произвольного
     непосредственного родителя» — изобретать не нужно, уже существует.
   - **`rel_main`** = «relates to main» — плоская ссылка с любой глубины
     сразу на корень дерева, параллельно `dep_`. Один из вариантов m2m:
     короткий путь к корню, минуя промежуточные уровни. Каждый потомок
     несёт И свой `dep_` (родитель), И `rel_main` (корень) одновременно.
   - Легаси `find_dep`/`related_records` делали это рантайм-перебором
     всех таблиц (`mysql_list_tables` на каждый чих) — «с кучей запросов,
     зато да». У нас граф компилируется в снапшот ОДИН раз, рантайм не
     ищет (§8).
   - Две корневые (main и well) — артефакт слетевшего тест-режима, НЕ
     норма: корень один, `main` (в дампе все `rel_main`, `steps.dep_main`).

   **Шаг 1 — композитная подпись объекта.** `record_label($db, $snapshot,
   $table, $id)` — подпись САМОЙ записи: три ветки — проекция через
   `lookup_labels` (переиспользован исполнитель а2) → `data_name` → `#id`.
   Попутно закрыт дефект резолвера: таблица с шаблоном, на которую НИКТО
   не ссылается voc-полем, раньше не компилировалась вовсе (подпись
   объекта = `#id`). `snapshot_build_dictionaries` дособирает планы для
   любой таблицы с маяком под ключом `@table` (`@` невозможен в имени
   поля — коллизий нет).

   **Шаг 2 — компиляция графа.** `snapshot_build_relations(structure)` —
   чистая функция над структурой, ноль SHOW. Обратный индекс
   `model.relations[parent] = [{child, fk}]` из `dep_`-полей. `rel_main`
   компилируется в отдельный разрез (`model.relations_root`), НЕ
   используется в рендере («нисходящих достаточно»). Врезано в
   `snapshot_build` + `snapshot_validate`.

   **Шаг 3 — карточка тянет детей.** `record_children($db, $snapshot,
   $table, $id)` — по `model.relations[$table]` один запрос на дочернюю
   таблицу (`WHERE dep_<table> = id`), подпись строк через `record_label`.
   index.php: блоки детей под формой edit + «добавить ребёнка» на `new`
   дочерней с предзаполненным родителем. Единственная точка касания
   записи: предзаполнение `dep_` — через доверенный параметр задания
   (`_parent_table`/`_parent_id`), сверяемый с `model.relations` И
   существованием родительской записи, НЕ сырой `dep_`-инъекцией из
   POST (§9). `record_save` получил структурный канал: **insert-only**
   (на UPDATE игнорируется — reparent остаётся отдельным защищённым
   действием, п.5), с проверкой `kind==='structural'` на каждое поле
   канала — не-структурное поле в канале отклоняется.

   Смоук: секция 1г, 7 проверок (граф на синтетике по топологии
   легаси-дампа, fail-fast на `dep_` в никуда, живые `record_children`/
   `record_label`). Полный HTTP-цикл подтверждён через `php -S`: форма
   с контекстом родителя → POST → 422 при неподтверждённой связи →
   redirect в карточку родителя → ребёнок виден в блоке. Все 10 секций
   смоука + 1а-1г зелёные на целевом localhost, 2026-07-08.

   **Два бага найдены и закрыты в процессе (журнал 07-08), оба —
   собственные регрессии дособорки `@table`:** (1) `compile()` в
   резолвере мемоизировал только успех, не провал — повторный вызов
   на уже проваленном источнике (из дособорки `@table` после провала
   в проходе 1) дублировал ошибку в `unresolved`; (2) первый фикс #1
   проверял памятку-о-провале РАНЬШЕ проверки цикла — циклический
   повторный вызов тихо гасился памяткой, не долетая до детекта цикла.
   Верный порядок: успех → цикл (в работе) → провал (завершён ранее).
   Учить на будущее: любая мемоизация в рекурсивном компиляторе обязана
   различать «ещё в работе» (цикл) и «завершилось неудачей» (не
   дублировать) — смешение этих двух состояний ломает то одно, то другое.

   **Границы (не сегодня):** `rel_main`-рендер (плоский срез дерева) —
   компилируется, не показывается; каскад при удалении родителя —
   открытый вопрос; пагинация блоков детей — «Позже».
4. **Конфигуратор БД v0 — ВЫПОЛНЕНО 2026-07-05**, живой цикл подтверждён
   на целевом localhost (создание `voc_usc`: форма → DDL → регистрация в
   model_registry/model_labels → пересборка snapshot → таблица видна и
   выбираема из формы без ручных действий). Отдельный файл
   `configurator.php` (см. файловое пространство). Реализовано:
   - живой слепок для списка/форм (снапшот-кэш не участвует, §8
     админ-режим), бейдж «используется как словарь» по факту адресации;
   - создание таблицы двумя путями: обычная (свободное имя, поля из
     `entities()` + синтетический `id`) и словарь уровня 0 (только
     именная часть без префикса — `voc_` подставляет код; поля `id`+
     `data_name` строит сервер, форма их не спрашивает — это и есть
     определение уровня 0, не место для доп. атрибутов);
   - `voc`-поле выбирается из живого списка существующих словарей
     (не свободный текст), подпись автоподставляется из `model_labels`
     цели, остаётся редактируемой (та же таблица в другой роли);
   - тип `id` скрывает имя/подписи в форме — сервер их и не спрашивал;
   - удаление — трёхступенчатое раскрытие подтверждения.
   Явно вне v0: ALTER (добавление поля к существующей таблице — ссылка
   «редактировать» пока заглушка), паспорт-словари (§16 уровень 2),
   составные сущности (date и подобные), любая авторизация (см. открытые
   вопросы — периметр прав по таблицам заявлен как обязательный будущий
   контур, не забыт).
5. **reparent как отдельное защищённое действие — ВЫПОЛНЕНО 2026-07-14
   (только dep_, rel_main сознательно не в объёме).** `record_reparent()`
   (core.php) пишет ровно одну колонку `dep_<parent>`, не задний ход
   `record_save` — её структурный канал остался закрытым на UPDATE, как
   и было решено изначально. Имя FK-поля не приходит из request —
   резолвится `record_parent_relation()` из собственных структурных
   полей таблицы (dep_-префикс снимается тем же способом, что и в
   `snapshot_build_relations`); больше одного `dep_` на таблице сегодня
   нигде в модели нет, появится — fail-fast, не угадывание (§16).
   Из request попадает только id нового родителя, проверяется
   существованием (`record_fetch`). Каскада нет по построению — потомки
   ссылаются на id самой записи, не на корень, перенос ветки их не
   касается.

   UI: действие `reparent`/`save_reparent` в index.php, вне `action_mode`
   (это не режим сущности — не идёт через `field_exec`). Форма —
   `record_reparent_view()`+`render_reparent_form()`, тот же трёхслойный
   инвариант view-сборки, что у списка/карты/формы (п.8). Ссылка «⇄» на
   карте объекта — только у записей с однозначной `dep_`-связью (флаг
   `reparentable` вычислен в `record_tree()`, render его не пересчитывает).

   Смоук — секция 1д: резолв связи синтетикой (одна/ноль/две dep_-связи)
   + `record_reparent()` живьём на двух fail-fast путях (таблица без
   dep_, неизвестная таблица) без единой записи в БД. Happy path записи
   (реальный UPDATE) синтетикой/фикстурой не покрыт — своей child-таблицы
   у изолированной фикстуры `main` нет, заводить её ради одной проверки
   признано лишним; **проверено вместо этого вручную через UI на боевом
   дереве** (Влад, клик «⇄» на `cementing_stage`, подтверждено). Честная
   граница: секция 1д доказывает резолв и оба отказа автоматически при
   каждом прогоне; сама запись подтверждена один раз, руками — не
   гарантирована регрессионным прогоном на будущее. Если понадобится
   такая гарантия — фикстура-child для `main` остаётся открытой задачей
   (не сделана, не забыта).
6. **Снапшот: тумблер режима + автоперестройка конфигуратором — ВЫПОЛНЕНО 2026-07-08.**
   Автоперестройка конфигуратором оказалась УЖЕ реализованной (часть
   «Конфигуратора v0», 07-05) — просто не была увязана с формулировкой
   п.6: `configurator_create_table`/`delete_table` уже гоняют протокол
   §8 целиком, ручной `rm snapshot.php` после структурных операций не
   нужен. Дореализован только тумблер `snapshot.mode` в config: ветка
   `live` в `snapshot_init` — прямой `return snapshot_build(...)` без
   файлового пути, лок проверяется одинаково первым шагом в обеих
   ветках (dev-режим не отменяет уважения к `schema.lock`). Дефолт
   `cached`. Смоук — секция 1в (live собирает и не пишет файл; дефолт
   cached). **Урок для смоука (журнал 07-08):** первая версия секции 1в
   проверяла тумблер запуском второго PHP-процесса (`PHP_BINARY` +
   subprocess) — хрупко, зависит от окружения сервера, на боевом
   `PHP_BINARY` оказался пуст → `Permission denied`. Правильно — в одном
   процессе, вызовом `snapshot_build()` напрямую (это и есть live-путь
   буквально). Принцип: **проверки смоука живут в одном процессе, без
   subprocess/PHP_BINARY.**

   Исходный контракт (для истории):
   Снимает недельную боль ручного `rm state/snapshot.php`. Два связанных
   шага, оба — тонкий слой поверх уже написанного, не новый механизм:
   - **Тумблер режима в config** — одно значение `snapshot.mode`:
     `cached` (по умолчанию, боевой путь — `include` файла через
     `snapshot_init`, как сейчас) либо `live` (dev-путь — та же
     `snapshot_build()` БЕЗ шага записи; результат живёт один рендер
     в памяти). Функция сборки одна, второго пути не появляется —
     «тонкая труба» цела. Флаг — параметр окружения в системном конфиге,
     **не** request (GET/POST): иначе обычная форма получает рычаг к
     schema-поведению (нарушение §15.4). В `live` staleness исчезает по
     построению — файла нет, нечему протухать/сиротеть. Прод `live`
     не ставит никогда — живая интроспекция как штатный режим прода
     остаётся табу §12.
   - **Конфигуратор сам перестраивает снапшот** в конце структурной
     операции — оформление протокола §8, который всю неделю проходили
     руками: `lock → DDL → запись в model_registry (+labels) →
     snapshot_rebuild_structure (собирает и атомарно пишет temp+rename) →
     unlock`. В `cached` цикл сохраняет файл; в `live` шаг записи пропущен.
     Ручное `rm` уходит из нормального workflow — остаётся только аварийной
     dev-операцией.

   Названная цена: живя в `live`, перестаёшь прогонять сам файловый путь
   (`include`, atomic write, холодный старт), а в проде работает именно он.
   Гарант остаётся один — смоук, всегда строящий + пишущий + читающий
   настоящий файл (секция 1). Пока смоук трогает файл — dev-режим выключает
   неудобство, не выключая покрытие продового пути.
   Остаточный класс (НЕ блокер сегодня): падение конфигуратора между `lock`
   и `unlock` оставляет осиротевший `schema.lock` → 503 (PHP `finally` не
   отрабатывает при fatal/exit, журнал 2026-07-03). Лечение пока ручное
   `rm state/schema.lock`; автоснятие сироты — открытый вопрос, повышенный
   до обязательного перед приходом нескольких писателей (см. открытые).
7. **Построение дерева Листа2 через конфигуратор — ВЫПОЛНЕНО ЦЕЛИКОМ
   2026-07-14 (проверено боем, реальные данные).** Дерево построено через UI
   конфигуратора целиком (без ручного SQL, §15.10 подтверждён живьём),
   наполняется реальными записями через `index.php`. Корень —
   **`well`, не `main`** (подтверждено live: `dep_well` в реальной схеме,
   14 полей на `well` из прошлых тестовых заходов, включая карантинный
   `calc_spike` — см. журнал 07-09). Формулировки ниже и в остальном
   документе, где встречается `main` как имя корня для Листа2, читать
   как `well`; сама доктрина (§16.1, `rel_<root>`) от переименования не
   меняется. Дерево:
   ```
   well → cementing_stage → cementing_interval → cementing_buffer (1:многие)
                        └→ cementing_reper → cementing_reper_component (1:многие)
   ```
   Поля: `cementing_stage` — `int_stage_number`, репер объём/плотность,
   давление раб/стоп; `cementing_interval` (ГЛАВНЫЙ поставщик данных
   листа) — `dec_depth_from`/`dec_depth_to`, `voc_material`, количество,
   объём план/факт, плотность план/факт; `cementing_buffer` — объём,
   `voc_composition`, вес (1:многие — дамп строки 5-6, два буфера на
   строку); `cementing_reper_component` — `voc_component`, `dec_amount`
   (состав с дозировками, НЕ `mlt_`). Группы полей (от/до, план/факт) —
   конвенция имени `<тип>_<смысл>_<роль>`, БЕЗ слоя групп (см. «Позже»).
   № п/п / площадь / № скв — подстановка из `well` через `record_label`,
   локально НЕ хранятся. «Количество ступеней» — считать через
   `count(record_children('well'))`, НЕ хранить (иначе синхронизация
   против «тонкой трубы»).
   Физика процесса (журнал 07-09, для будущей валидации диапазона —
   не реализовано, только домен-факт): раствор закачивается сверху,
   выдавливается вверх — `depth_from` глубже, `depth_to` мельче;
   правило будущей проверки диапазона будет `from > to`, НЕ `from < to`.

   **Статус на 2026-07-14 — ЗАВЕРШЕНО (проверка боем данными через
   браузер, расширение Claude в Chrome, за два захода 07-13/07-14):**
   вся скважина укомплектована полностью, обе ступени, все интервалы,
   все буферы, репер с тремя компонентами — сверено с исходной
   реконструкцией листа тампонажа, совпадает без расхождений. Попутно:
   в словаре `voc_component` не хватало «КМЦ» — доведён по ходу; поле
   `cementing_buffer.dec_dec_volume` (двойной префикс, ошибка при
   ручной сборке под копирку) — известно, НЕ почищено, инструмент
   ALTER для этого уже есть — мелкая косметическая правка на будущее,
   не блокирует ничего.
   **Карта объекта — ВЫПОЛНЕНО 2026-07-09** (см. журнал): `_action=view&_id=N`
   в `index.php` разворачивает всё дерево от записи до листьев на одной
   странице (`record_tree()` в core.php + `render_object_map()` в
   index.php), без ограничения глубины. Наследник легаси `db_tree()`
   с двумя исправлениями: граф из скомпилированного `model.relations`
   (не сканирование БД на узле), обход отделён от вывода. Форма `edit`
   осталась чистой формой без карты.
8. **View-слой (presentation assembly) — а/б/в/г ВЫПОЛНЕНО ЦЕЛИКОМ,
   2026-07-11/12.** Инвариант трёх слоёв утверждён 2026-07-10 (журнал).
   Средний слой между полем и страницей, отсутствие которого породило
   кустарные обходы. Три ступени: поле (`field_exec`) → view-сборка
   (core.php, только массивы) → рендер (render.php, массив→HTML).
   Сделано:
   - **(а) 07-11:** `render_object_map` вынесен из index.php в
     `render_object_tree` (render.php) — чистый перенос.
   - **(б) 07-11:** `record_view_columns`/`record_view_row` (общие кирпичи),
     `record_table_view` (список), `record_tree` прикрепляет готовое
     `view` к узлу; `render_record_table` (укладчик). Список и карта —
     одна пара функций, один стиль `.data-list`. Инвариант «render не
     лезет в БД» восстановлен (вчерашний вынос его нарушал: рендер карты
     сам звал разбор полей — исправлено, разбор ушёл в обход дерева).
   - **(в) 07-11:** `record_form_view` + `render_record_form` — форма
     new/edit через сборщик. index больше НИГДЕ не разбирает поля вручную
     (список/карта/форма — все через шов); стал тонким дирижёром.
     Фикс: `record_form_view` принимает `?array $row` (new = записи нет).
   - **(г) 07-11/12:** `table_group()`+`schema_view()` (core.php),
     `render_schema_card()`+`render_table_directory()` (render.php) —
     конфигуратор показывает 4 группы таблиц (главные с деревом
     зависимых в семейном блоке, отчёты, служебные, системные), поля
     таблицы в столбик по 5. `labels.php` первая страница переведена на
     тот же `render_table_directory` — «сверху одно и то же, под
     капотом разное» (Влад, журнал 07-11); её собственный редактор
     подписей/значений остаётся её спецификой. Кустарные `echo
     '<table>'` в конфигураторе и labels убраны целиком.
   Запреты (журнал 07-10): не режим `list`, не новые входные файлы, не
   HTML в entities.php/index.php/configurator.php.
9. **Переход на Postgres — начато 2026-07-16, полный катор (MySQL убирается
   из кода целиком, не dual-driver).** Отменяет решение 07-13 «не сейчас» —
   см. журнал 07-16 для повода. Объём переоценён на месте: обследование
   07-13 насчитало ~150 мест `mysqli_*`, но с тех пор (07-14) весь этот
   «водопровод» уже стянут в `db_select`/`db_execute` (см. п.0) — почти
   вся масса правки схлопнулась в один файл. `entities.php` — по-прежнему
   ноль обращений к БД, не трогается. Порядок выполнения (простое →
   сложное, по прямой директиве Влада 07-16):
   1) **связь/подключение** — `db.php` (`db_select`/`db_execute` на
      `pg_query_params()`; конвертация `?`→`$N`-плейсхолдеров ВНУТРИ этих
      двух функций, не в вызывающем коде — ни один `record_*`/
      `snapshot_build_*`/`lookup_*` не меняется, они как писали `?`, так
      и пишут); управление соединением (`config.php` + мест, сознательно
      оставленных вне db.php — `pg_connect`/`pg_close`, кодировка через
      `client_encoding` в строке соединения); интроспекция схемы
      (`snapshot_build_structure`, core.php, единственное bootstrap-место
      — `information_schema.tables/columns` вместо `SHOW TABLES/COLUMNS`,
      маппинг в тот же внутренний вид полей);
   2) **сущности и типы** — ревизия `entities.php` (секция `db.default_type`/
      `allowed_types` каждого паспорта): `mediumint` не существует в
      Postgres → `integer`; `tinyint(1)` (`bul_`) → `boolean`; `decimal`→
      `numeric` (переименование). DDL-билдер (`configurator.php`):
      `AUTO_INCREMENT`→`GENERATED ALWAYS AS IDENTITY`; `ENGINE=InnoDB`/
      `CHARSET`/`COLLATE` — убрать (не табличный уровень в Postgres);
      обратные кавычки — убрать вовсе, не менять на двойные (имена и так
      нижний регистр без пробелов, неквотированные идентификаторы Postgres
      сам приводит к нижнему регистру). `labels.php`: `ON DUPLICATE KEY
      UPDATE` → `ON CONFLICT (dep_model_registry) DO UPDATE SET ...`
      (конфликт-таргет — уже известный PK `model_labels`);
   3) **мультичойсы и соединительные таблицы** — настройка `mlt_`/будущей
      настоящей m2m поверх готового Postgres-фундамента; не спроектировано,
      ждёт своей очереди после 1) и 2), по прямой директиве Влада (не
      забегать вперёд).
   Тестовая БД собирается ЗАНОВО через конфигуратор на Postgres, не
   переносом `gpdp_test.sql`/боевых данных (решение 07-16, Влад: «тестовую
   базу соберём заново») — снимает задачу конвертации дампа целиком.
   `smoke_test.php` — ревизия на MySQL-специфичные предположения при
   первом касании. Окружение: `pgsql`/`pdo_pgsql` для PHP, сервер Postgres
   на целевом localhost, роль+база — предусловие, не код.

   **Шаг 1 (связь/подключение) — код написан 2026-07-16, НЕ проверено
   живьём (нет PHP pgsql-расширения на целевом localhost на момент
   написания).** Сделано: `db.php` переписан на `pg_query_params`
   (`?`→`$N` внутри файла, вызывающий код не менялся); `db_connect()`/
   `db_close()` заведены и подставлены во ВСЕ точки подключения — не
   семь, а фактически шесть источников (`admin_db_connect()` в
   helpers.php обслуживал index.php/configurator.php/labels.php уже
   одной функцией — находка при разборе, не была отражена в обследовании
   07-13) плюс четыре самостоятельных консольных скрипта
   (`diag_refresh.php`, `smoke_test.php`, `tools_bulk_import.php`,
   `tools_rebuild_snapshot.php`); тип `mysqli` → `PgSql\Connection`
   заменён примерно в 30 сигнатурах (`core.php`, `configurator.php`,
   `helpers.php`, `tools_bulk_import.php`, `labels.php`) — реальный
   объём правки типа оказался больше, чем «внутри db.php», как
   формулировка шага 1 звучала изначально в этой же записи; отдельно
   найден и закрыт `mysqli_begin_transaction`/`mysqli_rollback` в
   `tools_bulk_import.php` (dry-run транзакция) — не был учтён в
   обследовании 07-13 вовсе, обнаружен только сплошным grep по
   `mysqli_` после основной правки. `snapshot_build_structure`
   (core.php) переведён на `information_schema`/`pg_catalog` вместо
   `SHOW TABLES`/`SHOW COLUMNS`. Обратные кавычки убраны из ~56 строк
   реального SQL-текста (`core.php`/`configurator.php`/`labels.php`/
   `helpers.php`) — оказалось не «46 мест» из обследования 07-13
   (та цифра была про кавычки-замену один-в-один, здесь — полное
   удаление, идентификаторы и так строчные). DDL-билдер
   (`configurator_create_table`) — `id` на `GENERATED ALWAYS AS
   IDENTITY`, структурные `dep_`/`rel_main`-колонки на `INTEGER`,
   `ENGINE=`/`CHARSET=`/`COLLATE=` убраны целиком; `ALTER`/`DROP`
   очищены от кавычек. Пять `INSERT` в `model_registry`, чей `id`
   читается сразу после (`configurator_create_table` ×2,
   `configurator_add_field`, `configurator_adopt_field`,
   `configurator_adopt_table`), и `record_save()` (core.php,
   единственный универсальный insert-путь) получили `RETURNING id`.
   `labels.php`: `ON DUPLICATE KEY UPDATE` → `ON CONFLICT (
   dep_model_registry) DO UPDATE`. Новый файл `bootstrap_postgres.sql`
   — DDL четырёх системных `model_*`-таблиц в синтаксисе Postgres;
   **реконструкция по STATE.md + коду, не копия оригинала** (репозиторий
   никогда не версионировал этот SQL — таблицы заводились ad-hoc,
   журнал 07-05/07-08/07-14/07-15) — требует сверки Владом со старой
   боевой схемой перед запуском.

   **Осознанно НЕ тронуто в шаге 1** (следующие шаги той же записи):
   `entities.php` — типы колонок паспортов (`UNSIGNED`/`TINYINT(1)`/
   `MEDIUMINT` всё ещё в MySQL-виде, шаг 2); `smoke_test.php` — фикстуры
   `voc_smoke`/`main`, счётчик N+1 через `SHOW SESSION STATUS LIKE
   'Questions'` (аналога в Postgres нет в том же виде — нужен другой
   механизм, например `pg_stat_statements` или счётчик на уровне
   тестового кода) и статическая проверка «в index.php нет SQL» (сейчас
   ищет строку `mysqli_query|mysqli_prepare` — тривиально пройдёт даже
   если правило нарушено, раз этих имён в проекте больше нет нигде;
   нуждается в собственном пересмотре, не просто переименовании
   `mysqli_`→`pg_` в тексте проверки).

   **`smoke_test.php` — ЗАКРЫТ 2026-07-16, все 70 проверок зелёные,
   живьём на сервере Влада.** Довёл до конца отложенный хвост шага 1
   (Влад открыл файл через браузер по незнанию, что это CLI-инструмент
   — получил HTTP 500, лог Apache показал точную причину: сырой
   `mysqli_query()` на `PgSql\Connection`). Сделано: фикстуры
   `voc_smoke`/`main` заведены через конфигуратор (минимальные — только
   `data_nkust`+`voc_smoke`, весь граф `dep_`/`rel_main` в тесте
   синтетический, живой БД не касается вовсе, выяснилось при чтении
   файла целиком); все `mysqli_query`/`mysqli_real_escape_string`
   заменены на `db_select`/`db_execute` с `?`-плейсхолдерами (никакого
   ручного экранирования — параметризация решает вопрос сама); `UPDATE
   ... JOIN ... SET` (MySQL-синтаксис, для правки `model_labels` через
   `model_registry`) переписан на Postgres `UPDATE ... SET ... FROM
   ... WHERE` — условие связи переехало из `ON` в `WHERE`; статическая
   проверка «нет SQL в index.php» — паттерн `mysqli_query|mysqli_prepare`
   заменён на `pg_query_params|pg_query\(` (тот же принцип проверки,
   актуальный список запрещённых имён). Счётчик N+1 (`SHOW SESSION
   STATUS LIKE 'Questions'`) заменён на новую функцию `db_query_count()`
   в `db.php` (`pg_stat_statements` отвергнут — требует superuser/
   shared_preload_libraries и считает по БАЗЕ, не по соединению, шумит
   от чужих сессий на общем сервере; собственный счётчик точнее для
   своей же цели — считает ровно то, что выполнил `db.php`).

   **Найден и закрыт один настоящий баг, не покрытый прежними
   грепами:** `record_delete()` — `DELETE FROM ... WHERE id = ? LIMIT
   1`. `LIMIT` на `DELETE` — расширение MySQL, в Postgres такого
   синтаксиса нет вовсе (ошибка парсера, не предупреждение). Ни один
   грep по `mysqli_`/бэктикам это не ловил — не тот класс различия.
   Убран целиком, не заменён — избыточен был и под MySQL: `id` —
   первичный ключ, `WHERE id = ?` и так задевает максимум одну строку.
   Урок на будущее: пометка «контракт не менялся» для функций,
   проверенных только по типу параметра/грепу известных диалектных
   слов, не гарантирует отсутствие СИНТАКСИЧЕСКИХ MySQL-расширений
   внутри тела — нужен отдельный проход по `LIMIT`/`IFNULL`/
   `GROUP_CONCAT`/`ON DUPLICATE`/`STRAIGHT_JOIN` и подобным (сделан
   постфактum, по всему ядру — больше ничего не нашлось, кроме уже
   закрытого `ON DUPLICATE` в `labels.php`).

   Живой прогон Влада (`php smoke_test.php`, из `/var/www/html/gpdp`,
   терминалом — файл CLI по устройству: сам поднимает и убивает
   встроенный PHP-сервер через `shell_exec`/`exec`, через браузер/
   Apache не задуман независимо от диалекта БД): **70/70 OK, «ВСЕ
   ПРОВЕРКИ ПРОЙДЕНЫ»**, включая живой HTTP-цикл (view→new→save→PRG→
   view) через `php -S` и N+1-счётчик (0 лишних запросов на 30
   обращений к `lookup_labels`). Шаг 1 переезда на Postgres закрыт
   полностью, без единого известного пробела.

   **Живой прогон 2026-07-16 — ПОДТВЕРЖДЁН, шаг 1 закрыт целиком.**
   PHP-расширение `pgsql` поставлено (Влад), `bootstrap_postgres.sql`
   прогнан на `gpdp_test`. Полный цикл через конфигуратор: пустая база
   → `configurator.php` открывается без ошибок (`information_schema`
   читается) → создание словаря уровня 0 через форму → DDL (`id`
   GENERATED ALWAYS AS IDENTITY) → регистрация в `model_registry` +
   `model_labels` (`RETURNING id`) → подпись отображается корректно
   (не сырое имя таблицы) → «Состояние модели»: «Структура и реестр
   согласованы. Расхождений нет.»

   **Один реальный баг найден и закрыт по ходу проверки, не гипотетически:**
   `model_registry.active` в `bootstrap_postgres.sql` изначально был
   объявлен `BOOLEAN` (по аналогии с планом `TINYINT(1)`→`boolean` для
   будущего шага 2) — но весь код (`core.php`/`configurator.php`/
   `labels.php`, ~10 мест) пишет и читает его как литеральное целое
   (`VALUES (...,1)`, `WHERE active = 1`), не PHP true/false. Postgres
   не приводит `integer`↔`boolean` неявно — INSERT/WHERE падали
   ошибкой типа. Хуже: `configurator_create_table` не проверяла ошибку
   этого INSERT (проверялась только ошибка самого `CREATE TABLE`) —
   таблица физически создавалась, а регистрация в реестре ТИХО
   проваливалась, что и обнаружилось диагностикой конфигуратора
   («расхождений: 2») на первой же тестовой таблице. Исправлено
   заменой типа на `SMALLINT` в `bootstrap_postgres.sql` — простой
   фикс на уровне схемы вместо правки ~10 мест кода. Урок: план
   типов «TINYINT(1)→boolean» из обследования 07-13 верен для полей
   СУЩНОСТЕЙ (`bul_`-паспорт в entities.php, читается через
   `field_exec`, а не сравнивается с литералом напрямую в SQL-тексте
   ядра) — но не универсален для структурных/системных колонок вроде
   `active`, где решает не паспорт, а буквальный текст SQL по всему
   ядру; проверять по факту использования, не по одной лишь аналогии
   типа. Тестовые таблицы (`voc_pgtest`/`voc_pgtest2`) удалены после
   проверки, база возвращена в пустое состояние — готова для шага 2.

   **Шаг 2 (типы сущностей, entities.php) — ЗАКРЫТ 2026-07-16, проверено
   живьём.** Обнаружено: `allowed_types` — мёртвые метаданные, ни разу
   не читаются рантаймом ни в core.php, ни в configurator.php (только
   `default_type` реально попадает в DDL как `$field['db_type']`) —
   меняет объём шага: реальной правки требовало ровно ОДНО поле из
   двенадцати паспортов. `ent_bul`: `tinyint(1)` → `smallint`, НЕ
   `boolean` — сознательный выбор: php-pgsql отдаёт boolean-колонки
   строками `t`/`f` (не `1`/`0`), `bul_handler` сравнивает как
   `(int)$value === 1` — с boolean это был бы тихий баг, читающий
   любое значение как «нет» (тот же класс, что уже поймали на
   `model_registry.active`, шаг 1). Остальные одиннадцать паспортов
   (`data`/`voc`/`ltext`/`footnote`/`date`/`year`/`time`/`int`/`dec`/
   `calc`/`link`) — `default_type` уже был валиден для Postgres как
   есть (`varchar`/`text`/`int`/`smallint`/`date`/`time`/`decimal` —
   Postgres принимает эти имена без изменений), правки не потребовали.
   `allowed_types` вычищены от `mediumint`/`mediumtext`/`longtext`
   (не существуют в Postgres) — не функциональная правка, гигиена
   документации на случай, если поле когда-нибудь начнут читать.
   **Живая проверка**: таблица с полем `bul_flag` создана через
   конфигуратор (DDL прошёл), цикл `new` → save → `view` дал верный
   результат в обе стороны — отмеченный чекбокс читается как «Да»,
   снятый — как «Нет», без промежуточной ошибки. Тестовые таблицы
   (`pgtest_bul` и `voc_pgtest2` из шага 1) остаются в `gpdp_test» до
   явной уборки — не мешают следующему шагу.

   **Шаг 3 (мультичойсы/соединительные таблицы) — ОТЛОЖЕН 2026-07-16**
   (Влад: конкретного случая под рукой нет, доктрина требует
   классификации по реальному полю, не абстрактно — О_словарях.md).
   Вместо него: восстановление дерева Лист1+Лист2 на Postgres,
   Лист3/Лист4, автозагрузка тестовых данных — см. ниже.

   **Дерево Лист1+Лист2 — ВОССТАНОВЛЕНО на Postgres 2026-07-16, живьём.**
   Через конфигуратор (браузер, без ручного SQL, §15.10) заново собраны
   восемь словарей уровня 0 (`voc_mr`, `voc_enterprise`,
   `voc_subsoiluser`, `voc_technolog`, `voc_usc`, `voc_material`,
   `voc_composition`, `voc_component`), `well` (14 полей), дерево
   `well → cementing_stage → cementing_interval → cementing_buffer`
   (1:многие) `└→ cementing_reper → cementing_reper_component`
   (1:многие), плюс `cementing_interval.calc_volume_deviation`
   (формула `{dec_volume_plan} - {dec_volume_fact}`, добавлена отдельным
   ALTER после создания таблицы — calc_ требует существующих полей).
   «Состояние модели»: расхождений нет после каждого шага. Полный
   HTTP-цикл проверен: `well` → `cementing_stage` (через
   `_parent_table`/`_parent_id`, PRG-редирект в карточку родителя) →
   `cementing_interval` с планом/фактом объёма (15.00/15.00) — формула
   дала `0.00`, верно. Названия колонок для Postgres-версии дерева НЕ
   идентичны байт-в-байт исходной MySQL-версии (например, `int_number`
   вместо возможного `data_number` из черновика STATE.md «12. Лист 1» —
   тот текст был архивным наброском, не финальным контрактом; реальные
   имена фиксирует то, что реально создано через конфигуратор, не
   черновик). Тестовые данные Лист2 (реальная скважина/цементирование
   из `2_Тех_задание.xlsx`, ранее введённые на MySQL) — НЕ перенесены,
   только структура плюс одна проверочная запись; наполнение реальными
   данными — либо вручную повторно, либо через автозагрузку (следующий
   пункт).

   **Лист3+Лист4 — модель понята и построена 2026-07-16, живьём, ноль
   расхождений.** Разбор ТЗ-2 JSON (`2_Тех_задание_AI.json`, машинно
   вычищенная версия исходного Excel) + сверка со старым ТЗ-1
   (`1е_Задание.txt`, где смысл яснее, данных меньше) дали три ключевых
   находки, ни одна не была очевидна из самого ТЗ-2 без такого разбора:

   1. **Лист3 (лабораторные испытания) и часть Листа4 (АКЦ по
      материалам) — НЕ новые сущности верхнего уровня, а продолжение
      `cementing_interval`** (прямая формулировка Влада: «Страница 3 —
      продолжение интервалов цементирования»). Подтверждено данными:
      `laboratory_tests.materials[]` — ровно 4 записи, 1-в-1 с 4
      интервалами по имени материала; температура испытания внутри
      записи совпадает с реальной глубиной интервала (60/48 °C для
      мелких, 80/65 °C для глубоких) — это не абстрактный сертификат
      материала на все времена, а характеристика ИМЕННО этой заливки.
      Старое ТЗ-1 п.25 говорит то же самое про АКЦ по материалам:
      «привязка к марке материала, п.14» (п.14 = сам интервал).
      Плотность из теста НЕ продублирована отдельным полем — она
      совпадает с `dec_density_plan` интервала один-в-один в обоих
      проверенных случаях, это то же число, не новая информация.
   2. **«Мультичойсы» (отложенный шаг 3) нашлись сами, без специального
      проектирования.** Химреагенты испытания (наименование + %) —
      ровно тот же паттерн, что уже построенный `cementing_reper_
      component`: зависимая таблица с `voc_`-ссылкой и параметром
      (`dec_amount_percent`), не m2m/junction. `ent_mlt` по-прежнему
      не нужен — доктрина О_словарях.md сработала на реальном случае
      ровно так, как предсказывала.
   3. **Лист4 (АКЦ) — два разных по смыслу списка с одинаковой формой
      показа, не одна сущность.** Продуктивные пласты — геологическая
      реальность скважины, `dep_well`, параллельно `cementing_stage`
      (не зависит от того, чем цементировали). АКЦ по материалам —
      снова продолжение интервала (находка 1 выше). Обе разновидности
      АКЦ используют одну форму (частичное/жёсткое × с колонной/с
      породой × метры/проценты) — но НЕ через общую зависимую таблицу
      1:2: видов сцепления всегда ровно два (не переменный список,
      тот же тест «без сущности не обойтись», что уже отвёл слой групп
      полей, журнал 07-14), и «жёсткое» сцепление **во всех записях
      без исключения** не имеет процента — структурный факт домена
      (жёсткое меряют только в метрах), не случайный пропуск. Решение
      Влада: держать АКЦ по материалам и по пластам ДВУМЯ раздельными
      таблицами-сиблингами (не полями внутри `material_test`) — «иначе
      замучаемся отображать» на будущем отчётном слое.

   **Построено через конфигуратор** (без ручного SQL): словарь
   `voc_fluid_type`; `material_test` (dep_cementing_interval, 13 полей
   — В/Ц, растекаемость, Т стат./дин., давление испытания, время
   загустевания, время схватывания начало/конец — текстом, `ent_dur`
   ещё не построен — прочность изгиб/сжатие, линейное расширение,
   водоотделение, водоотдача); `test_reagent` (dep_material_test,
   `voc_component` + `dec_amount_percent`); `akc_by_material`
   (dep_cementing_interval, 6 полей); `productive_horizon` (dep_well:
   наименование, от/до, `voc_fluid_type`, `bul_peretok` — переток
   привязан к ПЛАСТУ, не к скважине целиком, по прямому обоснованию
   Влада: пласт гомогенен, опасен именно переток МЕЖДУ пластами —
   вода в нефть или потеря давления при уходе в соседний пласт);
   `akc_by_horizon` (dep_productive_horizon, та же форма из 6 полей,
   что у `akc_by_material`). «Состояние модели»: расхождений нет.
   Реальные данные из ТЗ-2 (химреагенты РТМ-75/РТМ-75 ПВ, продуктивные
   горизонты АС11/АС12(2)/АС12(3) и т.д.) — НЕ загружены, только
   структура; наполнение — следующим пунктом, автозагрузкой.

   **Реальные данные ТЗ-2 загружены 2026-07-16, живьём, ноль отказов.**
   Синтетика не понадобилась — взяты настоящие числа скважины Мамуринская
   №31 из `2_Тех_задание_AI.json` (машинно вычищенный JSON, приложен
   Владом вместе со старым ТЗ-1 для понимания смысла модели). Два файла
   через `tools_bulk_import.php` (`--dry-run` перед реальным прогоном
   на обоих): `import_dictionaries.json` — 8 словарей, 20/20 записей,
   ноль отказов; `import_tz2.json` — полное дерево `well` (Лист1) →
   `cementing_stage` → `cementing_interval` → `{cementing_buffer,
   material_test → test_reagent, akc_by_material}` (Лист2/3/4) плюс
   `cementing_reper → cementing_reper_component` и `well →
   productive_horizon → akc_by_horizon`, 32/32 записи, ноль отказов.
   Id начались не с 1 (`voc_mr` id=2, `well` id=3 и т.п.) — честная
   особенность `--dry-run`, задокументированная в докблоке инструмента:
   транзакция откатывается, автоинкрементная последовательность (в
   Postgres — `IDENTITY`/`sequence`) не откатывается никогда, это
   общее свойство SQL-последовательностей, не специфика диалекта —
   сработало на живых данных впервые, ожидаемо, не баг.

   **Визуальная сверка `well&_id=3` — точное совпадение с источником**
   по всем уровням дерева: 4 интервала (материалы ОТМ-3/ОТМ-5/РТМ-75/
   РТМ-75 ПВ), оба буфера ступени 1 (МБП-С+НТФ), три компонента репера
   (ПЦТ-G-CC-1/КМЦ/Бентонит с верными дозировками), все 4 лабораторных
   испытания с реагентами РТМ-75 (НТФ 0.01%, CFL-110 0.1%), АКЦ по
   всем 4 интервалам и по всем 3 продуктивным пластам (АС11/АС12(2)/
   АС12(3)). `calc_volume_deviation` посчитан верно на всех интервалах
   (0.50, 0.00, -1.00, -0.50) — формульный движок пережил переезд на
   Postgres без единой правки логики вычисления (правился только слой
   БД под ним, шаги 1-2). Тестовая база ТЗ-2 полностью функциональна
   на Postgres — от подключения до конкретной живой скважины со всеми
   четырьмя листами исходного технического задания.

Правило миграции SQL (**шаг 1 выполнен 2026-07-03**): новый SQL пишется
только в трёх семействах — `record_*` (весь рабочий CRUD, включая чтение:
`record_fetch`, `record_list`), `snapshot_build_*` (интроспекция bootstrap),
`lookup_*` (словари). index.php SQL не содержит — контракт охраняется
статической проверкой смоука. Шаг 2 — единая труба исполнения `sql_execute`
(prepare/bind/execute/ошибки в одном месте) — в слое «Позже», до первого
реального повода. Конструктор запросов общего назначения — табу §12.

## Референсная задача: тампонаж (ТЗ-2, Excel)

Критерий зрелости: **ТЗ-2 — не приложение, которое надо программировать,
а проверка, собирается ли оно из модели, сущностей и конфигурации.**
Каждый шаг дорожной карты сверяется с вопросом «приближает ли к сборке
тампонажа из конфигурации без правки ядра».

Требуемые способности → привязка к карте:

| Способность | Слой |
|---|---|
| скважина → ступени → интервалы → материалы; скважина → пласты → АКЦ | «Сейчас» п.3 (rel_/dep_) |
| словарь с собственными зависимыми данными (марка материала: применение + испытания + АКЦ) | «Сейчас» п.3 |
| состав материала (реагент + %), буфер (тип + объём/вес) | переклассифицировано по словарной доктрине как контекстные/зависимые таблицы («Сейчас» п.3) + словари п.1. **Уточнение 2026-07-06**: физически это те же FK+payload, что у junction-таблицы («буфер» = interval_id+material_id+объём/вес) — отличие пока ТОЛЬКО в имени (`dep_`/`rel_` как маркер владения), не в поведении: каскад и защищённый reparent не реализованы. До первого построенного 1:многие `dep_`-кейса это план, не проверенный факт |
| производные словари (реагенты материала, материалы скважины) | паспорт проекции с фильтром (role=filter) — слой «Позже», дом готов в п.1 |
| выбор скважины/родителя в формах («Мамуринская №31») | проекция с конкатенацией — «Сейчас» п.1 |
| дата | «Сейчас» п.2 |
| число с единицей измерения (м, т, м³, г/см³, МПа, %) | подпись в model_labels, не сущность |
| группировка полей в отображении (план/факт, двухуровневые шапки) | presentation-слой snapshot/labels — открытый вопрос |
| формат «ч-мм» (сроки схватывания) | решать при ent_date; отдельную сущность не плодить заранее |
| интервал «от/до» | два числовых поля; составная сущность — только когда появится поведение (валидация) |
| отчёты, группировки, агрегаты, проценты сцепления | слой «Позже»; граница «сложный словарь ↔ простой отчёт» размыта намеренно — оба суть проекции |
| многовидовость записи (4 листа = 4 формы одной скважины) | form contract `_form_id`, слой «Позже» |

Отклонено при анализе (см. журнал): инфляция композитных сущностей
(plan_fact, unit_value, percent, money, workflow_state...), каталог
«процессоров» впрок, аудит/версионирование на этапе базы, импорт Excel
как механизм runtime.

## Файловое пространство (утверждено)

```
index.php     дирижёр жизненного цикла запроса
config.php    окружение: БД, пути, application (root_table временно здесь)
core.php      ядро: entity_* field_* snapshot_* action_* record_*
entities.php  паспорта ent_* и функции сущностей <id>_*
helpers.php   инструменты сущностей: lookup_* (form_*, sql_* в будущем)
render.php    render_*: структурированный результат → HTML/JSON/...
smoke_test.php  инструмент проверки контрактов; в рабочий цикл не входит;
              запускается из консоли после КАЖДОГО переноса на сервер
configurator.php  отдельный вход, структурные операции (create/drop
              таблиц); другой периметр прав, чем index.php — сегодня
              никакого (auth не утверждён), в будущем — обязателен
              серьёзный контур доступа по таблицам (не забыто)
state/        generated: snapshot.php, schema.lock (в git — только .gitkeep)
```

Направление вызовов: index → core → entities → helpers; результат → render.
configurator.php вызывает core.php напрямую (те же snapshot_build_*/
snapshot_lock_*), в общий конвейер index.php не входит — параллельный,
не вложенный контур.
Жизненный цикл: boot → config() → db+utf8mb4 → snapshot_init →
(auth: зарезервировано) → разбор request → prepared task → конвейер →
render | PRG-redirect.

## Требования к развёртыванию (выстрадано 2026-07-04)

- `state/` должен быть доступен на запись ОБОИМ контурам: консоли и
  веб-службе. При сайте в /home — обязателен drop-in службы
  (`ReadWritePaths=<путь>/state`), иначе `ProtectHome=read-only` даёт
  EROFS только вебу («мигающая» ФС — две точки зрения на каталог).
  Чище — размещение вне /home.
- `state/`: владелец-группа единые, setgid на каталоге (`chmod 2775`),
  веб-пользователь в группе. Ручные chmod-«подтяжки» запрещены.
- `pcre.jit=0` в ini обоих SAPI (JIT упирается в MemoryDenyWriteExecute).
- Ручные прогоны — всегда от одного пользователя; root — только для
  системных действий.
- Новый localhost = залить эталонный дамп + прогнать смоук.

## Позже (не проектировать до работающей базы)

**Единый вход `index.php` через `_context`, дорожная карта (2026-07-17, решение принято, план — не начат).**
Повод — не косметика: сегодня выяснилось, что `configurator.php`/`labels.php`
сами рисуют HTML в обход `render.php` (нарушение §3 — «HTML рождается
только в render.php» — существовало и раньше, просто не проявлялось
больно, пока не понадобилась сквозная функция). Триггер (Влад):
следующие шаги — общее меню, стандартизированный вывод с обязательными
элементами, авторизация — физически требуют ОДНОГО канала вывода, не
трёх независимых генераторов HTML. Разобраны и отвергнуты два более
дешёвых варианта: (а) полное слияние в `core.php`/`render.php` с
удалением файлов (план Chat, оценка 3-5 дней — решает задачу, но
дороже, чем нужно для сформулированной причины); (б) файлы просто
подключаемыми библиотеками без выноса рендера (моё предложение —
дешевле, но НЕ решает саму задачу: HTML остался бы троиться). Принятый
вариант — третий, средний: операционная логика (DDL, валидация,
работа с реестром — то, что уже сегодня хорошо отделено от HTTP,
принимает соединение/спецификации параметрами) остаётся физически в
`configurator.php`/`labels.php`, файлы перестают быть самозапускаемыми
скриптами и становятся библиотеками функций; ВЕСЬ HTML-вывод обоих
переезжает в `render.php` — общий канал, тот самый, где будут жить
меню/авторизация/обязательные элементы.

Стадии (смоук зелёный после каждой, тот же порядок дисциплины, что на
переезде на Postgres 07-16/17):

0. **Инвентаризация — СДЕЛАНА 2026-07-17, проверено чтением файлов
   целиком, не по памяти.** `configurator.php` — 9 маршрутов + список
   по умолчанию: `create_table` (POST — покрывает и обычную таблицу, и
   `configurator_create_view`, различаются по форме спецификации, не
   отдельный маршрут), `delete_table`, `alter_add_field`,
   `alter_drop_field`, группа ремонта из пяти под-действий
   (`reg_adopt_field`/`reg_adopt_table`/`reg_drop_ghost`/`reg_drop_dup`/
   `reg_drop_column`, диспетчеризуются вложенным `match()` внутри
   одного POST-блока — не пять отдельных верхнеуровневых веток),
   `diagnose`, `new_table`, `edit`, `delete_confirm`, `list` по
   умолчанию (`$caction = ... ?? 'list'`). `labels.php` — 4 действия +
   два шага по умолчанию: `save_all` (пакетное сохранение подписей),
   `dict_add`/`dict_edit`/`dict_delete`; шаг 1 без `?table=` — каталог
   таблиц; шаг 2 с `?table=X` — форма подписей + правка значений
   словаря. **Находка, меняющая объём стадий 3-4 в меньшую сторону**:
   `render_table_directory()` (render.php) УЖЕ сегодня общая функция
   для списка таблиц в обоих файлах («содержимое»/«редактировать»/
   «удалить» — конфигуратор; тот же каталог — labels, шаг 1 без
   `?table=`) — строить с нуля не придётся, только сохранить
   вызывающий код при переносе.
1. **[СДЕЛАНО 2026-07-17, безопасная половина] Контекст определяется
   РАНЬШЕ `snapshot_init()`** (находка Chat, критическая, не мелочь) —
   `configurator`-контур обязан работать, когда снапшот сломан или
   схема заблокирована, это его смысл существования; строгий
   `snapshot_init()` — только для `data`. `$context = request_context(
   $_GET, $_POST)` — закрытый список (`REQUEST_CONTEXTS =
   ['data','configurator','labels']`, whitelist, не `'dispatch_' .
   $_GET['_context']` — тот же принцип §12, что уже держит весь
   конфигуратор); отсутствие параметра = `data` (старые ссылки не
   ломаются). Реализовано ИНАЧЕ, чем черновик ниже предполагал: не
   тихий `require` старых файлов, а честная заглушка 501 для
   `configurator`/`labels` («стадии 2-4 не начаты») — прозрачнее для
   отладки, не выдаёт себя за рабочее поведение. Условная загрузка
   `configurator.php`/`labels.php` как библиотек (стадии 2-4) — не
   начата, это была бы уже переделка самих файлов, не только
   маршрутизации. **Живая проверка**: смоук 73/73 (было 70, три новые
   — неизвестный `_context` → 400, `configurator` → 501, явный
   `_context=data` даёт тот же результат, что без параметра —
   регрессия не сломана).
2. **[СДЕЛАНО 2026-07-17] `configurator.php` — стал библиотекой И
   самостоятельным скриптом одновременно.** Реализовано ИНАЧЕ, чем
   черновик предполагал (не буквальное «убрать собственную
   инициализацию» — риск оставить файл нерабочим по прямому URL на
   весь переходный период до стадии 5): `require` → `require_once`
   (защита от фатала «cannot redeclare function» при будущем двойном
   подключении из `index.php`); весь диспетчер (`$caction=...` до
   `db_close`) обёрнут в `configurator_dispatch(PgSql\Connection
   $db_connection): void` — `$application` не параметр, берётся из
   `config()` внутри, тот же источник, что был; в конце файла —
   guard «запущен напрямую или подключён» (`basename($_SERVER[
   'SCRIPT_FILENAME']) === basename(__FILE__)`, тот же приём, что
   `if __name__ == '__main__':` в Python) — при прямом запуске
   самостоятельно подключается и зовёт диспетчер, при будущем
   подключении из `index.php` (стадия 5) — просто предоставляет
   функцию. Тела функций НЕ менялись ни строкой — только упаковка
   верхнего уровня, минимальный риск для самого рискованного файла
   проекта (единственный путь к DDL). **Живая проверка, полная
   регрессия**: создание таблицы → добавление поля → удаление →
   диагностика («Расхождений нет») — всё через прямой URL, guard
   отработал верно, смоук 73/73 не задет (не проверяет конфигуратор).
3. **[СДЕЛАНО 2026-07-17] `configurator.php` — HTML вынесен в
   `render.php` ЦЕЛИКОМ, ноль `echo` с тегами вне него.** Все восемь
   действий: `render_configurator_diagnose()`/
   `render_configurator_delete_confirm()`/`render_configurator_directory()`
   (маленькие, без JS) → `render_configurator_new_table()` (229 строк,
   форма создания со встроенным JS — `onKindChange`/`onFieldTypeChange`/
   `onVocPick`/`onViewSourceChange`) → `render_configurator_edit_table()`/
   `render_configurator_table_not_found()` (191 строка, цикл по полям +
   форма добавления с JS-подсказкой переменных формулы для `calc_`) →
   `render_configurator_page_open()`/`render_configurator_page_close()`
   (общая шапка/подвал, вынесенные последними). Логика (живое чтение
   структуры, счётчики данных, построение списков опций) осталась в
   `configurator.php` — санкционированное исключение §8, не HTML;
   перенесён только сам вывод, параметрами — уже вычисленные
   переменные, без переименования и без изменения содержимого HTML/JS
   ни строкой. **Живая регрессия, исчерпывающая**: создание таблицы с
   полями `int`/`voc`/`links` (JS-переключение видимости проверено
   именно на всех трёх — `voc` показал выбор словаря и автозаполнил
   подпись, `links` показал выбор цели с 11 вариантами) → добавление
   `calc_`-поля дважды (сперва намеренно невалидная формула — путь
   ошибки через flash сработал верно, НЕ добавилось; затем валидная —
   добавилось, JS-кнопки подстановки токенов дали верные `{int_num}`/
   `{voc_mr}`/`{links_colors}`) → удаление → диагностика чистая до и
   после. Смоук не тронут (не проверяет конфигуратор впрямую).
4. **[СДЕЛАНО 2026-07-17] `labels.php` — та же обработка, что
   `configurator.php`.** `require_once`, `labels_dispatch(PgSql\
   Connection $db_connection): void`, guard «запущен напрямую» в конце
   файла (тот же приём). `labels_registry_id`/`labels_save_label` →
   `model_label_registry_id`/`model_label_save` (Chat: подписи — часть
   model/presentation-слоя, не отдельное приложение; внешних ссылок на
   старые имена не было, проверено `grep`). Локальный `h()` убран —
   дословный дубль `render_escape()` (тот же `htmlspecialchars($s,
   ENT_QUOTES, 'UTF-8')`), заменён на него везде. HTML — в
   `render_labels_directory()`/`render_labels_editor()`, ноль `echo` с
   тегами в `labels.php`. Побочная находка по пути:
   `render_configurator_page_close()` (стадия 3) переименована в
   `render_admin_page_close()` — была общая функция под
   «конфигураторским» именем, теперь у неё два вызывающих (`labels.php`
   тоже), имя обобщено вслед за `render_admin_page_open()`, который уже
   называется так. **Живая проверка**: обе ступени (каталог таблиц,
   форма правки) → сохранение подписи таблицы (заголовок сменился
   верно) → добавление значения словаря (`voc_material`, id=10) →
   удаление того же значения → подпись возвращена к исходной (не
   оставлять побочный след в боевых данных); заодно `configurator.php`
   проверен на регрессию (переименованная общая функция) — «Расхождений
   нет». Смоук 73/73 (не проверяет `labels.php` впрямую, но `render.php`
   менялся существенно — прогнан как sanity-check). Заодно поправлены
   устаревшие формулировки «стадии 2-4 не начаты» → «стадия 5 не
   начата» в `index.php`/`smoke_test.php` (были правдой на момент
   написания стадии 1, устарели после стадий 2-4 тем же днём).
5. **[СДЕЛАНО 2026-07-17] `index.php` — условная загрузка библиотек по
   `_context` реально работает.** `require_once` (не `match()` с
   отдельной функцией на каждую ветку — просто `if`, тот же результат,
   меньше синтаксиса): `_context=configurator` → `require_once
   'configurator.php'; configurator_dispatch($db_connection);
   db_close($db_connection); exit;`; `_context=labels` — аналогично;
   `data` (или пустой `_context`) — прежний путь без изменений.
   Guard внутри `configurator.php`/`labels.php` (стадии 2/4) не
   срабатывает повторно при таком подключении — подтверждено
   устройством самого приёма (`basename($_SERVER['SCRIPT_FILENAME'])`
   остаётся `index.php`, не совпадает с `basename(__FILE__)` внутри
   требуемого файла), не просто «должно сработать».

   **Найден и закрыт один настоящий баг**: `configurator_dispatch()`
   закрывала соединение ВНУТРИ себя (унаследовано с исходного верхнего
   уровня файла при переносе на стадии 2 — тогда это было корректно,
   единственный вызывающий сам себя же и был), а `labels_dispatch()`
   (стадия 4, писалась позже, уже с осознанным правилом «закрывает
   вызывающий») — снаружи. Разное поведение двух функций делало
   `index.php` верным для `labels`, но двойным `db_close()` для
   `configurator` — фатал «PostgreSQL connection has already been
   closed». Приведено к одному правилу («закрывает вызывающий, не
   функция») — убран внутренний `db_close()` из `configurator_dispatch()`,
   перенесён в её guard (теперь оба guard'а и оба места вызова из
   `index.php` симметричны).

   Одна честная накладка по ходу отладки, не техническая: первая
   попытка проверить фикс живьём показала ту же ошибку — не потому,
   что фикс был неверным (гипотеза про opcache веб-SAPI, отдельный от
   CLI, была правдоподобной и чуть не привела к ненужному перезапуску
   Apache по ложному следу), а потому, что тестировал раньше, чем
   `git pull` на сервере успевал долиться (Влад: «я не всегда успеваю
   подтянуть файлы»). Урок: при быстрой серии правок туда-обратно
   стоит явно дожидаться подтверждения `git pull`, не сразу проверять
   вслед за пушем — иначе легко принять задержку доставки за
   архитектурную проблему и уйти в сторону.

   **Живая проверка, оба контекста, через настоящий Apache (не только
   встроенный сервер смоука)**: `index.php?_context=configurator&
   _action=diagnose` — «Расхождений нет», без ошибки после. `index.php?
   _context=labels` — полный каталог таблиц, идентичный прямому заходу.
   Смоук 73/73, включая две новые проверки («рабочий вызов, не
   заглушка» для обоих контекстов).
6. **[СДЕЛАНО 2026-07-17] Все внутренние ссылки/формы/redirect несут
   `_context=`.** Без генератора `context_url()`/`context_hidden()`
   (Chat предлагал) — прямая точечная правка по номеру строки (15 мест
   в `render.php`: формы/ссылки внутри `render_configurator_*`/
   `render_labels_*`, включая шаблон `render_table_directory`; 6 PRG-
   редиректов в `configurator.php`; 1 сборка `$back` в `labels.php`).
   Генератор не понадобился — объём оказался разовым (22 места), не
   постоянно растущим; можно завести позже, если появится третий
   контур. Одно место — «содержимое»/просмотр записи — получило явный
   `_context=data` вместо неявного дефолта (самоописание вместо
   умолчания). Лишний `_context=` в прямом заходе на `configurator.php`
   /`labels.php` безвреден — эти файлы его не читают, работает
   одинаково в обоих режимах доступа без условной логики.
   **Живая проверка — настоящая навигация кликами, не отдельные URL**:
   `index.php?_context=configurator` → клик «+ новая таблица» →
   контекст сохранился в URL → создание таблицы → PRG-redirect →
   контекст сохранился → клик «удалить» → подтверждение → диагностика
   чистая. То же для `labels`: клик «править» → контекст в URL →
   сохранение подписи → redirect → контекст сохранился. Оба контура
   подтверждены «ходимыми» через единую точку входа, не только
   загружаемыми один раз. Смоук 73/73 (прогнан дважды за стадию,
   оба раза чисто).

   **Довершено 2026-07-17 (тем же днём, отдельным заходом по замечанию
   Влада)**: четыре оставшихся прямых ссылки на файлы («Подписи и
   словари» в шапке конфигуратора, «← К списку таблиц» в лейблс, оба
   пункта на домашней странице `index.php`) переведены на
   `index.php?_context=...`. Главное — `configurator.php`/`labels.php`
   стали НАСТОЯЩИМИ библиотеками: guard, позволявший запускать файл
   напрямую (переходная подстраховка стадий 2/4, пока 5-6 не были
   подтверждены), заменён на чистый `redirect` без единой строчки
   логики диспетчера или подключения к БД снаружи `require` — прямой
   URL/старая закладка просто попадает на верный адрес через
   `index.php`, ни один файл больше не исполняет себя сам. Живая
   проверка: `curl -sI configurator.php?_action=diagnose` → `302
   Location: index.php?_context=configurator` (проверено до и после
   `git pull`, чтобы не спутать задержку доставки с багом — тот же
   урок, что уже был на стадии 5); аналогично `labels.php`; клик с
   домашней страницы → прямой переход без лишнего редиректа; полный
   цикл создание/удаление тестовой таблицы через `index.php` —
   пройден. Смоук 73/73 не пострадал.
7. **Смоук — частично покрыто попутно** (стадии 1 и 5 уже добавили
   `_context=configurator`/`_context=labels` — «рабочий вызов, не
   заглушка», неизвестный `_context` → 400, `_context=data` — явная
   регрессия). Не сделано: конфигуратор доступен при отсутствующем/
   невалидном снапшоте, `data` недоступен при структурном локе (или
   доступен — решить явно), контекст переживает PRG-redirect как
   отдельная автоматическая проверка (сегодня подтверждено вручную
   через браузер, не в `smoke_test.php`). Существующая проверка «нет
   SQL в index.php» — остаётся, это и есть предохранитель от буквального
   слияния (см. отвергнутый план Chat).
8. **[СДЕЛАНО 2026-07-17] `ARCHITECTURE.md` обновлён.** §6: добавлены
   `configurator_*`/`model_label_*` в таблицу семейств, `request_context()`
   отмечена как осознанное единичное исключение (не растущее семейство,
   не переименовывать в `context_*` ради формы). Резерв `adm_` «для
   конфигуратора» — подпись исправлена (в `ENTITY_RESERVED_IDS` осталось
   безвредно, но реальное имя оказалось `configurator_*` целиком).
   `ENTITY_RESERVED_IDS` (core.php) — добавлены `configurator`/`labels`/
   `context`, та же защита, что уже была у `render`/`core`/`index`.
   Диаграмма направления вызовов — добавлен контур `_context`
   (`index.php` → `configurator.php`/`labels.php` → `render.php`).
   Попутно поправлена устаревшая пометка про `root_table` («до
   появления конфигуратора» — он уже давно есть). Смоук 73/73 после
   правки `core.php` (добавление в `ENTITY_RESERVED_IDS`) — не пострадал.

Полная регрессия по каждому уже рабочему пути обязательна на
финальной проверке (не бесплатно, но дешевле, чем у плана Chat, — тела
операционных функций не меняются, меняется только точка входа и
маршрут HTML): создание таблицы, простой словарь, зависимая таблица,
словарь-представление (`VIEW`), `link_`/`links_`, `calc_`, добавление/
удаление полей, ремонт реестра, удаление таблицы/VIEW, правка
подписей, CRUD словарных значений, refresh/rebuild снапшота, PRG после
каждого действия — тот же список, что называл Chat, актуален и здесь.

**Внешний вид карточки объекта — план Chat, пп.4-7 (2026-07-17), первые
три пункта закрыты тем же днём (капнутая глубина, компактная строка,
выравнивание таблиц).** Остаток:
4. Корневая запись (скважина) — с таблицы на сетку полей: заголовок
   («Скважина №31, Мамуринская, 5 июня 2008»), поля сеткой 3-4 колонки,
   длинный текст (`Примечание`) — отдельным широким блоком.
5. Сворачивание ступеней/интервалов до заголовка+сводки, разворот по
   клику — требует JS, которого в проекте сейчас почти нет (кроме
   `onchange` в конфигураторе).
6. Варианты рендера отношения: `table`/`cards`/`inline`/`collapsed` —
   сейчас переключение table↔компактная строка жёстко зашито порогом
   `RENDER_COMPACT_MAX_COLS=4` в render.php, не выбор.
7. Паспорт представления — вынести выбор варианта рендера (п.6) в
   осознанное решение при проектировании поля/раздела, не жёсткий
   порог в коде. Архитектурно сопоставимо по объёму с `links_`/
   гибридными словарями — новая сущность модели, не косметика.

**[СДЕЛАНО 2026-07-17 — см. журнал того же дня] Проверка использования перед удалением словарной позиции (замысел
Влада, 2026-07-17) — реальная дыра, шире `links_`.** Сейчас удаление
ЛЮБОЙ строки `voc_`-словаря (в том числе через обычный `link_`, не
только будущий `links_`) молча осиротеет все ссылающиеся записи — ни
одной проверки нет вовсе, это не новый пробел от массивов, а уже
существующий, который `links_` только сделал заметнее. Идея: перед
`DELETE` строки таблицы, которая где-то фигурирует источником словаря
(есть запись в `model.dictionaries` с этим `source_table`) — искать
использование по всем ссылающимся полям (`voc_`/`link_`: `WHERE field
= id`; `links_`: `WHERE id = ANY(field)`); нашли — отказ со списком
(таблица + id записей), не тихое сиротство. Единственная точка
встраивания — `record_delete()` (core.php), он один обслуживает
удаление ЛЮБОЙ записи любой таблицы, включая словарные. Источник «кто
на что ссылается» — уже скомпилированный `model.dictionaries` в
снапшоте, не новый реестр с нуля.

Влад сам пометил объём честно шире, чем казалось на старте: «в
реляционной БД всё надо проверять» — не только словарные значения,
предположительно вся сеть `dep_`/`rel_main`/словарных ссылок разом.
Отложено намеренно, не спроектировано — заслуживает отдельного
прогона по §15, не тихого довеска к `links_`. По логике должна встать
ДО массового практического использования `links_`/вьюх на реальных
данных, не после — держать это в уме при выборе порядка следующих
шагов, не откладывать бесконечно молча.

**[ПЕРЕСМОТРЕНО 2026-07-16 — см. «Сейчас» п.9] Портируемость на Postgres — обследовано 2026-07-13, не спроектировано.**
`entities.php` (поведение полей) — НОЛЬ обращений к БД, полностью
независим от диалекта. Зависимость сосредоточена в узком «водопроводе»:
`configurator.php` (73 вызова `mysqli_*`), `core.php` (38), `labels.php`
(11), `helpers.php` (7) — итого ~150 мест, каждое требует механической
замены API вызова (не логики). Отдельно три места с настоящей разницей
синтаксиса SQL, не один-в-один: `AUTO_INCREMENT` и `ENGINE=InnoDB` при
создании таблицы (configurator.php, DDL), `ON DUPLICATE KEY UPDATE` при
сохранении подписи (labels.php) — у Postgres `ON CONFLICT ... DO UPDATE`.
Плюс кавычки идентификаторов (обратные апострофы → двойные кавычки,
~46 мест, чисто механически). Итог: ограниченная, предсказуемая работа,
не переписывание архитектуры — но не «поменять один параметр». Решение
о введении абстракции над `mysqli` (чтобы будущий переезд стал дешевле)
осознанно не принято — не была нужна сегодня.

**`link_` (Идея А) реализована и заморожена 2026-07-12/13 — см.
`ARCHITECTURE.md` §16 п.7, движок+UI в истории коммитов (`ec923cc`,
`a2f2b63`), живой пример `example_person`+`voc_color`.** Здесь в
«Позже» остаётся только неисполненный остаток: Идея Б — ссылка на
конкретное ПОЛЕ конкретной строки (не на запись целиком), кирпичик для
будущего слоя отчётов/параметризованных проекций (стресс-тест Chat
§6.2, `docs/dating_site_stress_test_2026-07-12.md`). Возможно, цель
`link_` стоит ограничить таблицами со прослеживаемой связью (через
`dep_`/`rel_main`), не разрешать куда угодно — направление Влада, не
решение.

параметризованные словари (role=filter: «реагенты данного материала») —
легаси-прецедент найден 2026-07-16 при финальной сверке `voc_utf8.php`:
`mlt_contragent` — одна таблица контрагентов с тремя булевыми флагами
(`prm_np`/`prm_zak`/`prm_bur` — недропользователь/заказчик/буровое
предприятие), из которой `mlt()` фильтрацией строила ТРИ разных
словаря на лету, не физическим дублированием. Подтверждает саму идею
(«несколько словарей в одном», формулировка Влада), но НЕ решает
сторону вставки — как именно флаг проставлялся при добавлении записи
в исходную таблицу, легаси-код на руках уже не восстановить (Влад сам
не помнит) — открытый вопрос на момент реализации, не готовый рецепт ·
шаблоны конструкций и конфигуратор проекций (создание словарных
конструкций формами; неполный шаблон = ошибка сохранения) ·
**отчёты и выборки — см. развёрнутую карточку ниже** · меню ·
настоящая m2m (`ent_mlt`) — **только после классификации по словарной
доктрине**; большинство кандидатов ТЗ-2 переклассифицированы в
контекстные таблицы · form contract (`_form_id`) · развитый UI
конфигуратора · batch-режимы (`read_many`, `preload`) · **единая труба
исполнения — см. ту же карточку, нижний уровень** · правила валидации
уровня записи/связи как конфигурация

**Два уровня работы с БД — разведены явно, не смешивать (журнал
2026-07-14, диалог с Chat + сверка).** Раньше в этом списке были две
голые строки («отчёты и выборки», «единая труба исполнения
`sql_execute`») без разграничения — рисковали слиться в одну задачу
при чтении. Это два разных по масштабу уровня:

- **Нижний уровень (уже спланирован, приоритет — см. п.0 «Сейчас»):**
  `db_select`/`db_execute` — механическая обёртка вызова
  (`prepare→bind→execute→fetch` в одну функцию вместо ~150 копий).
  НЕ конструктор SQL, НЕ план выборки — просто убирает дублирование
  вызова. Сущность в это не вовлечена вообще.

- **Верхний уровень (спроектирован по принципу, код НЕ написан) —
  `record_select($plan)`, структурированный план выборки, симметричный
  тому, что `record_save()` уже делает для записи.** Для записи:
  модель таблицы + массив значений = INSERT/UPDATE (уже так, проверено
  кодом — `record_save()` берёт поля из снапшота, значения из входного
  массива по совпадающему имени, один универсальный запрос). Для
  чтения должен появиться симметричный механизм: модель выборки +
  контекст + параметры = SELECT. НЕ готовый SQL, НЕ отдельная
  PHP-функция на каждый отчёт, НЕ универсальная строка SQL в данных —
  план, описывающий СМЫСЛ операции («операция — дети, связь — такая-то,
  id родителя — такой-то»), не текст запроса. Граница ровно та, что
  уже действует в §16.3 («текст SQL в данных не хранится и не
  исполняется») и §15 («не плодит ли абстракцию там, где хватает
  passport handler call»): план с ключом `'sql'`/`'join'`/`'expression'`
  внутри — запрещённый конструктор запросов (второй SQL внутри
  массива); план с ключами `'operation'`/`'relation'`/`'parent_id'`
  (или `'source'`/`'projection'`/`'criteria'`) — компилятор ОГРАНИЧЕННОГО,
  заранее известного набора типовых операций, разрешено.

  Что уже есть (проверено кодом, `record_children()`): типовая форма
  для `dep_` — один шаблон (`SELECT * FROM {child} WHERE {fk} = ?
  ORDER BY id DESC`), имена берутся из скомпилированного графа связей,
  ни одного запроса под конкретную пару таблиц не пишется вручную.
  `record_fetch`/`record_list`/`record_label` — та же дисциплина для
  своих канонических форм. Чего нет: типовой формы для `link_`
  (частично переиспользует словарный механизм, отдельного пути нет —
  это скорее хорошо), `rel_main` (граф компилируется в
  `model.relations_root`, но исполнитель среза не реализован), `m2m`
  (`owner → junction → target`, единой схемы нет), произвольных
  фильтров/диапазонов/сортировки/пагинации/JOIN/агрегатов/группировок.

  **Правило (сформулировано Chat, принято как есть):** ни одна новая
  предметная выборка не получает собственный SQL. Сначала определяется
  её типовая форма (список, связь, проекция, фильтр, агрегация, дерево,
  m2m, отчётный план). Форма реализуется один раз для всей системы.
  Опасность не в текущих `record_list`/`record_fetch`/`record_children`
  (канонические формы доступа, не предметные случаи) — опасность в
  первой функции вида `record_well_cementing_report()`, названной по
  смыслу конкретного экрана, не по типу операции. Такой функции пока
  нет ни одной — граница не пересечена.

  НЕ проектировать до реального повода (тот же принцип, что отложил
  таблицу весов и Postgres): типовые формы по `rel_main`/`m2m`/фильтрам
  добавлять по мере появления настоящего потребителя, не заранее.

Базовая словарная проекция (склад/паспорт, компилятор, модельный слой
снапшота) переехала из «Позже» в «Сейчас» п.1 — словари оказались
предусловием зависимых таблиц, а не надстройкой.

**Контекст, роли, фильтр отображения, слияние админ-входов (замысел
2026-07-11).** Контекст = объект + действие + роль (список открыт,
подъедет ещё). На контексте строится фильтр отображения. Роли по
нарастанию прав: простой пользователь (только отчёты — представления
данных; базовые таблицы напрямую не видит) → администратор данных
(управляет данными, видит базовые таблицы) → системный
администратор/конфигуратор (заливка БД, правка модели, почти всё).
Сегодня фильтр эмулируется разделением на файлы (index/labels/
configurator — каждый сам себе фильтр доступа); это временная несущая
подпорка, НЕ дублирование (см. журнал 07-11 — не сливать входы). Когда
дойдём: настоящий фильтр по контексту + слияние входов в один + сами
отчёты (типы таблиц-представлений). Требует пользователей, прав,
безопасности — не проектировать до работающей базы.

**Стресс-тест: сайт знакомств (внешний вход, Chat, 2026-07-12, отфильтровано
через Влада).** Полный документ + оценка Sonnet — `docs/dating_site_stress_test_2026-07-12.md`.
Задача: спроектировать модель сайта знакомств существующим кодом, не
дописывая ядро. Результат: структурный скелет (профиль, интересы/языки через
`dep_`-таблицы, лайки/match/сообщения) собирается уже сейчас без правки
ядра — доктрина «контекстная таблица вместо generic m2m» подтверждена.
Подтверждённые недостающие пункты (приоритет Chat, звучит разумно):
1) `sys_`/timestamp-семейство (`sys_created_at` и т.п., автопроставление) —
   уже было открытым вопросом, теперь подтверждено нагрузочным сценарием,
   не только теорией; 2) `bul_` (простое булево поле, checkbox/TINYINT(1)) —
   НОВАЯ, дешёвая, полезная сущность, отсутствует полностью; 3) параметры
   поля/ограничения (`NOT NULL`, `UNIQUE`, составной `UNIQUE`, индексы, FK
   для `dep_`) — конфигуратор сейчас создаёт только nullable без FK;
   4) **адрес источника поля, независимый от имени** (`voc_birth_city` и
   `voc_current_city` оба → `city`) — НЕЗАВИСИМО совпало с примером Влада
   в этом же разговоре (два цвета из одного словаря → идея `link_`,
   см. журнал 07-12) — конвергенция двух разборов, сильный сигнал;
   5) универсальная проекция/отчёты (фильтры, сортировка, пагинация) —
   уже в списке («отчёты и выборки» выше), подтверждено как центральный
   слой для выдачи анкет, не косметика; 6) row/relation validation — уже
   в списке («правила валидации уровня записи/связи»), подтверждено;
   7) модельные операции/workflow (событие→условие→транзакция) — НОВЫЙ,
   крупный, отдельный слой, ядро полей не покрывает; 8) file/image
   сущность (storage plan) — отсутствует; 9) form/view contracts
   (`_form_id`) — уже в списке, подтверждено реальным потребителем;
   10) geo/расстояние — низкий приоритет, безопасная функция в плане
   выборки достаточно на первом этапе.

**Резильентность снапшота — аварийная сборка (класс А).** При
подтверждённой неспособности читать/писать `state/` (поломка файла,
структура БД здорова) — временный откат на `snapshot_build()` в память
БЕЗ записи, с видимой индикацией аварийного режима на **каждый** ответ,
самовыключение по выздоровлении файла. Механически — близнец dev-режима
`live` (та же сборка без записи), отличие в триггере: авария по факту
поломки файла vs сознательный тумблер. Класс Б (активный `schema.lock` —
структура в незавершённом DDL) сюда НЕ попадает: там честный 503, а не
подхват полу-применённой структуры. Мотивировано инцидентом EROFS
(2026-07-04, сайт лежал часами при здоровой БД); условия безопасности —
журнал 2026-07-05.

**Форма продукта «код-раз, моделей-много»** (осознана 2026-07-07,
проектирование отложено). Один код обслуживает множество БД-моделей,
каждая БД = отдельное приложение; пользователь владеет одной/несколькими
моделями, разные пользователи — разными наборами; версия модели задаётся
на пользователя. Это SaaS-мультитенантность с изоляцией по БД — форма
известная, новизна лишь в наполнении (содержимое приложения в модели
данных, не в темах/плагинах). Три следствия для будущего (не сегодня):
снапшот принадлежит модели, а не инсталляции (идентичность модели
становится **адресом** файла, не полем-маячком); коннект = выбор активной
модели на запрос, а не глобальная константа (одна активная модель за раз
сохраняется — пул/мультиконнект НЕ проектируется); config раздваивается
на системный (где код, кого пускать, как связать пользователя с моделями)
и тенантский (какие модели, какая активна). Красная линия: назначение
модели — доверенный слой окружения, не параметр request (§15.4). Партицио-
нирование «инженер на свой участок БД» — та же группа отложенного. Не
проектировать до окончания базовой фазы (сущности, словари, зависимые
таблицы Листа 2). Точка возврата названа явно — журнал 2026-07-07 —
чтобы будущая сессия не изобретала форму заново.

**`rel_`/`dep_` — уточнённое различение (журнал 2026-07-08, по разбору
легаси param.php/button_new/db_tree, не только дампа).** `dep_<parent>` —
связь ВКЛЮЧЕНИЯ в иерархию (дерево владения): запись — ребёнок parent,
показывается в его карточке, `record_children` читает по этому графу.
`rel_<root>` — связь ПРИНАДЛЕЖНОСТИ корневому досье, ШИРЕ дерева: в
легаси покрывала не только сквозной доступ к корню для записей на
`dep_`-линии, но и **боковые таблицы вне линии** (пласты, замеры,
примечания) — те, что относятся к центральной записи, но не являются
ничьим прямым ребёнком. `record_children` их не видит и не должен;
будущая `record_root_related($db, $snapshot, root_table, root_id)` по
`model.relations_root` — читает и `dep_`-детей, и боковые `rel_`-записи
разом, как «досье» — в «Позже», не сегодня.

**`rel_main` — частный случай `rel_<root>`, не особое имя (уточнение
07-08).** `main` в легаси не был предметным понятием модели — он был
единственным системно-обязательным корнем, поэтому имя корня и имя
механизма совпали, что и создавало иллюзию, будто `rel_main` — кусок
модели данных, зашитый в системное имя. На деле формула та же, что у
`dep_<parent>`: `rel_<root>`, где `root` — имя корневой таблицы, сегодня
всегда `main`, потому что корень один. **Мультикорневость** (несколько
`rel_<root>` в одной модели, например `rel_скважины` наравне с будущим
вторым корнем) — не то же самое, что мультитенантность (много моделей)
из блока выше: это одна модель с несколькими деревьями. Логически та же
группа отложенного, не проектируется до реальной нужды — сегодня Лист2
даёт ровно одно дерево с одним корнем `main`, `rel_main` работает как
есть, код не трогается. Комментарий к `model.relations_root` в
`snapshot_build_relations` — уточнить при следующем касании `core.php`
(не сейчас, отдельным мелким заходом): сегодня написано «плоская ссылка
на корень, минуя уровни» — узко, не отражает боковые таблицы вне
`dep_`-линии.

**Группы логически связанных полей (`от/до`, `план/факт`, `объём/состав/вес`)
— отложено до первого реального потребителя, НЕ отклонено** (обсуждение
2026-07-08, Влад + Чат). Пары/тройки полей — не случайное соседство, а
логические связки: диапазон, план-факт, набор-измерение. Система подписей
(`model_labels`) их НЕ трогает — группировка через подписи запутала бы
презентацию (Чат прав); renderer тоже НЕ угадывает группу по суффиксу
имени — это была бы «магия по имени поля» (§1). Три пути кодирования
рассмотрены:
- **через тип поля** (`startdec`/`stopdec` — Влад): НЕ смешение кода
  и данных, как я сначала ошибочно решил — `start`/`stop` кодирует
  УНИВЕРСАЛЬНУЮ структурную роль «граница диапазона» (глубина, даты,
  цена, температура — любая пара), а не предметное «цементирование».
  Взрыв типов возникает лишь при `startint/startdate/plandec/...` —
  чистая пара `start/stop` как ОДИН паттерн, наоборот, экономит. Идея
  сильная, НЕ отклонена;
- **через модельный слой** (`model.groups`/`composite_field` — Чат):
  snapshot знает `{group: depth_range, type: range, members: {from→dec_depth_from,
  to→dec_depth_to}}`, renderer получает готовое, не гадает;
- какой из двух — **определяет ПЕРВЫЙ реальный потребитель** (валидация
  «from < to», поиск пересечений интервалов, сравнение план-факт), а не
  сегодняшнее воображение. Все эти потребители сами в «Позже» (отчёты,
  валидация) — значит по строгому тесту Влада («без сущности НЕ обойтись?»)
  слой групп сегодня НЕ проходит: CRUD Листа2 собирается целиком без
  понятия «группа».

**Решение на СЕГОДНЯ: группы выводятся в подчинённые таблицы**, не через
тип, не через слой. Это безопасно для будущего: когда придёт сущность-
группа, подчинённые таблицы либо остаются, либо сворачиваются обратно
в поля — осознанная миграция под реального потребителя, не переделка
угаданного. Форма сущности-группы определяется потребителем — записано
здесь, чтобы будущая сессия НЕ прочитала «startdec отклонён» (он не
отклонён — отложен) и не потеряла идею.

**Уточнено повторно 2026-07-14, тем же тестом, до кода.** Вторая попытка
построить общий слой групп/весов (на этот раз — Claude, `model_registry`
kind=`group` + отдельная таблица весов на ПОЛЕ) остановлена Владом до
кода тем же вопросом «без сущности не обойтись?» — и найдена точная
причина, почему вес НЕ может жить на поле в принципе, не только сейчас:
поле — одно, а контекстов чтения, где у него есть вес/видимость/сосед по
группе, — много (сама таблица, и потом любая выборка/отчёт, объединяющий
несколько таблиц). Вес на поле отвечает на вопрос «чей это вес» только
пока потребитель один; со вторым отчётом вопрос обязателен, а ответа в
такой схеме нет — значит проектировать вес как атрибут поля было бы
готовить сущность под потребителя, чья форма ещё не решена, второй раз
подряд то же самое нарушение §15.8.

**Явно разделены два уровня, оба с прямым (не опосредованным) доступом
к данным, но с разным смыслом.** Базовый уровень — структура и
`dep_`/`rel_` (обслуживает CRUD, чинится инструментом ремонта БД,
07-11): видно всё, порядок — физический порядок колонок; своих данных
скрывать не от кого. Уровень представления — когда данные становятся
полезными для конкретной задачи чтения, не только существующими; вес,
видимость, группировка — свойства ЭТОЙ задачи, живут в определении
самого представления (отчёта/выборки), не в поле и не в реестре модели.
Отсюда следствие для будущего слоя отчётов, когда до него дойдёт
очередь: вес/видимость/группировка — часть состава конкретного отчёта
(«эти поля, в этом порядке, с такой группировкой»), не общая метаданная
модели. Единственный легитимный запрос к базовому уровню на сегодня —
не про группы вообще, а частный: у широких таблиц (`well`, 14 полей)
физический порядок колонок в БД иногда неудачен для чтения; чинится
прямой перестановкой колонок тем же инструментом ремонта структуры,
когда он до этого дорастёт — маленькая отдельная задача, не связанная
с отчётами и не срочная.

**`calc_` — отдельное сестринское семейство, НЕ подтип словаря**
(зафиксировано 2026-07-08, до реализации; имя уточнено 2026-07-14 —
дрейф к `math_` в одной из формулировок ниже был опережением, не
решением, снят). Общая философия резолвинга — «перейти по ссылке →
разрешить на границе сборки снапшота → подставить готовое значение» —
одна и та же для словарных проекций (а1/а2) и для вычислений: обе
следуют §7 (проверка на границе один раз) и §8 (рантайм не
интроспектирует). НО общий механизм ≠ общая таблица-описатель.
`voc_x` резолвит ИСТОЧНИК значения (откуда взять готовую строку);
`calc_x` резолвит СПОСОБ вычисления (как получить число из других
полей) — разные по форме паспорта, разные хендлеры, разные слоты
в снапшоте (`model.dictionaries` vs будущий `model.formulas`).
Слияние в одну таблицу нарушило бы §1 («имя поля выбирает сущность»
однозначно, без заглядывания внутрь паспорта) и §15.8 (абстракция
ради формы, не ради нужды) — риск назван прямо: рано или поздно
перестаёшь понимать по имени поля, что перед тобой. Спайк `ent_calc`
(entities.php, под карантинным баннером — см. журнал 2026-07-07/08)
концептуально доказал механику; следующий шаг (архивный план, п.14:
паспорт формулы → конфигуратор → родительская цепочка → кросс-табличное)
— первый в очереди сейчас, независимо от `ent_prm`/`ent_mlt` (те —
мёртвые легаси-форматы, не очередь, см. журнал 07-14).

**Гипотеза 07-08 (общий резолвер-движок со словарными проекциями)
проверена реальным проектированием 2026-07-14, подтверждена наполовину.**
Извлечение токенов `{поле}` из строки — переиспользуется буквально,
`template_parse()` без изменений годится и для формулы: тот же
синтаксис ссылки, та же грамотность на два места. РАЗРЕШЕНИЕ токена —
не переиспользуется и не должно: словарный шаблон склеивает токены в
строку, формула обязана вычислить арифметику (операторы, приоритет,
скобки) — заставить `template_parse` считать значило бы чинить
абстракцию под чужую задачу (§15.8), та же ошибка, что уже поймана
сегодня на группах. Формуле — свой маленький безопасный вычислитель
(разбор в дерево операций по белому списку, не `eval()`, тот же
принцип «текст не исполняется как код», что и в §16 п.3).

**Устройство паспорта — ПОСТРОЕНО И ПРОВЕРЕНО ЖИВЬЁМ 2026-07-14.**
`model_formulas` — своя таблица, не колонка в `model_labels` (критерий
07-11: формула меняется другой рукой/ритмом, чем подпись):
`dep_model_registry` (строка реестра самого `calc_`-поля) + один
столбец `data_formula` с шаблоном `{поле} оператор {поле}` — токены и
есть список переменных, второго поля не понадобилось, так же как у
`data_label_template`. Область первого шага — только поля СВОЕЙ ЖЕ
таблицы, реализована структурно (`snapshot_build_formulas()`,
core.php: whitelist токенов = поля владеющей таблицы, не убеждением);
шаг с родительской цепочкой (архивный п.14, шаг 4) расширит whitelist,
код формулы переписывать не придётся. Колонка и строка `model_registry`
заведены ЧЕРЕЗ КОНФИГУРАТОР (тип «Вычисляемое (формула)» уже в общем
списке — `ent_calc` обычная зарегистрированная сущность, урок 07-11 в
действии: языком модели, не SQL в обход инструмента); только
`CREATE TABLE model_formulas` и сама строка формулы — переданы Владу
SQL-файлом (тот же приём, что `model_links` 07-12).

Первое реальное поле, не синтетика: `cementing_interval.calc_volume_deviation`
= `{dec_volume_plan} - {dec_volume_fact}`. Проверено на живой записи
(интервал ОТМ-3, план 12.00 − факт 12.50) — колонка «Откл. объёма, м³»
показала `-0.50`, сошлось. Спайк `ent_calc` (карантинный баннер
07-07/08) заменён продакшн-версией в том же коммите — формула больше
не зашита в PHP. Смоук (секция 1е) синтетикой покрывает `formula_parse`/
`formula_eval` (8 проверок, включая пустую переменную и деление на
ноль → `null`, не фатал); живой JOIN `model_formulas`/`model_registry`
и whitelist-отказ на чужом поле смоуком НЕ покрыты — тот же честный
разрыв «синтетика вместо живой фикстуры», что и у reparent (журнал
07-14), не заводили изолированную фикстуру ради одной проверки.

**Шаг 3 (экран конфигуратора для формул) — ВЫПОЛНЕНО И ПРОВЕРЕНО
ЖИВЬЁМ 2026-07-15.** Разрыв, названный в записи выше (строка в
`model_formulas` заводилась вручную SQL мимо инструмента), закрыт:
форма «Добавить поле» получила тип «Вычисляемое (формула)» — вход
формулы + кликабельная подсказка переменных, только полей ТЕКУЩЕЙ
таблицы (whitelist посчитан на сервере, не угадан в JS).
`configurator_parse_field()` проверяет синтаксис (`formula_parse()`,
переиспользован из core.php) и whitelist ДО записи — недостижимая
переменная не предлагается кликом и явно отклоняется при ручном вводе
(§14.2 архивного плана). Проверено живьём Владом: опечатка
`dec_dept_to` вместо `dec_depth_to` поймана честным отказом при
попытке создать поле, не тихим сохранением. `configurator.php`
изолированно смоуком не покрывается (не библиотека функций — top-level
DB-коннект и рендер страницы, require в smoke_test.php невозможен без
побочных эффектов) — проверено вместо этого извлечением тела функции
во временный файл (тот же приём, что вчера для `formula_parse`), 7
синтетических сценариев, все зелёные, включая контроль регрессии на
обычном поле `dec_`.

Дальше по архивному плану остаётся только шаг 4 — родительская цепочка
(`rel_`/`dep_`, переменные не только своей таблицы) — не
спроектировано, ждёт реального кросс-табличного кейса (сегодняшняя
формула — план/факт одной таблицы, потребности пока нет).

**Найдено попутно, стоит закрепить — `calc_` и статистика по отчёту
различаются не сложностью формулы, а тем, сколько строк она видит.**
Влад предложил разделение «`calc_` для простого — `math_` про запас для
сложного»; при проверке тем же тестом (§15.8/«без сущности не обойтись»)
оказалось, что линия разреза не в сложности вычисления. `calc_`
(сколь угодно длинная формула, хоть с родительской цепочкой шага 4)
остаётся сущностью ОДНОЙ строки — обычное поле, обычный `field_exec`.
Статистика/агрегаты по ВЫБОРКЕ («средний расход по кусту», «процент
отклонений за квартал») не принадлежит ни одной записи вообще — это
свойство отчёта, не поля, ни при каком имени и ни при какой сложности
формулы. Значит `math_` как «сложный калькулятор про запас» не строится
никогда как отдельная сущность-сестра `calc_` — то, что под этим именем
имелось в виду, живёт в ещё не спроектированном слое отчётов, когда до
него дойдёт очередь. Перенесено в «Открытые вопросы» ниже, чтобы не
воспроизвестись повторно.

Состав слоя подтверждён референсной задачей; ничего не добавлять,
пока база не собирает тампонаж-CRUD.

## Открытые вопросы (зафиксированы, не проектируются)

- **статистика/агрегаты по выборке — не сущность поля ни при каком
  имени** (найдено 07-14, при проверке предложения «`calc_` для
  простого, `math_` про запас для сложного»): различие `calc_` и
  отчётной статистики не в сложности формулы, а в том, сколько строк
  она видит — одна (поле, `field_exec`) или выборка (отчёт). «Средний
  расход по кусту», «процент отклонений за квартал» не принадлежат ни
  одной записи — решаются вместе со слоем отчётов, не отдельной
  сущностью-сестрой `calc_`;
- портирование в `snapshot_init`: раздельная диагностика причин null,
  автоснятие мёртвого bootstrap-lock (>60 с), ожидание параллельной
  сборки, автосоздание `state/` — при первом касании snapshot-кода;
- **автоснятие осиротевшего `schema.lock`** (метка времени + `source`;
  протух → сирота) — переведено из «желательного» в **обязательное до
  расширения периметра писателей** (конфигуратор-как-писатель уже сейчас,
  позже — несколько инженеров): с N писателей вероятность падения под
  локом растёт линейно. Сегодня не блокер (ручной `rm state/schema.lock`),
  но предусловие мультиписательской фазы; стыкуется с аварийной сборкой
  класса А — сирота-лок растворяет ложный лок, аварийный режим подхватывает
  здоровую-но-нефайловую структуру, порознь каждый оставляет дыру;
- вес/видимость/группировка полей (план/факт, двухуровневые шапки) —
  атрибуты состава конкретного отчёта/выборки, не поля и не labels
  (уточнено 07-14, см. блок про группы выше); решается вместе с
  механизмом отчётов, не раньше;
- где живут правила валидации уровня модели (интервал ≤ глубины скважины,
  дочерняя запись не живёт без родительской, реагент принадлежит материалу);
- формат длительности «ч-мм» — при реализации ent_dur (composite-механика,
  единственный кандидат, у которого нет родного HTML-виджета);
- **`timestamp`** (аудит-штамп created_at/updated_at) — уточнено
  2026-07-05: «ноль кода» было неточно. MySQL проставляет ЗНАЧЕНИЕ сам
  (`DEFAULT CURRENT_TIMESTAMP`/`ON UPDATE`), но ИМЯ колонки всё равно
  обязано пройти `field_parse()` — свободного прохода не существует ни
  для чего (тот же класс, что вчера уронил `active`). Решение: новый
  структурный префикс (`sys_`?) в `STRUCTURAL_PREFIXES`, не строить,
  пока нет конкретного поля. Важно: имя `ent_time` уже занято другим
  смыслом (время суток, 2026-07-05) — для аудит-штампа нужно другое имя.

## Тестовая БД

Старая база под новым ядром не запускается. Legacy — источник предметки
(тампонаж: main, словари, зависимые таблицы), не архитектурного контракта.
Новая тестовая БД строится по правилам:

- `id` вместо `index`;
- никаких двоеточий в именах колонок;
- только поля с существующими сущностями;
- простые voc-таблицы как обычные таблицы;
- mch в будущем через junction table;
- model_registry + model_labels как служебные таблицы модели (subscr упразднена);
- root table — параметр модели, не hardcode `main`;
- данные из ТЗ-2 при переносе нормализуются (Excel-сериалы дат → date).

Подготовку дампа можно поручить ChatGPT на основе tt.sql и ТЗ-2.

**Эталонный дамп `gpdp_test.sql` версионируется парой со смоуком** — смоук
проверяет и данные (константы: «Самотлор» id=1, «Приобское» id=2, четыре
значения voc_mr, подписи labels «Месторождение»/«МР», «Номер куста»);
разошедшийся дамп валит секции 1/6 на чистом коде (поймано живьём).
Новый localhost = залить дамп + прогнать смоук.

## Требуемые правки существующих файлов

Выполнены: `snapshot_ensure`-дубль удалён из core.php (единственная дверь —
`snapshot_init`); index.php переведён на `config()` и канон `$db_connection`;
503-ветка индекса показывает причину (lock / `snapshot_last_error()`).
Открытых правок нет.

**Внимание:** в инструкциях проекта Claude ссылка «контрольные вопросы
§12» устарела — после вставки разделов о пространстве имён и тонкой
трубе они стали **§15** (анти-паттерны — §12). Поправить одну цифру.
Также заменить в файлах проекта устаревшие schema_runtime_draft.php и
старый index.php на актуальные core.php / index.php / helpers.php /
render.php — драфт дважды дезинформировал разработку.

## Журнал ключевых решений

| 2026-07-18 | **Системное меню — механика сборки/представления (Влад: три режима уже есть — главная/подписи/конфигуратор, — нужно меню, живущее отдельно от страниц, не заново собираемое каждой).** Решение: `REQUEST_CONTEXTS` (core.php, до этого — простой список для whitelist) стал ЕДИНЫМ источником и для проверки контекста, и для меню — каждый ключ несёт `icon`/`label`/`href`. Влад подтвердил направление явно: «брать пункты меню из контекста — на то он и контекст». `render_context_menu()` (render.php) строит `<nav class="sys-menu">` по этому списку, текущий пункт — `<span class="menu-current">`, не ссылка. `render_admin_page_open()` сменил сигнатуру: вместо свободной строки `$nav_html` (каждый вызывающий собирал сам — отсюда была вчерашняя асимметрия, конфигуратор ссылался на подписи, подписи не всегда на конфигуратор) берёт ключ контекста и строит меню сама — структурно исключает повторение той же ошибки. Все пять точек вызова (`index.php` ×3, `configurator.php`, `labels.php`) обновлены. Постраничные хлебные крошки («← К списку таблиц» и подобное) сознательно оставлены ОТДЕЛЬНО от системного меню — разная природа (переключение контура vs навигация внутри одного), не смешивать.

**Явно разведено с будущим меню уровня представления** (отчёты/представления, ещё не начато): то меню будет расти из записей модели (как `snapshot_build_dictionaries` — сборка по факту реестра, не хардкод), не из `REQUEST_CONTEXTS` — контур запроса и пункт презентационного меню разной природы, общий источник был бы неверным упрощением. Паспортизация (AI-документы) сознательно не проводилась перед этой работой — не нужна была: сессия велась с живым контекстом всего, что построено сегодня, паспорта нужны ДРУГОЙ сессии без этого контекста (Chat), не этой.

**Побочный эффект, не только рефакторинг**: страницы отдельной записи (`index.php?_table=...&_action=view`) раньше не имели системного меню вовсе, только ссылку «Домой» — теперь получили полное меню автоматически, вместе со всеми остальными местами. Оформление — намеренно просто (строка ссылок, `.sys-menu`, палитра уже установленная Chat — `#dce7f2` для текущего пункта, тот же акцент, что у строки ступени), с явным заделом на будущий выпадающий список: смена вида потребует правки только CSS, не PHP/разметки вызова.

**Живая проверка**: все три контура (`data`/`configurator`/`labels`) — меню идентично, текущий пункт верно подсвечен, не кликабелен; хлебные крошки не смешаны с меню; карточка отдельной записи (well) подтверждена получившей меню впервые. Смоук 73/73.

| 2026-07-17 | **Внешний вид карточки объекта, второй заход — план Chat пп.1-3 закрыты, выравнивание таблиц по ширине сделано, единый правый край — упёрлось в структуру вложенности (отложено к пп.4-7).** Сделано и подтверждено живьём: глубина капнута на одном шаге отступа (`min(depth,1)*24`, не нарастающий); узкие записи (буфер/реагент/репер/компонент, ≤4 колонки данных, `RENDER_COMPACT_MAX_COLS`) — компактной строкой `render_record_compact` вместо таблицы-на-три-значения; действия — hover-меню на чистом CSS (`.act-menu`); убраны вертикальные линии обёрток (сбивали заголовки); таблицы сжимаются по содержимому; кэш-бастинг `style.css?v=filemtime` (после серии правок без видимого эффекта — оказалось, браузер держал `style.css` отдельно от HTML).

**Диагностика через браузер (Chrome MCP, javascript_tool — прямые замеры `getBoundingClientRect`, не по скриншотам), сэкономила несколько слепых итераций:** «ширина не сжимается» оказалась не багом деплоя и не кэшем (шапку удалось перекрасить — CSS доезжал), а сочетанием реального широкого монитора Влада (`window.innerWidth=2520px`) и `width:100%`/`auto` у таблиц, которые на таком экране раздувают ячейки, распределяя всю ширину поровну между колонками. Решение (подсказка Chat + замеры): `width:max-content` на всех таблицах (единый режим, двухрежимность fit/wide и `:has()` убраны как лишняя сложность) — таблица ровно по содержимому, `max-width:100%` как потолок с прокруткой.

**Не доведено — единый правый край всех таблиц.** Замер показал: правые края таблиц по-прежнему разные (1882/1040/934/1412…), хотя оптически на скриншоте кажутся выровненными. Причина не в CSS-правиле, а в том, что `render_object_tree` вкладывает `div`-обёртки друг в друга с нарастающим сужением по глубине — `margin-left:auto` прижимает таблицу вправо внутри НЕПОСРЕДСТВЕННОГО родителя, но родитель сам уже сужен предыдущим уровнем вложенности, до края `body` (2458) ни одна не доходит. Это та же структурная проблема, которую адресуют пп.4-7 плана Chat (семантические роли вместо кодирования глубины через `margin-left`) — не косметика поверх, а перестройка вложенности рендера дерева. Отложено туда сознательно, чтобы не ломать уже работающее (сжатие ширины, компактные строки, hover-меню) очередным слепым тычком в стиль — за сессию их накопилось больше десятка. Влад: «фиксируй». |

| 2026-07-17 | **Внешний вид карточки объекта — первый заход закрыт и подтверждён живьём после ложной тревоги с деплоем.** По замечаниям Влада (скриншоты): действия каждой записи сведены в одну ячейку — hover-меню на чистом CSS (`.act-menu`/`.act-trigger`/`.act-menu-list`, `<select>` отвергнут — физически не умеет открываться по наведению, только по клику); колонка под меню — узкая (`col-actions`, `width:1%`); заголовок дочернего раздела + кнопка «+ добавить» — одна строка (`.block-head`, тот же приём, что уже был у `.schema-head` в конфигураторе). Отдельно — правка объединения сиблингов (`render_object_tree_block`, новая функция): развилка по наличию у сиблингов СВОИХ детей — лист (буфер, реагент, компонент репера) сводится в одну таблицу с несколькими строками; есть свои дети (интервал — у него испытания/буфер/АКЦ) — каждый остаётся своей карточкой, чтобы связь строки с её вложенным содержимым не терялась (первая версия объединяла всех подряд, что оторвало интервалы от их разделов — поймано и исправлено тем же днём).

**Ложная тревога с деплоем**: после первого прогона фикса объединения буферы/реагенты всё ещё показывались раздельными таблицами на скриншоте, хотя код (перепроверен построчно) логической ошибки не содержал. Заподозрили PHP opcache — проверка показала `opcache.validate_timestamps=On` (файл и так должен подхватываться без перезапуска), то есть гипотеза не подтвердилась буквально; `systemctl restart apache2` всё равно решил дело — вероятнее всего скриншот был снят до завершения `git pull`, не баг деплоя как такового. Итоговый скриншот — оба буфера и оба реагента второго интервала в одной таблице каждый, всё дерево читается чисто.

**Получен развёрнутый разбор от Chat** (полный дизайн-обзор, не в этой записи целиком — см. сообщение Влада с документом): общая оценка — «архитектурно направление верное, визуально между отладочным деревом и рабочим интерфейсом»; предложение — карточки вместо одинаковых вложенных таблиц на всех уровнях, семантические HTML-роли (`record`/`relation`/`header`/`body`) вместо кодирования глубины через `margin-left`, укрупнение ступени/интервала до самостоятельных карточек со сводкой в шапке, однострочные сущности (буфер/реагент/компонент) — не полноценная таблица, а компактная строка. Существенно больше, чем правка сегодняшнего дня — обсуждение порядка внедрения впереди, не начато. |

| 2026-07-17 | **Проверка использования перед удалением словарной позиции — построена и проверена на реальных данных ТЗ-2.** Ровно замысел Влада того же дня, без расширения объёма: `record_delete_check_usage()` (core.php) — для удаляемой строки таблицы, если она хоть где-то `source_table` в уже скомпилированном `model.dictionaries`, ищет использование по всем ссылающимся полям (`voc_`/`link_` — `field = id`; `links_` — `id = ANY(field)`, различаются по `entity` поля-владельца, не по имени поля). Для подавляющего большинства таблиц (не словарь ни для одного поля) — быстрый выход, ноль дополнительных запросов. Единственная точка встраивания — `record_delete()`, он один обслуживает удаление любой записи любой таблицы. Отказ идёт через уже существующий generic `errors[]`-путь `index.php` — интерфейс менять не пришлось вовсе, труба и так общая.

**Живая проверка на настоящих данных ТЗ-2** (не синтетика): удаление `voc_material` «ОТМ-3» (используется `cementing_interval#6`) — заблокировано с точным сообщением «используется в cementing_interval#6 (поле voc_material)»; создание одноразового неиспользуемого материала → удаление прошло чисто, без препятствий. Ветка `links_`/`ANY()` отдельно живьём не гонялась (в текущем дереве ТЗ-2 нет активного `links_`-поля) — синтаксис `$1 = ANY(колонка)` стандартный, той же формы, что уже проверялась при разработке `links_`; помечено как логически, не живьём подтверждённое — довести до полной проверки, когда появится реальный `links_`-кейс на данных (например, контрагент с ролями). |

| 2026-07-17 | **`configurator_create_view()` — гибридный словарь (Postgres VIEW) построен и проверен живьём целиком. Порядок «снапшот → links_ → вьюхи» исполнен полностью.** Узкий v1, ровно по замыслу: источник — существующая таблица с `id`+`data_name`, фильтр — ОДНА существующая `bul_`-колонка источника, `= 1`, закрытый выбор, не текстовый `WHERE` (§12). `configurator_validate_spec` — новая ветка `'view_filtered'`; DDL и регистрация — на кирпичах вчерашнего рефакторинга (`configurator_with_lock`/`configurator_register_element`, вьюха — третий потребитель, не шестая копия каркаса); `configurator_delete_table` — развилка `DROP TABLE`/`DROP VIEW` по `object_type` (заведён вчера в `snapshot_build_structure` именно под этот случай, пригодился на первом же реальном использовании). Форма — четвёртый вариант в конфигураторе, список `bul_`-полей источника подтягивается по JS при выборе источника (не общий список по всем таблицам сразу).

**Три реальных бага, ни один не всплыл на этапе написания — только на живой проверке:**
1. **PHP heredoc принял JS template literal `${...}` за свою интерполяцию** — фатал `Undefined constant` прямо на форме. `${f.value}` внутри `<<<HTML ... HTML;` — синтаксис PHP complex curly variable interpolation, не JS. Исправлено на конкатенацию строк. Урок: JS внутри PHP heredoc никогда не использовать template literals (обратные кавычки с `${}`), только конкатенацию или одинарные кавычки без интерполяции.
2. **`data_name`-поле вьюхи оставалось сиротой** — `configurator_create_view` регистрировал саму таблицу-вьюху в `model_registry`, но не её единственное поле (в отличие от `voc_simple`, который явно регистрирует своё синтетическое `data_name`-поле). Нашлось диагностикой сразу после первого создания («расхождений: 1»). Почищено тем же приёмом, что уже есть в `voc_simple`.
3. **Тестовые данные не сохранились из-за моей же ошибки методологии, не бага продукта** — `document.querySelector('input[name=bul_bur]')` находил ПЕРВЫЙ элемент с этим именем в DOM, а это скрытый sentinel-input (value=0) ПЕРЕД самим чекбоксом (тот же приём для `bul_`/`links_`, что уже применяется — «скрытое поле раньше в разметке, реальный контрол после»). Ставил `.checked=true` на скрытое поле, реальный чекбокс не трогал вовсе — представление честно возвращало пустой список, потому что данных для фильтра действительно не было. Диагностировано по прямому наблюдению Влада («ты не отметил ни кого ни в одну категорию» — со скриншотом реальных данных), не самостоятельно. Урок: при автоматизации через `bul_`/будущие `links_`-поля всегда `input[type=checkbox][name=...]`, не голый `input[name=...]` — на любом поле со скрытым sentinel-паттерном первый матч в DOM обманчив.

**Живая проверка**, после исправления всех трёх: таблица `contragent` (`data_name`+`bul_bur`+`bul_zak`, ровно форма легаси `mlt_contragent`) → три тестовых контрагента с разными сочетаниями ролей → `voc_contragent_bur` (VIEW, фильтр `bul_bur=1`) → `voc_`-поле, адресующее его через уже существующий `voc_pick` конфигуратора, наравне с обычными словарями → выпадающий список показал РОВНО двух контрагентов с `bul_bur=Да`, третьего (`bul_bur=Нет`) корректно исключил → запись → чтение (подпись верна) → `DROP VIEW` без ошибок (подтверждает развилку `object_type`). Все тестовые таблицы снесены после проверки, диагностика чистая.

**Побочный результат**: сама идея решённой темы вчерашнего дня подтвердилась практикой — легаси `mlt_contragent` (три булевых флага на одной таблице контрагентов) воспроизведён на Postgres буквально, только через `VIEW` вместо ручного `WHERE` в каждом запросе приложения. |

| 2026-07-17 | **`links_` — многозначная ссылка на словарь (0..N) — построена и проверена живьём целиком.** Новая сущность `entities.php::ent_links`/`links_handler`, физическое хранение — Postgres `integer[]` (не скрытый спутник — решение Влада: «меню Postgres уже в руках»); целостность НЕ на стороне БД (Postgres не умеет `REFERENCES` на элемент массива — проверено 07-16, патч 2012 года так и не вошёл в ядро) — проверяется в `validate` тем же способом, что уже принят для `dep_`/`rel_main` (без FK-constraint, не новый прецедент). Имя поля — во множественном числе (`links_favorite_colors`, не `links_favorite_color`) — подпись сама себя объясняет без открытия формы. Адресация цели переиспользует ровно тот же механизм, что `link_` (`configurator_register_link`/`model_links`) — не новая таблица адресов.

**Разложено по слоям, где именно живёт какая часть:**
- `pg_*` всегда отдаёт array-колонки ТЕКСТОМ в литеральном синтаксисе (`'{1,3,5}'`), не PHP-массивом — `links_array_parse`/`links_array_build` (entities.php) делают перевод туда-обратно.
- Раскладка выбранных значений «в столбик, не через запятую» (требование Влада — иначе таблица растягивается вширь) — решается СТРОГО в `render.php` (§3: единственное место, где рождается HTML), не в разборе данных: `record_view_row()` (core.php) перестал форсить `(string)`-каст ячейки — список проходит как есть; `render_record_table`/`render_value` раскладывают список через `<br>`, если значение — массив. Правка общая для ВСЕХ типов полей, не только `links_` — задел на будущее.
- Форма — `<select multiple>` с тем же скрытым сентинелом ПЕРЕД полем, что уже есть у `bul_`-чекбокса (при снятии всех отметок браузер не пришлёт ключ вовсе — без сентинела это читалось бы как «поле не трогали», не как «выбор явно очищен»).
- Легаси `mch()` пересматривать не пришлось вовсе (Влад: «тебе и в легаси не надо будет смотреть») — виджет мультиселекта там уже был правильный по форме, порочным было только CSV-хранение; здесь взят тот же виджет, другое хранение.

**Три реальных бага найдены и закрыты живьём, ни один не всплыл на этапе написания кода** — паттерн: логика, завязанная на литеральную строку `'link'`, добавлялась в четырёх РАЗНЫХ местах кода по мере роста `link_`-механизма, и каждое такое место нужно было находить и расширять на `'links'` отдельно, вручную, не автоматически:
1. `configurator.php::configurator_parse_field` — проверка `$entity_choice === 'link'` (обязательность выбора цели) не пускала `links_` дальше валидации без цели.
2. `configurator.php`, JS дважды (форма создания таблицы + форма добавления поля) — `select.value === 'link'` управляет видимостью поля выбора цели; без правки поле цели просто не показывалось бы в интерфейсе для `links_`.
3. `core.php::snapshot_build_dictionaries`, Проход 1б — `$field['entity'] !== 'link'` в резолвере словарей; без правки — fatal error `TypeError` при первой же попытке открыть форму с `links_`-полем (`lookup_labels()` получал `null` вместо словарной записи).

Урок: `entities()` — открытый список (12 → 13 типов без правки самого списка), но НЕ КАЖДОЕ место кода, которое различает поведение по `db.kind` (здесь — `column_with_table`), читает эту классификацию универсально — часть логики писалась под единственного на тот момент представителя вида (`link_`) и завязывалась на его буквальное имя, не на его категорию. При следующем добавлении сущности того же `db.kind` — сверяться по `grep "'link'"` (или по имени любого предыдущего единственного представителя своего вида) явно, не полагаться на то, что `db.kind`-классификация используется всюду последовательно.

**Живая проверка** (после доустановки — кэш снапшота не пересобирается сам по себе на правку кода, нужен `php tools_rebuild_snapshot.php` вручную после структурных фиксов резолвера, это тоже честно всплыло по ходу): тестовая `links_test_person` с полем `links_favorite_colors` → `voc_color` (4 значения) — создание с двумя выбранными значениями → чтение (Красный/Синий, каждое своей строкой) → редактирование → снятие ВСЕХ отметок → сохранение → чтение (пусто, без падения и без мусора). Полный цикл пройден, таблица снесена после проверки.

Следующий шаг по согласованному порядку (снапшот ✓ → `links_` ✓ → вьюхи) — `configurator_create_view()` и первый живой кандидат: контрагент, роль через `links_`, взамен трёх булевых колонок легаси `mlt_contragent`. |

| 2026-07-17 | **Гибридные словари (Postgres VIEW), шаг 0 — закрыт и проверен живьём.** По итогам вчерашнего решения (порядок: снапшот → `links_` → вьюхи) первым делом починена интроспекция: `snapshot_build_structure()` (core.php) переведена с `pg_catalog.pg_tables` (видел только настоящие таблицы) на `information_schema.tables` с `table_type IN ('BASE TABLE', 'VIEW')` — представления теперь попадают в структуру наравне с таблицами. Добавлено поле `object_type` (`'table'`/`'view'`) в каждую запись структуры — не используется пока нигде, задел на будущее различие `DROP TABLE`/`DROP VIEW` при удалении. **Живая проверка**: создано тестовое `CREATE VIEW view_smoke_check AS SELECT id, data_name FROM voc_mr` вручную (разово, не часть модели) — диагностика конфигуратора увидела его как обычную неуправляемую таблицу («Таблица есть в базе, но система про неё не знает»), без единой ошибки и без специального кода — подтверждает вчерашний тезис: резолвер словарей уже был VIEW-агностичен, не хватало только видимости в структуре. Представление снесено после проверки.

Следующий шаг по согласованному порядку — `links_` (многозначная ссылка на обычный словарь, надстройка адресации `link_`, форма хранения — массив вместо одиночного значения, при показе — несколько строк по выбранным вариантам; Влад: «легаси смотреть не надо», случай новый, не архивный). После него — сама `configurator_create_view()` и первый живой кандидат (контрагент, роль через `links_`, взамен трёх булевых колонок легаси `mlt_contragent`). |

| 2026-07-16 | **Рефакторинг `configurator.php` выполнен и проверен живьём — три находки из предыдущей записи закрыты.** `configurator_with_lock()` (каркас лок/try/finally), `configurator_register_element()` (INSERT в `model_registry`+`model_labels`), `configurator_is_managed()` (проверка «уже под управлением») — восемь функций переведены (`create_table`, `delete_table`, `adopt_field`, `adopt_table`, `drop_registry_row`, `drop_column`, `add_field`, `drop_field`). Текст отказа при занятом локе унифицирован на более частый вариант («Схема заблокирована другой операцией.» — было в 6 из 8 мест, 2 места говорили чуть иначе, смысл тот же). Поведение не менялось. **Живая проверка**: создание таблицы (`refactor_check`, поле `int_num`) → добавление поля (`bul_flag2`) → диагностика («Расхождений нет») → удаление таблицы — все три операции прошли чисто, дерево ТЗ-2 не задето. Прямой повод (не после `configurator_create_view()`, а до) выполнен — вьюха будет третьим потребителем чистых кирпичей, не шестой копией каркаса. |

| 2026-07-16 | **Проектирование гибридного словаря (Postgres VIEW + `voc_`-адресация) начато; аудит модульности `configurator.php` дал три конкретные, уже существующие находки дублирования, не гипотетические.** Повод: контрагенты (`mlt_contragent`, легаси, три роли на одной таблице) — реальный кандидат на переделку в параметризованный словарь (уже стоявший в «Позже» пункт «role=filter»), но не переделываем сейчас, только проектируем.

**Идея (Влад)**: привести контрагентов к Postgres `VIEW` — представление собирает нужные поля источника (составную подпись, доп. параметры) и фильтр по параметру; `mlt()`-подобная сущность делает выборку НЕ из таблицы, а из этого представления. Гибрид: снаружи выглядит как обычный словарь (`id`+`data_name`), внутри — `SELECT` с условием, не хранимые данные. Рабочее имя намеренно не выбрано — легаси-имена `mch_`/`mlt_` в тот же день отправлены в архив ровно за то, что тянут за собой чужой смысл; тот же урок применён здесь заранее, не только задним числом.

**Проверка по факту кода (не по ощущению) — модульность разная на чтение и на запись:**
- **Чтение полностью модульно.** `snapshot_build_dictionaries()` резолвит `voc_x` чисто по структуре (есть `id`+`data_name`?) — ему всё равно, таблица это или представление. `lookup_options()`/`lookup_labels()` — обычный `SELECT ... FROM $source`, синтаксически неотличим. `voc_handler` — тем более. Единственная необходимая правка — `snapshot_build_structure()` сейчас берёт `pg_catalog.pg_tables`, которая VIEW не показывает вообще (`pg_views` — отдельный каталог); нужно переключить на `information_schema.tables` с `table_type IN ('BASE TABLE','VIEW')`, оба типа класть в структуру одинаково. Маленькая правка, не архитектура.
- **Запись НЕ модульна — придётся дублировать, если не рефакторить заранее.** `configurator_create_table()` целиком: сборка DDL + запись в `model_registry`/`model_labels` + пересборка снапшота лежат вперемешку в одном теле функции, не разнесены. Будущая `configurator_create_view()` иначе как копированием ~25 строк регистрации+пересборки не построится.

**Аудит `configurator.php` целиком (по прямому запросу Влада «на сколько соблюдена модульность») дал три конкретные находки дублирования — не про вьюхи специально, факт кода уже сегодня:**
1. **Каркас `lock → try → refresh → finally unlock`** дословно повторён минимум в пяти функциях (`configurator_adopt_field`, `configurator_adopt_table`, `configurator_drop_registry_row`, `configurator_drop_column`, частично другие) — меняется только причина лока и тело `try`. Предложенный вынос: `configurator_with_lock(PgSql\Connection $db, string $reason, callable $body): array` — функция высшего порядка, каждый вызывающий сжимается до одного вызова с замыканием.
2. **Пара «INSERT в `model_registry` (RETURNING id) + INSERT в `model_labels`»** повторена минимум четыре раза дословно (`create_table` — дважды: таблица и поле; `adopt_field`; `adopt_table`). Кандидат на вынос: `configurator_register_element($db, $kind, $owner, $element, $short, $full): int`.
3. **Проверка «элемент уже под управлением?»** (`SELECT id FROM model_registry WHERE data_kind=... AND data_owner=... AND data_element=... AND active=1`) повторена дословно дважды (`adopt_field`, `drop_column`).

Ни одна находка не меняет поведение — чистое снятие дублирования. Не выполнено (решение Влада «заноси, обсуждать будем дальше» — зафиксировать находку, не бросаться рефакторить в этот же присест). Прямой практический повод рефакторить ДО, не после `configurator_create_view()` — иначе вьюха станет шестой копией каркаса №1, не третьей. |

| 2026-07-16 | **Пространство имён для многозначных связей закрыто: `links_`/`m2m_`/`pool_`, легаси `mch_`/`mlt_`-названия — только с развёрнутым пояснением, если понадобятся. Легаси `voc_utf8.php` сдан в архив — финальная сверка сделана, вычерпан целиком.**

Итоговая номенклатура (заменяет черновой `mch_` из более раннего в этой же сессии разговора с Владом про многозначные связи — тот разговор в журнал ещё не попадал, фиксируется впервые здесь):

- **`links_`** — переименованный `link_` во множественном числе: односторонний атрибут владельца, БЕЗ нагрузки, 0..N. Переиспользует ту же адресацию, что и `link_` (не только концептуально — тот же кусок кода, тот же принцип §15 «переиспользует ли контракт без нового кода», которым в своё время проверялся и сам `link_`). Физическое хранение — скрытый спутник ИЛИ нативный Postgres `array`, выбор реализации отложен до реального кандидата.
- **`m2m_`** — двусторонняя связь, БЕЗ нагрузки, «просто пары индексов» — видимая таблица-связка (не прячем, в отличие от `links_`: обе стороны владеют одинаково, прятать за одной из них было бы произволом).
- **`pool_`** — не тип поля сущности, а физический примитив хранения (Postgres `array`); самостоятельно вряд ли будет использоваться, но доступен как один из бэкендов внутри `links_`/`m2m_`.
- Таблица с нагрузкой (`test_reagent`, `cementing_reper_component`) — по-прежнему без нового префикса, обычная `dep_`-таблица с дополнительными полями; третья, отдельная категория от вышеперечисленных двух.

**Важное архитектурное уточнение (Влад, 07-16), меняющее понимание `link_`/`links_`**: скалярность `link_` — НЕ свойство природы словаря («словарь по природе однозначен»), а свойство самой ССЫЛКИ (обычная скалярная колонка физически держит одно значение, точка). Словарь-цель ничего не знает и не обязан знать, сколько раз на него ссылаются. Следствие: в Postgres (где есть `array`-хранение для самой ссылки) нет никакого запрета или особого механизма, требуемого от цели — `links_` может адресовать ЛЮБОЙ обычный `voc_`-словарь, тем же способом, что и `link_`, без специального «мульти-словаря» на стороне цели. Снимает последнюю неясность в переиспользовании адресации.

**Легаси-сверка `voc_utf8.php` (единственный файл с реальной логикой; `rel_utf8.php`/`index_utf8.php` — диагностические дампы `SHOW TABLES`, без предметной логики) — проведена целиком, повторов не найдено:**
- `voc()` = наш `voc_`, закрыт.
- `mlt()` = «мультисловарь» (уточнение Влада) — не просто «одиночный выбор из подмножества», а параметризованный словарь: `mlt_contragent` фильтрацией по трём булевым флагам строила три разных словаря из одной таблицы. Подтверждает уже стоявший в «Позже» пункт «параметризованные словари (role=filter)» реальным легаси-прецедентом — деталь вставки (как проставлялся флаг) восстановить не удалось, Влад сам не помнит; открытый вопрос на момент реализации, зафиксирован в самой карточке «Позже».
- `mch()` = ровно кейс, ушедший в `links_`/`m2m_`/`pool_` — CSV-хранение отвергнуто, но сам виджет формы (нативный HTML `<select multiple>`, не текстовое поле) стоит унести как образец UI для будущей реализации.
- `enn()` = частный случай `voc_`, отдельной сущности не будет — решение висело с 07-04, сверкой не поколеблено.
- `reagent` (легаси, `dep_material`+`rel_main`+`voc_reagent`+`data_amount`, была пустой в дампе) — не сущность, обычная `dep_`-таблица с нагрузкой; предвосхищает `test_reagent`, построенный ранее сегодня (Лист3 ТЗ-2) — задним числом подтверждает, что сегодняшнее решение легло на уже существовавшую (хоть и не реализованную) легаси-идею, не изобретено с нуля.

Побочная находка при сверке, не про эту тему: у `mlt_contragent` было ТРИ роли (недропользователь/заказчик/буровое предприятие), в текущем `well` — только ДВЕ словаря (`voc_enterprise`/`voc_subsoiluser`), роль «Заказчик» никуда не перенесена. Открытый вопрос Листа1, не решён: заказчик всегда совпадает с недропользователем на практике (тогда поле не нужно) или это реальный, забытый пробел модели. |

| 2026-07-16 | **Найден повод перехода на Postgres, не зафиксированный записью выше того же дня — из параллельного диалога с Chat, пересказанного Владом.** Разговор шёл о том же вопросе, что здесь называли «мультичойсы»: несколько любимых/нелюбимых цветов у `example_person`. Ход рассуждения (Chat, проверено против легаси `voc_utf8.php`): CSV-в-ячейке (легаси `mch()`, `explode(',')`) и JSON-в-ячейке — одна и та же болезнь в разных костюмах, прямое нарушение §12/§7 (узаконивание плохого legacy-формата) — ломает FK-целостность, поиск (подстрока "5" находит "15"/"50"), типизацию, весь конвейер `lookup_id_by_label` (он 1:1, не 1:N). Единственная таблица `person_color(person_id, color_id, role)` — тоже отклонена, но по ДРУГОЙ причине: колонка `role` со значением "любимый"/"нелюбимый" прячет смысл в ДАННЫХ, которые рантайм обязан прочитать — прямое нарушение «имя определяет поведение» (§1), того же калибра нарушение, что колонка `role` в устройстве, не эстетика. **Решение**: две ОТДЕЛЬНЫЕ зависимые таблицы (`person_favorite_color`, `person_unloved_color`), роль — в имени таблицы, не в строке данных; каждая — «скрытый спутник» нового типа поля `mch_` (префикс уже зарезервирован в легаси-таблице префиксов, просто не занят): не generic-CRUD, не видна в модели, которую держит в голове проектировщик, создаётся/сносится паспортом `mch_` целиком. **Важная поправка от Влада (из его же переписки с ChatGPT, Chat её не учёл в своём ответе)**: спутник `mch_`, В ОТЛИЧИЕ ОТ снапшота, НЕЛЬЗЯ исключать из бэкапа и нельзя трактовать как «восстановимый удалением» производный артефакт — он несёт настоящие пользовательские данные (выбор конкретного человека), снапшот — нет. Категория «инфраструктура поля, не предметная модель» — верна для видимости/CRUD, неверна для бэкапа. Держать это явно при реализации `mch_`, иначе первая же чистка «служебных» файлов при рефакторинге снапшота случайно снесёт реальные данные.

Отдельно, тем же разговором: **`mch_` (многозначный атрибут БЕЗ нагрузки) и `ent_mlt` («Позже», настоящая m2m С нагрузкой) — разные типы, не один и тот же отложенный пункт**, как эта запись сама неточно смешивала вчера (см. ответ Влада «сколько не закрыто по типам»). Различитель — несёт ли множественная связь собственный параметр применения (процент, дата, роль-с-содержанием): нет → `mch_`, спутник; да → видимая `dep_`-сущность с нагрузкой (`test_reagent`/`cementing_reper_component` — уже построенные вчера примеры ИМЕННО этой третьей категории, не `mch_` и не `ent_mlt`). Выбор физического хранения `mch_` (скрытый спутник vs нативный Postgres `integer[]` + GIN-индекс) сознательно отложен тем разговором «до постгресового меню в руках» — меню появилось только сегодня, переход завершён; это разблокированный, но ещё не принятый следующий шаг.

**Реальный повод перехода на Postgres, тем же разговором**: не один довод, а совпадение двух независимых тяг плюс отказ от половинчатости. (1) Выбор хранения `mch_` зависит от СУБД — Postgres даёt `integer[]`+GIN как законную альтернативу спутнику, MariaDB такой карты не даёт вовсе. (2) Держать две целевые базы одновременно — половинчатость, прямо противоречащая уже принятому 07-13 решению «никакого абстрактного конструктора диалектов»: раз диалект-нейтральность уже отвергнута, тянуть второй бэкенд без абстракции над ним — работа на два фронта без выгоды ни от одной стратегии. Названные Chat дополнительные технические доводы (транзакционный DDL — неудачный `CREATE`/`ALTER` не оставляет огрызка таблицы, в отличие от MariaDB; `information_schema` вместо `SHOW COLUMNS`; строгая типизация с `DOMAIN`/`CHECK` вместо тихого проглатывания `''` в типизированную колонку) — сопутствующие, не решающие сами по себе. Честная методологическая проверка, применённая тем же разговором и стоящая записи: «если бы не было ни разговора о цветах, ни усталости от двух баз — потребность отчётного слоя (условие отсрочки 07-13) уже стучалась бы?» — вопрос был задан, не переспрошен явно в показанном фрагменте; решение принято тем не менее («будем делать. И нужны оба решения»), стратегически, тем же способом, что записано выше без повода. Порядок исполнения (тот же разговор): сначала закрыть `db.php`-воронку целиком (07-14, уже было сделано ДО этого разговора), потом открывать фронт Postgres — ровно то, что и произошло по факту (закрыто 07-14 → переход начат 07-16), не постфактум совпадение, а исполнение спланированного порядка |

| 2026-07-16 | **Решение 07-13 «Postgres не сейчас» отменено — переход начат, полный катор, не dual-driver.** Прямая директива Влада, вне обсуждения повода в этом разговоре (не зафиксирован конкретный внешний триггер — решение стратегическое, не привязано к появлению слоя отчётов, которым 07-13 обусловил отсрочку). Уточнено дважды по прямым вопросам: (1) MySQL убирается из кода целиком, не остаётся выбираемым драйвером; (2) тестовая БД собирается заново через конфигуратор на Postgres, перенос боевых данных/`gpdp_test.sql` — не в объём первого шага. Порядок работ — по прямой директиве Влада: сначала связь/подключение (простое), затем сущности и типы, затем мультичойсы/соединительные таблицы (`mlt_`/m2m) — не забегать вперёд. Объём переоценён относительно обследования 07-13: тогда ~150 мест `mysqli_*` по проекту без посредника; сегодня почти все стянуты в `db_select`/`db_execute` (слой абстракции закрыт 07-14, изначально — уборка техдолга, не подготовка к этой миграции, но именно она снизила объём catoра на порядок). Технические решения, зафиксированные до кода: `?`→`$N`-конвертация плейсхолдеров живёт ВНУТРИ `db_select`/`db_execute`, не в вызывающем коде (`record_*`/`snapshot_build_*`/`lookup_*` не меняются); обратные кавычки идентификаторов не заменяются на двойные, а убираются вовсе (имена и так нижний регистр, Postgres сам так их и трактует неквотированными). Полный план по шагам — «Сейчас» п.9. Старая запись в «Позже» помечена `[ПЕРЕСМОТРЕНО]`, не удалена и не переписана — дисциплина HANDOFF.md §3 (не релитигировать молча) |

| 2026-07-15 | **Дерево (`dep_`) в `tools_bulk_import.php` проверено на живых данных — обе дорожки конвейера оптовой загрузки пройдены.** Утренняя проверка была только плоской (`example_person`, без вложенности); днём — настоящее дерево: структура `book`/`book_reading` (зависимая) + два словаря (`voc_author`, `voc_genre`) создана через конфигуратор (только доверенным ИИ, по терминологии дня — «предложение структуры» и «создание структуры» разные роли). Chat сгенерировал 8 книг с 1-3 вложенными прочтениями (15 записей), Sonnet проверил независимо (не поверил встроенным ассертам). `--dry-run` и реальный запуск — оба 23/23, ноль отказов, `id` родителя верно подставлен детям на каждом уровне. Попутно найден и исправлен настоящий, хоть и косметический баг: путь дочерней записи в отчёте ломался из-за классической PHP-ловушки (`$var[$i]` внутри двойных кавычек — доступ к символу строки по индексу, не буквальный текст) — данные это не задевало, чинена только читаемость отчёта |

| 2026-07-15 | **Оптовая загрузка данных — конвейер построен и проверен целиком, от идеи до реальных записей в базе.** Пять шагов за одну сессию: (1) `lookup_id_by_label` (`helpers.php`) — обратный поиск по `model.dictionaries`, зеркало `lookup_labels`, изолированно проверен на синтетике до касания БД; (2) `BULK_IMPORT_FORMAT.md` — формат данных для уже существующих таблиц; (3) `tools_bulk_import.php` — сам загрузчик, рекурсивный обход дерева `dep_`, `--dry-run` через настоящую транзакцию с `ROLLBACK` (не отдельный путь валидации — `record_save()` один в один); (4) `STRUCTURE_PROPOSAL_FORMAT.md` — пара для случая, когда таблиц ещё нет; явно разведено «предложение структуры» (любой ИИ) от «создание структуры» (только доверенный, с полным контекстом и доступом — конфигуратор). Первая полная проверка на реальных данных: Chat сгенерировал 40 записей `example_person` под формат (пары любимый/нелюбимый цвет из `voc_color`), Sonnet проверил независимо (не поверил чужим ассертам — свой прогон подтвердил: 40 уникальных имён, цвета валидны, любимый≠нелюбимый, ровное распределение по всем семи), `--dry-run` на сервере — 40/40, ноль отказов, реальный запуск — без замечаний. Первая настоящая AI-к-AI цепочка (задача → генерация → независимая проверка → реальная запись), не просто перенос кода по шаблону |

| 2026-07-15 | **`calc_`: шаг 3 (экран конфигуратора для формул) закрыт — проверено живьём.** Разрыв, названный в записи 07-14 («строка в model_formulas заводилась вручную SQL мимо инструмента»), закрыт тем же уроком 07-11: форма «Добавить поле» получила тип «Вычисляемое (формула)» — `configurator_parse_field()` принимает whitelist полей владеющей таблицы (`null` — контекста нет, создание новой таблицы, `calc_` там осмысленно недоступен), проверяет синтаксис (`formula_parse()`, переиспользован из core.php, не задублирован) и переменные ДО записи; недостижимое не предлагается кликом (подсказка на сервере), а не отклоняется постфактум. `configurator_register_formula()` — тонкая труба, пишет уже проверенное. **Проверено Владом на границе**: опечатка `dec_dept_to` вместо `dec_depth_to` поймана честным отказом при попытке создания поля, не тихим сохранением. `configurator.php` изолированно смоуком не покрывается в принципе (точка входа с top-level DB-коннектом и рендером страницы, не библиотека — `require` в smoke_test.php невозможен без побочных эффектов, разрыв не только сегодняшний) — вместо этого тело функции извлечено во временный файл (тот же приём, что вчера для `formula_parse`/`formula_eval`), 7 сценариев прогнаны изолированно, все зелёные, включая контроль регрессии на обычном поле `dec_`. Архивный план по `calc_` исчерпан кроме шага 4 (родительская цепочка `rel_`/`dep_`) — не спроектирован, ждёт реального кросс-табличного кейса, которого пока нет |

| 2026-07-14 | **`calc_`: паспорт формулы реализован и проверен живьём — первое рабочее поле, не синтетика.** Продолжение предыдущей записи (архитектурное согласование) — тот же день, доведено до кода и до реальных данных. Построено: `formula_parse()`/`snapshot_build_formulas()` (core.php, whitelist переменных = поля владеющей таблицы, fail-fast на синтаксис/чужое поле), `model_formulas` подключена во все три сборочных пути (`snapshot_build`, `snapshot_refresh_model`, `snapshot_validate`) тем же способом, что `dictionaries`/`relations`. Карантинный спайк `ent_calc` (07-07/08) заменён продакшн-версией `calc_handler`/`formula_eval` в том же коммите (`5b1a737`) — формула больше не зашита в PHP. Колонка и строка `model_registry` для `cementing_interval.calc_volume_deviation` заведены ЧЕРЕЗ КОНФИГУРАТОР (тип «Вычисляемое (формула)» уже в общем списке типов — урок 07-11, языком модели, не SQL в обход инструмента); Владу переданы SQL-файлом только `CREATE TABLE model_formulas` и сама строка формулы (тот же приём, что `model_links` 07-12). **Проверено на живой записи** (интервал ОТМ-3, план 12.00 − факт 12.50) — колонка показала `-0.50`, сошлось с ручным расчётом. Смоук (секция 1е, коммит `5b1a737`) покрывает `formula_parse`/`formula_eval` синтетикой (8 проверок, прогнаны отдельно вне смоука для контроля перед коммитом — все зелёные), включая честные крайние случаи (пустая переменная, деление на ноль → `null`, не фатал). Честно не покрыто смоуком, как и у reparent: живой JOIN `model_formulas`/`model_registry` и whitelist-отказ на чужом поле — синтетика вместо изолированной фикстуры, тот же принятый разрыв, что журнал 07-14 уже называл для reparent. Дальше по архивному плану (шаг 3 — экран конфигуратора для формул, шаг 4 — родительская цепочка `rel_`/`dep_`) — не спроектировано, ждёт очереди |

| 2026-07-14 | **`calc_`/формулы: план паспорта согласован, гипотеза 07-08 (общий движок со словарными проекциями) проверена и подтверждена наполовину; отклонено предложение отдельной сущности `math_` для отчётной статистики.** Разговор о продолжении очереди («Сейчас» закрыт целиком, см. запись ниже) довёл до `calc_`/`math_`, спайк `ent_calc` (07-07/08) — первый кандидат. Проверка реального кода (не архивного черновика 07-07 с гипотетической таблицей `role/source_table/source_field/sort_order/payload`) показала: словарный маяк составной подписи на деле устроен проще — один столбец `data_label_template` на `model_labels`, разбираемый `template_parse()`. Тест гипотезы: извлечение токенов `{поле}` — переиспользуется буквально; разрешение токена — нет (склейка строки vs арифметика с приоритетом операторов), формуле нужен свой безопасный вычислитель, не `eval()`. Согласован план: `model_formulas` (своя таблица, критерий 07-11 — другая рука/ритм, чем подпись) с `dep_model_registry` + один столбец `data_formula`, токены в строке — они и есть список переменных, второго поля не нужно; whitelist переменных на первом шаге — поля своей же таблицы, структурно (не убеждением), расширяемо под будущую родительскую цепочку без переписывания. Отдельно — Влад предложил `calc_` для простых инлайн-расчётов и `math_` «про запас» для сложных отчётов со статистикой; проверено тем же тестом §15.8/«без сущности не обойтись» — линия разреза оказалась не в сложности формулы, а в том, сколько строк она видит: `calc_` (любой сложности, включая будущую родительскую цепочку) остаётся сущностью ОДНОЙ строки, статистика по выборке не принадлежит ни одной записи вообще и не может быть сущностью поля ни при каком имени — это часть ещё не спроектированного слоя отчётов. Название `math_`, случайно проскользнувшее в одну из формулировок 07-08, снято как опережение; производственное имя — `calc_`. Реализация ещё не начата — это архитектурное согласование, до кода. STATE.md: блок `calc_`/`math_` в «Позже» переписан, новый пункт в «Открытых вопросах» |

| 2026-07-14 | **Группы/веса полей — вторая попытка построить общий модельный механизм остановлена ДО кода, тем же тестом, что и первая (07-08).** CSS вынесен в файл (`style.css`, коммит `da20150`) — попутно, отдельно, без спора. Дальше предложен `model_registry` kind=`group` + таблица весов на ПОЛЕ под грядущую двухуровневую шапку Листа2; Влад остановил письменным вопросом «а если поле в разных таблицах/отчётах с разным весом?» до единой строчки DDL. Найдена точная причина, не просто «рано»: вес — не свойство поля, у поля один вес отвечает на вопрос «чей это вес» только пока потребитель один; с появлением второго отчёта ответа в такой схеме нет в принципе. Явно разделены базовый уровень (структура, `dep_`/`rel_`, всегда видно всё, порядок — физический) и уровень представления (вес/видимость/группировка — атрибуты состава КОНКРЕТНОГО отчёта/выборки, не поля и не реестра модели). Решение: механизм не строится вообще, ни сейчас, ни как заготовка — переносится целиком в определение будущего слоя отчётов, когда до него дойдёт очередь. Единственный сохранившийся частный вопрос — не про группы: физический порядок колонок у широких таблиц (`well`, 14 полей) при случае поправить прямой перестановкой через инструмент ремонта структуры (07-11), маленькая отдельная задача, не срочная. STATE.md «Позже», блок про группы полей и открытые вопросы — обновлены |

| 2026-07-14 | **п.5 «Сейчас» закрыт: reparent как отдельное защищённое действие (только dep_).** Первое архитектурное решение сессии, до кода прогнанное через все десять вопросов §15 письменно (не общим ощущением). Устройство: `record_reparent()` пишет ровно одну колонку `dep_<parent>`, имя FK резолвится `record_parent_relation()` из собственных структурных полей таблицы (не из request, не эвристикой графа — прямой разбор имени, §1); больше одного `dep_` на таблице — fail-fast, не угадывание. UI — отдельное действие `reparent`/`save_reparent`, вне `action_mode` (не режим сущности). Смоук (секция 1д) покрывает резолв синтетикой и `record_reparent()` живьём на обоих fail-fast путях без записи в БД; happy path записи (реальный `UPDATE`) синтетикой не покрыт — своей child-фикстуры у изолированной `main` нет, заводить её ради одной проверки сочтено лишним — **вместо этого подтверждено вручную через UI на боевом дереве** (Влад, клик «⇄» на `cementing_stage`). Честная граница на будущее: автоматической регрессионной гарантии на сам факт записи сегодня нет, только на резолв и отказы; фикстура-child для `main` — открытая, не забытая задача, если такая гарантия понадобится. Побочно: собственная регрессия при правке смоука (заголовок секции 2 случайно стёрт при вставке 1д — `old_str` был сам заголовок, `new_str` его не вернул) — поймана и исправлена в том же заходе, коммит `584c294` |

| 2026-07-14 | **Слой абстракции БД завершён целиком — первая настоящая делегированная работа Chat по AI Context Protocol, проверенная и принятая.** Задача из `HANDOFF_2026-07-14T10-33.md` (`delegate_tasks`) передана Chat напрямую (Влад — посредник передачи текста, попытка автоматического ввода через браузер не удалась технически, не критично). Результат — два коммита (`afa0cab`, `2c15076`), проверены той же дисциплиной, что весь день: коммиты существуют на GitHub (не выдумка), `diff` построчно — текст SQL не тронут, поведение при ошибке явно откомментировано в обоих местах, не формально. Смоук зелёный целиком после — включая раздел 7, напрямую проверяющий переписанный код `helpers.php`. Работа образцовая с первого раза, без правок с моей стороны. Проект полностью свободен от прямых `mysqli_*`-вызовов, кроме сознательно оставленных (управление соединением, одна интроспекция схемы) |

| 2026-07-14 | **Механизм доверия AI-документам исправлен дважды за один разговор — оба раза по вопросу Влада, не по своей инициативе.** (1) Точка отсчёта свежести — не commit..HEAD по всему репозиторию (ложная тревога: проверено на живых данных, 11 «мешающих» коммитов между `ddcdebe` и `HEAD`, из них 0 трогали `*.php` — паспорта были полностью свежими, широкая проверка сказала бы «нет»), а по файлу-источнику каждого документа (`*.php` для паспортов/snapshot, `STATE.md` для индекса). (2) Точка отсчёта коммита — не самоотчётное поле `generated_at_commit` внутри документа (может быть забыто/сбито), а `git log -1 --format=%H -- <файл>` — реальная история самого файла, механически, без доверия чужой записи. Проверено на всех трёх документах — верный, разный результат (паспорта и snapshot свежие, индекс устарел, не содержит запись `cc2a193`). Отдельно рассмотрена и отклонена (для ЭТОЙ задачи, не вообще) идея файла-флага по образцу `SNAPSHOT_REBUILD_REQUIRED`: годится как триггер-уведомление для Chat (формат `HANDOFF`, §4 `AI_CONTEXT_PROTOCOL.md` — уже специфицирован, впервые заполнен по-настоящему тем же числом), но не как механизм доверия — там, где есть железный, не зависящий от памяти способ (git-история), опираться на дисциплину «не забыть проставить» было бы шагом назад |

| 2026-07-14 | **Закрытие сессии: слой абстракции БД завершён, AI Context Protocol запущен в работу.** (1) `db_select`/`db_execute` — весь `core.php` и весь `configurator.php` переведены (7 коммитов, `db.php`→`40e40a0`...`a1c8144`), смоук зелёный на каждом шаге, проверено вживую на сервере. Осталось: `labels.php`/`helpers.php`/`index.php` (меньшие файлы), `smoke_test.php` — отдельный вопрос. (2) `AI_CONTEXT_PROTOCOL.md` принят и запущен — Chat ведёт `FUNCTION_PASSPORTS_AI.md`/`ARCHITECTURE_SNAPSHOT_AI.md`/`STATE_INDEX_AI.md` в `main`. Первая проверка боем: паспорт `record_fetch` содержал неполный `called_by` (только `record_tree`, реально 4 места) — найдено сверкой с кодом, Chat починил методом (grep по index.php/configurator.php), проверено `diff` — только `called_by` менялось, остальные поля не тронуты, метод сработал на ~28 функциях, не только на контрольном примере (коммит `89010bb`). `FUNCTION_PASSPORTS_AI.md` сверен с кодом дважды сегодня — годен как рабочий документ первого поколения. `ARCHITECTURE_SNAPSHOT_AI.md`/`STATE_INDEX_AI.md` существуют в `main`, НЕ сверены с кодом — первая задача для проверки в будущем, не готовый факт |

| 2026-07-14 | **AI Context Protocol принят — техзадание для Chat: паспорта функций, snapshot, индекс.** Обоснование измерено, не теоретическое: скриншот usage (Влад) — одно архитектурное сообщение потребило 27% лимита сессии на только что сброшенном лимите. Полная спецификация — `AI_CONTEXT_PROTOCOL.md`. Ключевое решение: компактные документы — навигация («что читать»), не замена чтению кода перед архитектурным решением; механическая работа по готовому шаблону может опираться на карту без сверки, архитектурная — нет. Язык английский (кириллица токенизируется дороже латиницы — измеримый факт, не вкусовщина), символы только частые/простые (✓✗⚠→←), не изобретённые. Ключевая находка по ходу обсуждения: поле `contracts_unchanged` в HANDOFF-формате — самое ценное (разрешает не открывать файл вовсе) и самое опасное (утверждение об ОТСУТСТВИИ изменения труднее доказать честно, чем перечислить присутствие) одновременно — добавлена обязательная пометка способа проверки (`[checked: read/grep/none]`), не просто факт. `status` (✓/⚠/✗) в паспортах функций — привязка к commit SHA, дешёвая проверка свежести одной git-командой вместо доверия вслепую |

| 2026-07-14 | **Два уровня работы с БД разведены явно — не смешивать. Правило Chat про план выборки принято.** Полная карточка — раздел «Позже». Триггер: Влад забеспокоился, что обсуждение «шаблон vs обработка данных» для обёртки вызовов (нижний, узкий уровень) прозвучало сложнее, чем должно быть — подозрение на дрейф от простоты «атомарных шаблонов» легаси. Причина беспокойства найдена: Sonnet ответил на вопрос нижнего уровня (`db_select`/`db_execute`, механическая обёртка) с оговорками, относящимися на самом деле к верхнему уровню (план выборки) — уровни слились в одном ответе. Разведены явно. Верхний уровень подтверждён СИММЕТРИЧНЫМ record_save() (проверено кодом: поле таблицы = ключ входного массива = ключ нормализованного массива, один универсальный запрос) — для чтения нужен тот же принцип: план (смысл операции), не текст SQL. Проверено кодом, что `record_children()` уже ЕСТЬ типовая форма по типу связи (`dep_`), ровно как описал Chat — не выдумка. Реальная опасность (не сложность плана, не абстракция сама по себе) — первая функция вида `record_well_cementing_report()`, названная по смыслу экрана, не по типу операции; такой пока нет. Правило принято как есть: новая предметная выборка не получает собственный SQL, сначала определяется типовая форма, реализуется один раз. НЕ проектировать типовые формы rel_main/m2m/фильтров до реального потребителя |

| 2026-07-13 | **Решено: делаем слой абстракции над вызовами БД — приоритет следующей сессии, перед остальным.** Второй философский вопрос дня (после «два уровня истины», см. запись выше и §15). Триггер — обследование портируемости на Postgres (запись выше, коммит `4348ba9`): ~150 прямых вызовов `mysqli_*`, без посредника. Влад: «в любом случае это облегчит работу» — независимо от факта перехода на другую СУБД, обёртка ценна сама по себе (уборка технического долга, не только подготовка к миграции). РАЗГРАНИЧЕНИЕ МАСШТАБА (важно не потерять): делаем ТОЛЬКО простую обёртку вызова («выполни запрос, верни строки» — одна-две функции вместо повторяющихся 4 строк `prepare→bind→execute→fetch`), НЕ полноценный конструктор SQL под диалекты. Три места с настоящей разницей синтаксиса (`AUTO_INCREMENT`, `ENGINE=InnoDB`, `ON DUPLICATE KEY`) этой обёрткой не закрываются и не должны — они остаются точечными местами, которые решаются отдельно, когда (и если) реально переезжаем. НЕ переходим на Postgres сейчас — решение отложено до появления настоящего потребителя (слой отчётов/выборок), не «на всякий случай» (тот же принцип, которым вчера отложили таблицу весов для порядка полей). Сопутствующий принцип из диалога с Chat (docs/gpdp_philosophy_2026-07-13.md — второй файл в той же серии, про Postgres), стоит зафиксировать независимо от даты перехода: «если возможность СУБД меняет архитектуру GPDP — решение выбрано неправильно»; специфичные фичи (`JSONB`, `MATERIALIZED VIEW`, `GiST`/`GIN`) — свойства адаптера хранения/паспорта конкретной сущности, не модели GPDP. Проверка обещаний Chat по списку (Влад заподозрил завышенность, справедливо): `DOMAIN`/`RANGE`/индексы — действительно просто смена типа в паспорте, одна команда; `JSONB` — локальная доработка паспорта, не архитектура; `MATERIALIZED VIEW` — НЕ «одна команда», реальный инженерный вопрос («когда обновлять»), Chat подал ровнее, чем есть. Связь с `link_`: только Идея Б (ссылка на поле, с фильтром) выиграла бы от `JSONB` для доп-условий — не сегодняшняя Идея А, она уже готова без Postgres |

| 2026-07-13 | **Найдена и закрыта дыра: снапшот на диске не знает о правках кода резолвера сам по себе.** После деплоя вчерашней унификации словарей — живой `example_person`: `Undefined array key "plan"` в `helpers.php`. Причина: снапшот-файл (`state/`) — компилированный артефакт, обновляется только как побочный эффект структурных действий конфигуратора; правка КОДА (не БД) его не трогает. Разово починено `tools_rebuild_snapshot.php` (полная пересборка, не только presentation, в отличие от `diag_refresh.php`). Постоянное решение — предложил Влад (файл-флаг, git видит его и решает подтягивать/нет), я предложил ГДЕ проверять: не боевой веб-трафик (первый заход после деплоя платит целиком, риск гонки при параллельных запросах), а `git`-хук `post-merge`, версионируемый через `core.hooksPath` (не разовое ручное копирование в `.git/hooks`, а обычный файл в репозитории). Флаг `SNAPSHOT_REBUILD_REQUIRED` кладёт тот, кто коммитит системную правку (`core`/`helpers`/`entities`, меняющую резолвер); хук после `pull` видит флаг → `tools_rebuild_snapshot.php` → убирает флаг. Дисциплина записана в `HANDOFF.md` §8. Настройка на сервере — один раз: `git config core.hooksPath .githooks` |

| 2026-07-13 | **Три куска за сессию: `bul_`, обследование Postgres, унификация словарей.** (1) `bul_` — простое булево поле (стресс-тест Chat 07-12), попутно найден и исправлен реальный, ранее не сработавший баг: `render_input` рисовал чекбокс без скрытой страховки — непроверенный чекбокс браузер не отправляет, `record_save` трактует отсутствие ключа как «не трогали» (частичное обновление законно), «снять галочку» было бы неотличимо от «не сохранять», старое значение тихо оставалось бы висеть. Исправлено общим приёмом (hidden+checkbox), не только для `bul_`. (2) Обследование портируемости на Postgres по факту кода (не на глазок): `entities.php` — ноль обращений к БД, полностью независим; зависимость — ~150 мест в `configurator`/`core`/`labels`/`helpers`, три места с настоящей разницей SQL-синтаксиса (`AUTO_INCREMENT`, `ENGINE=InnoDB`, `ON DUPLICATE KEY`). Ограниченная, предсказуемая работа, не переписывание архитектуры. Абстракция над `mysqli` осознанно не введена — не была нужна. (3) Унификация словарей исполнена по готовому плану (решение 07-11, коммит `a20d956`): `data_name` — частный случай шаблона из одного куска, `lookup_options` удалена, один вход на все словари. Попутно унифицирована и самоподпись объекта (`record_label`/`@table`) — та же развилка (простой словарь отдельным путём) пряталась и там, план её не предвидел; без этого унификация вышла бы половинчатой. Заодно: найдена и исправлена ошибка в собственной же правке (`compile()` захватывал `$templates` по значению, не по ссылке — поздний синтез из self-label-цикла был бы не виден), удалён осиротевший докблок (§16.1, до link_/проекций, без функции-хозяина после вчерашних правок). Все три куска проверены изолированными прогонами до касания живой БД. Смоук переписан под новое поведение словарей — деплой + прогон смоука ещё не подтверждён на живом сервере, первое, что стоит сделать в новой сессии |

| 2026-07-12 | **`link_` (Идея А) — утверждена, план на реализацию, код не сегодня.** Задача-триггер: одна таблица, два поля («любимый цвет»/«нелюбимый цвет»), оба должны выбирать из ОДНОГО словаря — по имени поля это невозможно (два поля не могут одинаково называться), а два словаря-дубля недопустимы (расходится содержимое). Независимо совпало со стресс-тестом Chat (`docs/dating_site_stress_test_2026-07-12.md`, §6.1: `voc_birth_city`/`voc_current_city` оба → `city`). ПОДТВЕРЖДЕНО Владом явно: (1) приставка `link_` — как `voc_` говорит «словарь», `link_` говорит «ссылка с явным адресом, не по имени»; (2) реализуем только Идею А (ссылка на целую запись другой таблицы — работает как `voc_`-выпадающий список, любая таблица со своей подпись, не обязательно словарь-склад); Идея Б (ссылка на конкретное ПОЛЕ конкретной строки — кирпичик для будущих отчётов/параметризованных проекций, то же самое нашёл Chat как «второй критический элемент») — СОЗНАТЕЛЬНО отложена, смешивать с сегодняшней задачей означало бы две стройки под одним именем; (3) `link_` признан ценным заделом для будущего слоя отчётов — лучше, чем черновая идея Влада «поля-ссылки по именам» (сам назвал её «грубой и некрасивой»); (4) будущее уточнение, НЕ решать сейчас: возможно, выбор цели `link_`-поля стоит ограничить только теми таблицами, где прослеживается реальная связь (через `dep_`/`rel_main` граф), не разрешать ссылку куда угодно — записано как направление, не как решение. МОИ ПРЕДЛОЖЕНИЯ ПО УМОЛЧАНИЮ, НЕ ПЕРЕСПРОШЕНЫ ЯВНО (нужно подтвердить в следующей сессии, не считать решённым): (а) адрес цели `link_`-поля хранится в ОТДЕЛЬНОЙ маленькой таблице (той же формы, что `model_labels` — 1:1 с `model_registry` через FK), НЕ в поле `data_label_template` (Влад предложил переиспользовать шаблонизатор подписи — я возразил: разная причина изменения, §17-критерий 07-11, шаблон — рецепт склейки, адрес цели — факт; Влад не подтвердил и не опроверг явно, реплика ушла в другую сторону разговора); (б) адрес не переопределяется после создания поля (задаётся один раз в конфигураторе, без экрана правки); (в) цель `link_` может быть тем же, на что может ссылаться `voc_` (словарь-склад или адресный склад) — не более широкий список целей на первую версию. Конфигуратор при создании `link_`-поля должен явно давать выбрать существующую целевую таблицу (не создавать новую). Разрешается на той же ступени сборки снапшота, что и словари (§16.1-подобно, но адрес читается из записи, не вычисляется из имени) |

| 2026-07-12 | **Смоук зелёный целиком — все 10 разделов, впервые.** Путь за один день: потерянная фикстура main → хрупкость от совпадения имени с боевым voc_mr (изолирован в voc_smoke) → моя ошибка в подсчёте options (забыл про пустой вариант выбора) → находка с OPcache-инвалидацией (snapshot_save молча не долетал до чтения в том же запросе) → расхождение секции 9 с новой навигацией «Домой» (index.php больше не имеет неявного root_table по умолчанию, нужен явный _table). Заодно: папка сайта (/var/www/html/gpdp) стала настоящей git-копией (git init + fetch + checkout -b main --track origin/main) — ручная перекачка файлов через чат больше не нужна, `git pull` на сервере. Расширение Claude в Chrome впервые подключено и использовано вживую весь день: чтение вывода страниц напрямую, самостоятельная работа в конфигураторе (создание/удаление таблиц, диагностика) без пересказа Влада |

| 2026-07-12 | **Находка: `snapshot_save` не инвалидировал OPcache после `rename()` — тихая рассинхронизация refresh'а. Исправлено.** Вскрыто смоуком впервые: секция 8 (`refresh presentation без lock` → `snapshot_load()` в том же запросе) до сих пор не доходила до этой проверки из-за более ранних фейлов, поэтому баг жил незамеченным с самого выбора формата снапшота (журнал 2026-07: «PHP `return [...]` (include + opcache)» — решение осознанное, ради скорости, но без обязательной страховки). Механизм: `snapshot_save` пишет `.tmp`, проверяет себя `include`'ом, переименовывает в боевой файл — но OPcache кеширует скомпилированный код **по имени файла**, и `rename()` поверх существующего имени для него невидим (особенно с `opcache.validate_timestamps=0` или в пределах `revalidate_freq`). Итог: `snapshot_refresh_presentation()` возвращает `true` (файл на диске обновился корректно, самопроверка прошла), но следующий `snapshot_load()` — даже в том же запросе — может отдать старый снапшот из кеша. Не ошибка, а тишина: refresh «сработал», но эффекта нет. Исправление: `opcache_invalidate($file, true)` сразу после `rename()`, и такая же инвалидация `.tmp`-пути перед его собственным `include` (та же ловушка — `.tmp` тоже фиксированное имя, переиспользуется каждым save). Обе — с проверкой `function_exists('opcache_invalidate')`, не падают там, где OPcache выключен. Найдено через расширение Claude в Chrome — сессия сама зашла на localhost, увидела FAIL, прочитала код, поставила диагноз без пересказа Влада |

| 2026-07-12 | **Находка: смоук-тест был хрупок из-за совпадения имени фикстуры с боевым словарём. Исправлено.** Расширение Claude в Chrome впервые подключено и использовано вживую — Claude сам зашёл на localhost, прочитал вывод smoke_test.php напрямую (без пересказа Влада), сам создал таблицу `main` через конфигуратор в браузере. Первый FAIL: таблица-манекен `main` (изолированный стенд смоука, отдельно от боевой `well`) физически исчезла из БД — вероятно, побочный эффект вчерашних экспериментов с инструментом ремонта. Пересоздана конфигуратором. Второй, более глубокий FAIL: манекен использовал поле `voc_mr` — по главному принципу (имя есть адрес, §1) это ЖЁСТКО адресует боевую таблицу `voc_mr` (реальный словарь месторождений, которым Влад пользуется и который сам правил). Смоук хардкодил id=1/2 → «Самотлор»/«Приобское» и count=4, ничего из этого не создавая сам — чистое предположение о внешнем состоянии живой БД. Как только боевой словарь изменился (правки Влада), числа разъехались. АРХИТЕКТУРНАЯ ПРИЧИНА (не баг теста, а следствие доктрины): нельзя развязать имя поля и имя словаря частично (это и есть смысл §1) — значит у изолированной фикстуры должен быть ПОЛНОСТЬЮ СВОЙ словарь, не разделяемое с боевыми данными имя. Исправление: `main.voc_mr` → `main.voc_smoke`, смоук сам создаёт/очищает/наполняет `voc_smoke` при каждом прогоне (4 значения, id вычисляются, не хардкодятся), боевой `voc_mr` больше не участвует в проверках вовсе. `main` пересоздана инструментом ALTER (drop+add поля) — первое живое применение вчерашней Волны 2 на реальной задаче, не на игрушечном примере |

| 2026-07-11 | **Инструмент ремонта структуры — ПРОВЕРКА БОЕМ ПРОЙДЕНА вживую.** Первый запуск на реальной базе поймал весь накопленный экспериментальный мусор: поле-сирота `cementing_stage.dec_pressure_stop` (тот самый, из-за которого labels не вешал подпись), призраки `voc_ai`/`tasks_ai` (от удалённых экспериментов), дубли `cementing_stage` #67 / `cementing_interval` #69 (из «под-копирочной» сборки). Влад прожал кнопки — все взялись/убрались куда положено, плашка обновлялась, расхождения ушли. `voc_mr`/`data_name`, показанные как сироты, оказались НЕ багом диагностики (Claude сперва заподозрил ложную тревогу на словаре-складе и хотел «чинить» детектор) — это экспериментальные таблицы Влада, диагностика сработала верно. Урок закреплён: детектор сирот менять по следам того скриншота НЕ нужно, он корректен. Цикл увидел→показал→починил замкнут и работает на языке модели, без SQL. Первый косяк, лечившийся раньше только костылём, теперь лечится кнопкой |

| 2026-07-11 | **Инструмент диагностики-починки структуры БД — утверждён, строим по-настоящему (работа конфигуратора).** Первый боевой косяк (Влад, эмулируя юзера): у `cementing_stage` поле `dec_pressure_stop` есть в таблице, но нет в реестре → labels не вешает подпись («Сохранено: 3, с ошибками: 1»). Влад настоял: не латать разово (SQL/пересборка), а строить полновесный инструмент — это несущая часть ценности продукта («сила = безболезненное изменение структуры живой БД с данными»; на заводе с миллионом строк пересобрать таблицу нельзя). Требование: починка на ЯЗЫКЕ МОДЕЛИ (таблицы/поля/подписи), не SQL — потому что система сама отучает от SQL (Влад за 18 лет + шаблоны забыл MySQL — это успех абстракции, но значит в аварии SQL требовать обратно нельзя). Утверждённые границы (все три вопроса — Влад «все три»): (1) три вида расхождений сразу — поле-сирота (в БД есть, в реестре нет), запись-призрак (в реестре есть, в БД нет), дубль (зарегистрировано дважды); (2) обе кнопки, СУДИТ ЧЕЛОВЕК — система показывает расхождение и даёт «взять под управление»/«удалить», не угадывает, кто прав (Мир А; эталон-«бумажка»/Мир Б — позже, придёт побочным продуктом ИИ-онбординга); (3) регистрация сироты — дефолтная подпись (имя поля), осмысленную человек правит в labels. Место: раздел конфигуратора «Состояние модели» + плашка-уведомление вверху (не молча). НАХОДКА: половина уже есть — `snapshot_build_registry` собирает `orphans` (записи-призраки), но нигде не показывает; поле-сирота и дубль не детектятся (дубль молча затирает ключ в map). Строим: диагностика (ядро, расширить orphans до 3 видов) → показ (раздел конфигуратора) → починка (действия конфигуратора). Три опоры продукта (для контекста, §Позже): онбординг чужой БД через ИИ по правилам+разметке → эволюция живой структуры → ремонтопригодность |

| 2026-07-11 | **Разделение админ-файлов = ВРЕМЕННЫЙ фильтр доступа, не дублирование. НЕ сливать входы до ролей/прав.** Влад раскрыл замысел, стоявший за разными файлами: контекст = объект + действие + роль (+ ещё подъедет); на нём строится фильтр отображения. Роли по нарастанию прав: простой пользователь (видит только отчёты — представления данных; базовых таблиц напрямую НЕ видит) → администратор данных (управляет данными, видит базовые таблицы) → системный администратор/конфигуратор (заливка БД, правка модели — почти всё). Пока ролей и прав нет — фильтр ЭМУЛИРУЕТСЯ разделением на файлы: сам файл и есть фильтр (не пустить в конфигуратор = не дать файл/URL). Отсюда index.php (пользователь) / labels.php (админ данных) / configurator.php (сисадмин) — разделены осознанно, не от незрелости. СЛЕДСТВИЕ ДЛЯ БУДУЩИХ СЕССИЙ: не «оптимизировать» разделение файлов, не сливать админ-входы в один — это снесёт несущую подпорку доступа. Свести можно только ОБЩИЙ ВИД (первая страница — `render_table_directory`, уже сделано 07-11), но не сами входы. Слияние входов + настоящий фильтр по контексту — когда дойдём до пользователей/прав/безопасности, не раньше (см. «Позже»). Claude едва не снёс это по незнанию (предлагал слить labels+конфигуратор) — Влад притормозил; правильная граница (вид общий, входы раздельны) названа явно |

| 2026-07-11 | **Критерий размещения метаданных `model_`: причина изменения, не заполненность.** Вопрос Влада «сыпать в одну таблицу до края или охотно заводить новую». Ответ — ни та крайность, ни другая: новая метаданная в существующую таблицу, если меняется тогда же/теми же руками/тем же экраном, что уже живущее там; своя причина/ритм/хозяин или иная форма связи → новая таблица. Тот же принцип, что отделил `model_labels` от `model_registry`. Ориентиры: статус таблицы + комментарий → `model_labels` (правятся с подписями); порядок полей → своя таблица весов (отдельное действие, форма «отношение в группе», не 1:1). Обе крайности запрещены: «всё в реестр» = пустующие колонки + тряска всей таблицы; «плодить охотно» = таблицы, всегда меняющиеся вместе (болезнь кустарного рендера). Записано в ARCHITECTURE §17 |

| 2026-07-11 | **Граница простого словаря: подпись по шаблону из собственных полей, `data_name` — частный случай.** Разговор вскрыл терминологический туман (моё «разрешает адрес» прозвучало как DNS-резолвинг прямого адреса — Влад справедливо возмутился «нафига ресолвить прямое указание»). Проверка по коду: DNS-ресолвинга нет, §16.1 прямо гласит «имя поля есть явный адрес, эвристического выбора не существует». Для простого `voc_material` компилятор делает один `isset` (подтвердить, что таблица есть) + пометку подтипа; превращение id→подпись — в рантайме, в read-хендлере через `lookup_labels`, по прямому адресу. Я вчера переоценил роль компилятора («без него не показал бы ОТМ-5») — признано, показывает хендлер. Настоящая (не-DNS) вещь, которую Влад унюхал: лесенка `voc_x` → иначе `x` (адресный словарь, переиспользование готовой таблицы) даёт `voc_x` два возможных дома. Решение: лесенку ОСТАВИТЬ (переиспользование готовых таблиц под словари нужно). Уточнение доктрины (Влад): подпись простого словаря собирается по ШАБЛОНУ из собственных полей таблицы; `data_name` = шаблон из одного куска, `{voc_area} №{data_number}` = из нескольких — оба простые, различие лишь в числе кусков. `data_name` перестаёт быть особым правилом «всегда эта колонка», становится частным случаем — одно правило вместо двух, и оно переиспользует уже готовую машинку составных подписей (а2), а не держит урезанный путь. По факту это упрощение, не расширение. Сложное (фильтры, срезы, цепочка в ДРУГИЕ словари) — позже. Метод: Влад попросил объяснять «как для лабрадора» (без терминов, описаниями) — на простых словах ошибка виднее обоим; принято как постоянный стиль. §16.1 обновлён |

| 2026-07-10 | **Инвариант трёх слоёв: поле → view-сборка → рендер. Экран НЕ собирается во входной точке.** Диагноз (Влад + ChatGPT): обходы (кустарные `echo '<table>'` в `index.php`, `configurator.php`, `labels.php`, дающие три разных вида одного списка) появились не из-за плохой архитектуры, а из-за **неназванного среднего слоя**. Claude ошибся дважды и признал: (а) фактически — соврал «режима `edit` в ядре нет»; он есть (`ACTION_MODES['edit']='edit'`, `voc_handler`/`int_handler` дают на `edit` виджет, на `read` — значение); четыре режима поля: `read`/`new`/`edit`/`validate`. (б) концептуально — предложил новый режим поля `list`; неверный уровень. Режим поля `list` НЕ нужен: `read` уже отдаёт готовое к показу значение (для `voc_` — подпись словаря, не id). Не хватало не режима, а **сборщика представления**. Утверждённая граница (не выбор — следствие архитектуры): `core.php` собирает смысловую структуру представления (view-массив, без HTML); `render.php` превращает её в HTML; `index.php`/`configurator.php` только выбирают сценарий и запускают ветку. Три слоя: **поле** — `field_exec($data, read/new/edit/validate)`; **view-сборка (presentation assembly)** — `record_table_view()` (использует `read`), `record_form_view()` (`new`/`edit`), `record_tree_view()` (`read` + дерево связей) — возвращают массивы `['kind'=>..., 'fields'=>[field_exec(...), ...]]`; **рендер** — `render_record_table()`, `render_record_form()`, `render_object_tree()` — массив → HTML. Прямые запреты (в силе для всех ИИ): не добавлять `list` в паспорта сущностей; не создавать новые входные файлы; не писать HTML-таблицы в `index.php`/`configurator.php`; не переносить HTML в `entities.php` (сущность отдаёт структурированный результат `value`/`choice`/`input`, `<select>` рождает только `render_choice`); не называть `read` «ячейкой списка» — это режим показа значения. `record_tree()`(core) + `render_object_map()`(index) от 07-09 — тот же средний слой, начатый верно, но недочищенный (рендер живёт в index, должен в render). Запись сделана ДО кода — сначала инвариант, потом реализация |

| 2026-07-09 | **`index.php` повзрослел: навигация, общий стиль, настоящая карта объекта (`record_tree`).** Вторая половина дня, после наполнения Листа2 реальными данными вскрыла не только баги (см. запись выше), но и то, что сам `index.php` был недостаточен как рабочий инструмент — «терпимо было, больше нельзя». Четыре слоя правки, каждый следующий вскрывал более глубокую проблему предыдущего: **(1) Внешний вид** — общий CSS вынесен в `render_admin_styles()` (render.php), один источник для `index.php` и `configurator.php`, левый край, отступ списков 20px, класс `.act`/`.act-danger` под компактные действия. **(2) Навигация «Домой»** — точка входа без `_table` (или `?_action=home`) показывает список «главных таблиц»; критерий («нет `dep_`/`rel_main`», плюс явное исключение `voc_`-словарей, подтверждено Владом как верное решение) сознательно НЕ компилируется в снапшот — сам критерий назван временной заглушкой «для обкатки», компилировать временное в модельный слой значило бы усложнять под то, что скоро заменится настоящей пометкой через конфигуратор; считается на лету, один проход по уже загруженной структуре. **(3) `~`/`×` вместо текстовых ссылок** в списке, плюс первая ячейка строки — крупная область клика (CSS `display:block` на весь паддинг ячейки, не просто текст в `<a>`) — навигация естественна и на телефоне. **(4) Карта объекта — главная правка дня, потребовала два захода.** Первая попытка (`view&_id=N` = read-строка + плоские блоки-ссылки на детей) оказалась НЕ картой, а навigacией — Влад поправил прямым разбором легаси `db_tree()`/`find_dep()`/`related_records()`/`t_reader()`/`button_new()` (param.php): карта — это когда ВСЁ дерево объекта развёрнуто на одной странице сразу, не кликанье по уровням. Решено и построено: `record_tree()` (core.php) — рекурсивный обход ВСЕГО дерева от записи вниз до листьев в СТРУКТУРУ (не HTML) через уже скомпилированный `model.relations` (легаси сканировало БД на каждом узле — `find_dep`); `render_object_map()` (index.php) — рекурсивный рендер этой структуры отдельно от обхода (легаси мешало обход/SQL/HTML в одной функции `db_tree`). Ограничения глубины НЕТ сознательно — решение Влада: «карта есть карта, приходится не иметь ограничений»; ацикличность гарантирована компилятором графа (dep_ — дерево владения, не цикл), рекурсия конечна. Форма `edit` осталась чистой формой без карты; «Отмена» стала контекстной (edit → своя карточка, new-ребёнок → карточка родителя, new без родителя → список). Проверено живьём на полном дереве well→stage→interval→buffer (4 уровня, вложенность div 0→24→48→72px корректна), все регрессии (список/CRUD/домашняя) чисты |

| 2026-07-09 | **Первая боевая проверка (Лист2, реальные данные): пять найденных и закрытых багов.** Дерево построено через UI конфигуратора, наполняется через `index.php` — оба механизма впервые под реальной нагрузкой, не синтетикой. (1) `has_id` был опционален для режима `plain` (не только для `dependent`, который чинили 07-08) — таблица без явного выбора `id` в форме могла родиться без первичного ключа, ломая весь CRUD (`WHERE id = ?`); закрыто универсально для всех режимов, `id` убран из списка типов формы вообще (сервер гарантирует, не спрашивает). (2) `calc_handler` (карантинный спайк `ent_calc`, `well.calc_spike` — обнаружился на реальной таблице, ранее в тестах не встречался) объявлял возврат `: array`, но пытался вернуть `null` на `new`/`edit` — `TypeError`, форма создания `well` падала фатально; сигнатура исправлена на `: ?array` (диспетчер `field_exec` и `index.php` уже корректно обрабатывали `null`, нарушение было только в объявлении). (3) `render_input()` в `render.php` никогда не читал `meta` — механизм существовал (footnote_handler давно готовил `maxlength`), но был мёртв для ВСЕХ хендлеров; `dec_`-поля рендерились как `<input type=number>` без `step`, браузер требовал целое число на любую дробь. Починен как общий механизм (meta → HTML-атрибуты для textarea/date/time/number/text), `dec_handler` получил `step=any`; `maxlength` у footnote заработал бесплатно тем же фиксом. (4-5) UX конфигуратора: кнопка удаления поля формы, порядок подписей (полная первой), автооткрытие списка типов при «+ добавить поле», связка «содержимое» между списком таблиц конфигуратора и `index.php` (наполнение словарей/дерева данными — index.php уже универсален, не хватало только ссылки). Все пять — баги, не архитектурные решения: ни один не менял контракт, все найдены именно потому, что реальное использование бьёт по путям, которые синтетические тесты не задевают (частный случай: `calc_spike` есть только в производственной `well`, ни в одной тестовой фикстуре его не было). Корень дерева подтверждён живьём — `well`, не `main` (см. п.7) |

| 2026-07-08 | **Топология Листа2 согласована; группы полей отложены, НЕ отклонены; третий режим конфигуратора («зависимая таблица») готов.** Дерево исправлено дважды за сессию по мере углубления в реальное ТЗ (легаси растворяла интервал в material — неудачно): финал `main → stage → interval → buffer` + ветка `reper → reper_component`, интервал цементирования признан отдельной предметной таблицей (главный поставщик данных листа), не полями material. Группы логически связанных полей (от/до, план/факт): идея Влада `start`/`stop` как универсальная сущность-диапазон — сильная, НЕ смешение кода и данных (кодирует структурную роль «граница», не предметность); идея Чата `model.groups` как модельный слой — тоже валидна; ОБЕ отложены до первого реального потребителя (валидация/пересечения/отчёты — сами в «Позже»), т.к. по строгому тесту «без сущности не обойтись?» CRUD Листа2 собирается целиком без понятия «группа». На сегодня группы → подчинённые таблицы (буфер 1:многие — дамп + двухчастный состав; репер-компонент с дозировками — не `mlt_`). Третий режим конфигуратора реализован и обкатан живым HTTP: родитель из живого списка, `dep_<parent>` генерируется, `rel_main` явным флажком, структурные колонки без FK-constraint (защита связей отложена, журнал 07-06). Детали — п.7 «Сейчас» и блок групп в «Позже» |

| 2026-07-08 | **`rel_`/`dep_` различение уточнено по легаси (param.php, button_new, db_tree, не только дампу); `rel_main` признан частным случаем `rel_<root>`, не особым именем.** `dep_` — дерево владения (record_children); `rel_` — принадлежность корневому досье, шире дерева: покрывает и сквозной доступ, и боковые таблицы вне `dep_`-линии (будущая `record_root_related`, «Позже»). `main` в легаси — не модель, а единственный системный корень; когда корней в одной модели станет больше одного (мультикорневость — своя тема, не мультитенантность), формула обобщается до `rel_<root>`. Не проектируется сегодня — Лист2 даёт одно дерево. Полный разбор и границы — блок «Позже» рядом с формой продукта |

| 2026-07-08 | **Пункт 3 (`rel_`/`dep_`, зависимые таблицы) закрыт целиком** — все три шага (композитная подпись объекта, компиляция графа, дети в карточке) реализованы и подтверждены живьём: все 10 секций смоука + 1а-1г зелёные, полный HTTP-цикл через `php -S` (форма с контекстом родителя → POST → 422 при неподтверждённой связи → redirect в карточку → ребёнок виден). Детали контракта, семантика `dep_`/`rel_main` по легаси-дампу, оба найденных и закрытых бага компилятора шаблонов (мемоизация провала vs цикла) — см. п.3 «Сейчас» полностью, не дублирую здесь |

<!-- 2026-07-08, конец дня -->
| 2026-07-08 | **Тумблер `snapshot.mode` закрыт; автоперестройка конфигуратором была уже готова.** Половина п.6 (автоперестройка) оказалась реализованной в «Конфигураторе v0» 07-05, просто не увязана с формулировкой. Дореализован тумблер: ветка `live` в `snapshot_init` = прямой `snapshot_build()` без файла, лок уважается одинаково в обеих ветках. **Урок про смоук:** первая версия секции 1в проверяла тумблер запуском второго PHP-процесса (subprocess + `PHP_BINARY`) — на боевом сервере `PHP_BINARY` пуст, `sh: Permission denied`, ложный FAIL. Отладка этого сожгла лишние круги (Влад прямо указал: не жечь сессию на тестовом файле). Исправлено на проверку в одном процессе прямым вызовом `snapshot_build()`. **Принцип: проверки смоука — в одном процессе, без subprocess/PHP_BINARY.** |
| 2026-07-08 | **Шаг 1 пункта 3 — `record_label`, композитная подпись объекта.** Подпись самой записи (карточка/список/«к чему привязан»), не только опций чужого списка: проекция через `lookup_labels` → `data_name` → `#id`. Обёртка над готовым исполнителем а2, не новый механизм — по просьбе Влада «быть ленивым в хорошем смысле» (одна функция вместо параллельной логики). **Попутно закрыт дефект резолвера:** таблица с `data_label_template`, на которую нет входящих voc-ссылок, в проход 1 не попадала (его вход — voc-поля) → подпись объекта возвращала `#id`. Теперь `snapshot_build_dictionaries` дособирает планы для любой таблицы с маяком под ключом `@table`. Файлы шага 1 (`core.php`) НЕ отданы в проект — ждут отдачи одним куском с шагами 2-3, чтобы Влад не заливал `core.php` дважды. |
| 2026-07-08 | **Семантика `dep_`/`rel_main` прояснена по легаси-дампу `tt_utf8_fixed.sql`.** `dep_<table>` = FK потомка на непосредственного родителя (подчинение); `rel_main` = плоская ссылка с любой глубины на корень (m2m-вариант), параллельно `dep_`. Легаси `find_dep`/`related_records` искали связи рантайм-перебором всех таблиц — у нас граф компилируется в снапшот один раз (`model.relations`), рантайм не интроспектирует. Ключевое: «третий вид связи», который Fable чуть не начал изобретать, — это и есть `dep_`, уже существует. Для большинства задач достаточно нисходящих (`dep_`, карточка тянет детей) — с них и начинаем, `rel_main`-рендер отложен. |

| Дата | Решение |
|---|---|
| 2026-07-08 | **(а2) реализован и подтверждён живьём.** Все 10 секций смоука зелёные на целевом localhost (включая 1б, 6, 9, 10 — полный жизненный цикл HTTP с составной подписью). Контракт — п.1 «Сейчас» без отступлений от финальной формы (шаблон в `model_labels.data_label_template`). Извлечена `template_parse()` — чистая функция синтаксиса `{token}`/литерал, отделена от семантики словарного компилятора; побочно ужесточила контракт: непарная скобка — fail-fast сборки, не тихий литерал |
| 2026-07-08 | **Открытый вопрос-гипотеза (не решение): общий резолвер-движок для словарных проекций и будущего `math_`.** DRY-довод (Влад, при поддержке Чата/Gemini): `template_parse` + whitelist-сверка + рекурсия + циклодетект — рассматривать как ОБЩИЙ движок под капотом для (а2)-словарей и будущего `math_`, а не писать вторую такую же механику с нуля. Раздельность (журнал 07-08, «сестринское семейство») НЕ отменяется — остаётся разный entity-контракт (словарь: presentation-метаданные без своего поля; `math_`: полноценная сущность с префиксом и хендлером) и разная семантика листа графа (строка vs вычисление, `math_` потребует операторов/приоритета — «мощнее» простой подстановки). Общее — только механика прохода по графу ссылок. Граница «что общее, что раздельное» ПОМЕЧЕНА КАК ГИПОТЕЗА, не факт: проверяется только реальным проектированием `math_`, тогда же может быть пересмотрена в любую сторону, включая возврат к полному разделению, если арифметика не ляжет на общий каркас |
| 2026-07-08 | **(а2) Составная подпись словаря — контракт заморожен: шаблон в model_labels, не отдельная таблица-паспорт.** Три пересмотра за один разговор, каждый следующий проще предыдущего: (1) первый черновик — отдельная служебная таблица `model_dict_passport` со строками role/source_table/source_field/position/value, компилятор с JOIN-цепочками; (2) упрощение — две новые колонки в `model_labels` (order позиции на строках полей + glue-шаблон на строке таблицы), НО вскрыло омонимию: `data_short` строки `field:well.data_number`, если использовать как номер позиции, конфликтует с той же строкой как настоящей подписью поля в его собственной форме; (3) финал — одна колонка `data_label_template` с шаблоном фигурных плейсхолдеров (`{voc_area} №{data_number}`, по образцу KDE HTML Clock placeholders), плейсхолдер ссылается на поле по имени, омонимия снимается полностью (шаблон не создаёт вторую строку реестра, не трогает `data_short`/`data_full` поля в его роли). Исполнение — без SQL JOIN, через переиспользование `lookup_options()` рекурсивно. Полный контракт — п.1 «Сейчас» |
| 2026-07-08 | **Методологический принцип: список зон сложности (§13) не закрыт, расширение легитимно при явном прохождении §15 — вписано в ARCHITECTURE.md.** Разведены два разных предложения из разговора: (а) «расширение в рамках функции элемента легитимно, если не противоречит базовым принципам» — принято, это переформулировка того, что §15 уже делает, польза — явно снимает иллюзию, что §13 это закрытый список; (б) «жёсткие рамки нужны были для обучения ИИ, теперь можно ослабить, раз ИИ демонстрирует понимание» — ОТКЛОНЕНО как принцип: демонстрация понимания внутри одного разговора не переживает смену сессии (в этом же разговоре — дважды наблюдался конкретный механизм этого провала: смоук месяцами проверял ископаемый снапшот вместо текущего; спайк ent_calc чуть не стал негласным паттерном, потому что код громче комментария). Уточнение от Влада сильнее исходного (а): не «доверие к пониманию» делает расширение безопасным, а накопленное ВЕРИФИЦИРУЕМОЕ поле уже принятых решений (§15-пройденных, документированных) — оно тяготеет к себе правильное мышление о новых решениях, но остаётся проверяемым любым читателем документов вне зависимости от истории разговора, а не является актом доверия к конкретной сессии. Легаси-код (`voc_utf8.php`) — точный пример обратного: был опорой только пока не появилось верифицированное поле решений рядом; перестал быть образцом не потому что «ИИ повзрослел», а потому что появилась альтернатива с проверяемой легитимностью |
| 2026-07-08 | **`math_`/`calc_` — сестринское семейство, не подтип словаря.** Обсуждение спайка `ent_calc` (07-07, «модель начинает принимать грязный хардкод за паттерн») довели до архитектурного решения: словарные проекции и вычисления используют ОДНУ философию резолвинга (ссылка → разрешение на границе сборки → подстановка значения), но НЕ одну таблицу-описатель — иначе имя поля перестаёт однозначно определять сущность (нарушение §1), а общий описатель на два разных смысла — абстракция ради формы (§15.8). `math_` получит собственный паспорт (описание формулы + ссылки на поля-операнды) и собственный слот в снапшоте, отдельно от `model.dictionaries`, когда до него дойдёт очередь после (а2). Заодно закрыт должок с 07-07: карантинный баннер над `ent_calc` в entities.php уже стоит в коде (закрытие (а1)); эта запись — журнальная фиксация самого решения о карантине, которая тогда осталась несделанной |
| 2026-07-08 | **(а1) Адресный словарь закрыт.** `snapshot_build_dictionaries()` разрешает `voc_x` при сборке снапшота (склад/адрес/fail-fast), рантайм (`voc_handler`) читает скомпилированный `$field['source']`, из имени поля больше ничего не выводит. Контракт и все четыре границы — согласованы 07-07 с Чатом до кода, реализованы без отступлений (см. п.1 «Сейчас»). Секция 1а смоука — 8 проверок, все зелёные на целевом localhost. Живой тест адресного словаря (создать `area` через конфигуратор, добавить `voc_area`-поле) — не прогонялся, оставлен как желаемая, но не обязательная ручная проверка: код-путь доказан лестницей на синтетике, живой прогон добавил бы наблюдение, не новое знание |
| 2026-07-08 | **Побочная находка: миграция (а0) на presentation была неполной.** `render.php` не тронули при переходе `subscr` → `model_registry`/`model_labels` (07-05): докблок и код читали `$result['subscr']['data_fild']` (до-реестровый ключ), тогда как `field_data` уже несколько дней кладёт туда строку `model_labels` с ключами `data_short`/`data_full`. Подписи в форме не рисовались вообще — тихий откат на `$result['name']`, без ошибки. Аналогично три проверки в `smoke_test.php` (секции 1 и 8) сверялись со старым форматом `subscr['data_fild']`. Правки: `render.php` — `data_fild` → `data_full` (одна строка + докблок); смоук — три проверки переведены на `presentation['labels']['field'][...]` и обновление подписи через `model_labels` JOIN `model_registry` вместо мёртвой `subscr` (с сохранением/восстановлением исходного значения). `data_short` (короткая подпись для шапок таблиц) заявлена в докблоке `render.php`, но нигде не используется — не чинилась, это не баг, это недоделанный кусок presentation, отдельная будущая задача |
| 2026-07-08 | **Как вскрылась находка — честная запись, не для повтора ошибки.** До сегодня `smoke_test.php` молча подхватывал СТАРЫЙ файл `state/snapshot.php` с диска (собранный ещё до текущего ядра) вместо пересборки — `snapshot_validate()` не отличала файл до-словарной эпохи от актуального, `snapshot_load()` находил валидный (по старым меркам) файл и на этом успокаивался. Три проверки, зависевшие от ископаемого формата `subscr`, годами проходили не потому, что код работал, а потому что смоук их фактически не проверял против текущего ядра. Вскрылось только когда (а1) добавил в `snapshot_validate()` проверку на `model.dictionaries` — она отбраковала старый файл как невалидный, заставила пересобрать текущим ядром, и все ископаемые места честно всплыли. Вывод на будущее: `snapshot_validate()` должен расти вместе с форматом снапшота при каждом структурном изменении модели — иначе смоук проверяет прошлое, а не настоящее |
| 2026-07-07 | **Тумблер режима снапшота (`snapshot.mode`: cached/live)**: dev-режим — сборка налёту той же `snapshot_build()` без шага записи, результат один рендер в памяти; убирает staleness по построению (файла нет — нечему протухать/сиротеть). Легитимность даёт область и триггер, не механизм: dev-only, дефолт cached, флаг в системном конфиге, **не** request (§15.4), прод live не ставит (табу §12 на живую интроспекцию как штатный режим). Мотивация — нулевая dev-нагрузка (1 запрос/час, 1 разработчик) + недельная боль ручного rm. Названная цена: живя в live, не прогоняешь файловый путь (include/atomic write/холодный старт), который в проде работает — гарант остаётся смоук, всегда строящий+пишущий+читающий настоящий файл (секция 1) |
| 2026-07-07 | **Конфигуратор сам перестраивает снапшот** в конце структурной операции — оформление протокола §8 (`lock → DDL → registry+labels → snapshot_rebuild_structure (temp+rename) → unlock`), не новый механизм. Ручное `rm state/snapshot.php` уходит из нормального workflow, остаётся аварийной dev-операцией. Остаточный класс Б (осиротевший lock после падения между lock/unlock) не закрыт, лечение ручное; автоснятие сироты повышено до обязательного-до-мультиписателей (см. открытые вопросы) |
| 2026-07-07 | **Форма продукта «код-раз, моделей-много» осознана и осознанно отложена.** GPDP по природе — SaaS-мультитенантность с изоляцией по БД: код универсален, приложение = модель в БД, пользователь владеет набором моделей, версия задаётся на пользователя. Сдвиг допущения зафиксирован явно: STATE.md до сегодня жил в «одна инсталляция / одна модель / один пользователь», часть решений (config, снапшот-файл, root_table в config) выросла из него. Под новой формой: снапшот принадлежит модели (идентичность = адрес файла, не поле-маячок), коннект = выбор модели на запрос (пул/мультиконнект НЕ проектируется, одна активная за раз), config раздваивается системный/тенантский. Красная линия: назначение модели — доверенный слой окружения, не request (§15.4). Проектирование отложено до окончания базовой фазы (сущности, словари, зависимые таблицы Листа 2) — не заготовка впрок (§12). Аварийная сборка класса А и партиционирование «инженер на свой участок» — в «Позже» той же группой. Точка возврата названа, чтобы будущая сессия не изобретала форму заново |
| 2026-07-06 | **Честная поправка к формулировке про m2m** (пойман overclaiming при пересказе в другой диалог): «доктрина уводит большинство кандидатов от m2m» ≠ «m2m решена». Правильно: настоящая m2m не решена, а ОТЛОЖЕНА (`ent_mlt` в «Позже»). Контекстные таблицы физически неотличимы от junction-таблиц с payload (два FK + параметры в строке) — разница пока ТОЛЬКО в имени (`dep_`/`rel_` как маркер владения), поведенческая защита (каскад, protected reparent) не реализована ни строкой кода. До первого построенного и обкатанного 1:многие `dep_`-кейса (не 1:1, как единственный сегодняшний пример model_labels→model_registry) заявление «архитектура не даёт багам появиться» для зависимых связей — гипотеза, не наблюдение. Разграничение «спроектировано верно» vs «проверено в бою» — держать явно при любом внешнем изложении проекта |
| 2026-07-05 | **Семейство времени (date/year/time) подтверждено живьём на целевом localhost, ядро не тронуто.** Разбор с Чатом дал пять правок к первому черновику: (1) пустая строка ≠ валидное значение для DATE/YEAR/TIME (в отличие от varchar) — конвертация в NULL по живой `field['schema']['nullable']`, не по дефолту паспорта — реальный класс бага, не мелочь; (2) `year` — `smallint`, не MySQL `YEAR` (узкий диапазон 1901–2155 без реальной причины его придерживаться); (3) `time` — нормализация HH:MM ↔ HH:MM:SS на границе виджета; (4) `meta.min/max` не добавлен в render.php — обоснование исчезло вместе с отказом от жёсткого диапазона года; (5) `timestamp` — «ноль кода» было неточно, имя колонки всё равно требует структурного статуса. Composite-механика (day+month+year частями POST) — отклонена как ненужная для календарных сущностей, отложена до `ent_dur` |
| 2026-07-05 | **Конфигуратор БД v0 подтверждён живьём на целевом localhost**: полный цикл форма→DDL→model_registry/model_labels→snapshot rebuild на примере `voc_usc` (первая таблица, созданная кодом системы, не руками через phpMyAdmin — закрывает целый класс ошибок недели: коллация, забытый шаг, лишний id, недосозданные подписи — все они были следствием ручного SQL). Три доводки по итогам разбора формы: (1) латиница обязательна только там, где строка становится частью адреса модели (имя таблицы/поля) — подписи всегда свободный текст, это не было нарушено, но не было и явно сформулировано; (2) синтетический тип `id` в форме визуально скрывает поля имени/подписи — сервер их не требовал и раньше; (3) `voc`-поле выбирается из живого списка существующих словарей уровня 0 (не свободный текст), подпись автоподставляется из их `model_labels`, остаётся редактируемой на случай той же таблицы в другой роли |
| 2026-07-05 | **Три роли текста разведены** (было ошибочно слито в одну «footnotes→long_text»): `data` — общий случай, без семантических претензий; `footnote` — краткая пометка, жёсткий контракт ≤50 символов (константа `FOOTNOTE_MAX_LENGTH`, проверяется в `validate` через `mb_strlen`, не полагаемся на молчаливое обрезание MySQL); `ltext` — длинный текст без потолка (объяснительная записка и подобное). Легаси `footnote_`/`footnotes` — археология СМЫСЛА роли, не формата (обе новые сущности используют current-стиль паспорта, не легаси-структуру) |
| 2026-07-05 | **Отклонён вариант «виджет по db_type внутри одного data_handler»** (был предложен, затем сам же отменён при разборе): подглядывание в физический тип колонки для выбора поведения вводит вторую ось определения поведения помимо имени поля — прямое нарушение §1. Правильное решение — отдельная сущность (`ltext`) для отдельного поведения, имя определяет всё |
| 2026-07-05 | Наполнение Лист1 (ТЗ-2) для практической обкатки: подтверждён набор — `voc_mr` (переиспользуется), `data_number`, `date_done` (ent_date, в проектировании), `voc_enterprise`/`voc_subsoiluser`/`voc_technolog`/`voc_usc` (простые словари уровня 0), семь `data_*` с числовыми подписями-единицами, `ltext_remark` (примечание — открытая по природе графа, не подходит под контракт footnote). Честная поправка: Лист1 сам по себе НЕ требует сложных словарей (уровень 1/2) — они естественно придут с Лист2 (зависимые таблицы, адресация `well` как родителя) |
| 2026-07-05 | **(а0) выполнено на целевом localhost**: model_registry (3 строки) + model_labels (3 строки) живые, snapshot собирается, подписи отображаются в браузере. Мёртвый дубль `snapshot_ensure()` (жил в core.php с первого дня холодного старта, звал несуществующие функции) обнаружен и удалён при сверке файла — второй сборки-двери больше нет физически, не только в журнале |
| 2026-07-05 | **Служебные таблицы `model_*` исключены из структурного сканера целиком** (`SYSTEM_TABLE_PREFIX`, снапшот): не проходят классификацию полей, не видны в `structure.tables`, не адресуемы через `?_table=`. Одним решением закрыты два вопроса: fail-fast на собственных структурных именах ядра (`active` и любые будущие) и вчерашняя дыра «форма-конвейер может открыть системную таблицу» — обе исчезают по факту отсутствия в структурной карте, не патчем-заплаткой |
| 2026-07-05 | **Правило суррогатного `id` уточнено дважды за день, финально — по кардинальности связи, не по форме таблицы**: id нужен, когда возможно несколько записей на родителя или несколько родителей у записи (контекстные таблицы: состав, буфер — там будет и id, и dep_, и rel_, не по одному). Когда связь строго 1:1 (как model_labels→model_registry сегодня) — суррогат лишний, FK-родитель сам становится PK, инвариант «не более одной подписи на элемент» обеспечен схемой, не дисциплиной кода. Решать по факту каждой конкретной связи, не шаблоном на все таблицы |
| 2026-07-05 | **Роль fail-fast сборки уточнена после разбора**: это не защита от пользовательского ввода (та — забота будущего Конфигуратора, который не даёт написать чепуху) и не броня от неограниченных будущих угроз (§13 запрещает код против несуществующих). Это защита целостности физической схемы независимо от пути попадания записи в БД — путь мимо конфигуратора не исчезает никогда полностью (ручной SQL при аварии, миграции, восстановление бэкапа, баг самого конфигуратора). Подтверждено живым инцидентом дня (a0), не гипотезой. После появления Конфигуратора ожидаемая частота срабатывания → почти ноль; это будет доказательством его работы, не поводом убрать проверку |
| 2026-07-05 | **Открытый пункт на будущее — деградация при недоступности файла снапшота** (не решать сейчас, только зафиксировать условия безопасности): временный откат на построение из живой БД БЕЗ сохранения в файл, только при подтверждённой неспособности писать/читать state/ (не как постоянный режим — прежнее табу на живую интерospekцию как штатный режим остаётся в силе), той же `snapshot_build()` без второго пути сборки, обязательная видимая индикация аварийного режима на каждый ответ (не тихо), `schema.lock` уважается даже в деградации. Мотивировано живым инцидентом EROFS (2026-07-04) |
| 2026-07-05 | Мотивировка Конфигуратора уточнена: как только он — единственный путь записи модели и сам работает по Naming-Driven Behavior (предлагает только существующие элементы, не берёт ничего из воздуха), опечатка в имени поля становится практически недостижимой с его стороны. Это довод в пользу его скорейшей постройки, не довод против сегодняшних проверок (см. запись про роль fail-fast выше) |
| 2026-07-04 | **Лестница словарей пересмотрена — введён адресный словарь (уровень 1)**: `voc_x` без собственной таблицы адресует таблицу `x` → `x.id + x.data_name`, подпись наследуется от подписи таблицы. Запись «неявный триггер отклонён» уточнена: отклонён был эвристический ВЫБОР (несколько кандидатов); адресация по имени — главный принцип §1, не магия. Явная `voc_x` всегда побеждает. Термины: адресный / паспортный словарь. Конвенция: `data_name` — всегда просто «имя», субъектность в таблице; адресуемой таблице `data_name` обязателен |
| 2026-07-04 | **Реестр модели**: subscr раскалывается на `model_registry` (data_kind/data_owner/data_element + active; уникальный полный адрес) и `model_labels` (dep_model_registry + data_short/data_full; четвёрка подписей упразднена — контекст переехал в data_kind). Семейство служебных таблиц `model_*`. labels — первая живая зависимая таблица: dogfooding rel_/dep_ на служебных данных до предметки. Границы: registry — что/где/жив ли; labels — как звать; passports — как работает. Запись в реестр — внутри lock-окна структурных операций |
| 2026-07-04 | **active** — структурное имя (ряд id/dep_*/rel_main) для служебных таблиц модели; в предметных — ошибка модели (доктринально, без механического запрета). active=0 исключает из model/presentation-компиляции, физика правдива; зависимость плана от неактивного — fail-fast. Не bul_active: статус элемента модели — не пользовательское поле |
| 2026-07-04 | **Инвариант восстановимости снапшота** (§8): удаление + запрос = эквивалентная пересборка из DDL+реестр+паспорта; факт, живущий только в снапшоте, — ошибка архитектуры; снапшот вне бэкапов/миграций. Формула: реестр — исходник модели, снапшот — её компилят. Правило мусора: сироты реестра инертны (отчёт сборки); зависимость от сироты — fail-fast |
| 2026-07-04 | Колонки паспорта уровня 2 переименованы: value/position (бывш. payload/sort_order — имя, требующее объяснения, плохое). Точка в физических именах колонок подтверждена запрещённой; ссылки — значениями строк |
| 2026-07-04 | **Инцидент «мигающий EROFS» закрыт**: причина — systemd `ProtectHome=read-only` у веб-службы (весь /home для неё read-only независимо от владельцев и chmod). «Мигание» = две точки зрения на каталог: консоль видела rw, служба — ro. Диск невиновен. Лечение: `ReadWritePaths=<путь к state/>` drop-in'ом службы либо размещение сайта вне /home. `MemoryDenyWriteExecute=yes` той же службы — источник PCRE-JIT-warning; лечение `pcre.jit=0` в ini обоих SAPI, защиты не ослаблять |
| 2026-07-04 | **Уроки диагностики**: гипотеза проверяется прибором, не уверенностью — chown-гипотеза опровергнута воспроизведением раскладки прав (rename поверх чужого файла работает), EROFS пойман пошаговым diag-скриптом. `get_current_user()` возвращает владельца файла скрипта, НЕ исполнителя — для диагностики непригоден. Ручные chmod «для подтяжки» запрещены: маскируют причину |
| 2026-07-04 | Смоук подтверждён как детектор среды: секции 1/8 дважды ловили деградацию окружения (EROFS веб-контура, осиротевшее «Родовище» от прогона, умершего в ro-окне). Секция 8 уязвима к смерти самого восстановления — радикальное лекарство прежнее: эталонный дамп |
| 2026-07-04 | **Словарная доктрина принята** (О_словарях.md — доктринальный документ, в носители контекста). Словарь = контракт потребления (id+label+резолв), не тип таблицы; классификация смысловой роли ДО выбора хранения; контекстные таблицы (состав, применение) — зависимые записи, не m2m. Буфер и химреагенты ТЗ-2 переклассифицированы; `ent_mlt` понижен в «Позже». Кандидат-§16 подготовлен для ARCHITECTURE.md |
| 2026-07-04 | **Паспорт проекции**: `voc_<имя>` может быть не складом значений, а описателем — моделью запроса данными (source_table/source_field значениями строк, шаблон склейки label). SQL в данных не хранится и не исполняется; запрос собирает код по снапшоту. Компиляция плана — на сборке снапшота (модельный слой, refresh без lock); циклы ссылок — fail-fast. Точка в физических именах колонок запрещена — как двоеточие: смысл не вшивается в имя спецсимволом |
| 2026-07-04 | **[ПЕРЕСМОТРЕНО тем же днём, см. запись выше]** Неявный триггер отклонён: словарь существует только явно, отсутствие `voc_<имя>` — fail-fast. — Отклонён был не адресный словарь, а эвристический ВЫБОР между кандидатами. Адресация `voc_x → x.id + x.data_name` без собственной таблицы `voc_x` — легальна и является уровнем 1 лестницы (§16). Актуальное правило: явная `voc_x` (склад/паспорт) побеждает; при её отсутствии — адресация; далее fail-fast. Подтип словаря — явный маяк — остаётся в силе. Строка сохранена для истории обсуждения, не как действующая норма |
| 2026-07-04 | **Label проекции**: части — существующие элементы модели, разрешаются по имени из снапшота; проекция новых элементов не добавляет. Ограничение «один JOIN» отклонено — глубину цепочки строит компилятор, ограничение одно: завершимость (цикл-детект). Правилом станет ссылка на базовые таблицы |
| 2026-07-04 | Словари признаны предусловием зависимых таблиц: выбор родителя в форме = родительская таблица, приведённая к словарю (конкатенация label «Мамуринская №31»). Порядок «Сейчас» перекроен: словари → ent_date → rel_/dep_ |
| 2026-07-03 | **Полный зелёный на целевом localhost: 47 проверок смоука**, включая HTTP-цикл. Шаг 1 централизации SQL выполнен: чтение через `record_fetch`/`record_list` (возврат — данные, не результат-контракт §3; null/пустой массив вместо ошибок); index.php свободен от SQL и mysqli_result, контракт охраняется статической проверкой смоука |
| 2026-07-03 | **Смоук узаконен** (smoke_test.php в файловом пространстве): подключение через `config()` — та же дверь, что у системы, hardcode root упразднён; правило переноса: пара «функция + вызов» едет на сервер одним заходом и прогоняется смоуком до браузера |
| 2026-07-03 | **Правило смоука «действие → сбор результатов → восстановление → check»**: `check()` делает exit(1) при провале, поэтому восстановление состояния (подписи subscr, ручной lock) обязано стоять ДО проверок. Поймано живьём: осиротевшее «Родовище» от умершего прогона валило секцию 1. Тот же класс бага, что lock после fatal |
| 2026-07-03 | Корень очищен: snapshot_ensure.php (труп транспорта) и schema_runtime.php (драфт, 29 КиБ функций gpdp_*) удалены из рабочего каталога — лежащий рядом мёртвый код однажды кто-нибудь require'ит |
| 2026-07-03 | **Холодный старт подтверждён живьём на целевом localhost**: первичная сборка snapshot из gpdp_test, полный CRUD-цикл с PRG, кириллица насквозь. Точка входа — `snapshot_init()`; параллельный `snapshot_ensure()` удалён как второй путь сборки. Улучшения из ensure — в очередь портирования в `snapshot_init`: раздельная диагностика причин null, автоснятие мёртвого bootstrap-lock, ожидание параллельной сборки, автосоздание `state/` |
| 2026-07-03 | **Имя соединения — `$db_connection` везде**: параметр, не глобаль. Контракт; второе имя (`$db`) не существует. Урок: имя, не записанное в журнал, стоило вечера встречных переименований |
| 2026-07-03 | **Контракт config.php**: файл объявляет функцию `config()` со статическим кэшем; подключение — `require` БЕЗ присваивания (`$x = require` вернёт int(1)); доступ только через `config()`. Структура — тоже контракт: `db` (host/user/password/name), `paths` (snapshot/lock/entities), `application` (root_table) |
| 2026-07-03 | **Урок про lock**: PHP `finally` НЕ выполняется при fatal error и exit() — упавшая сборка оставляет осиротевший `schema.lock`, и система отвечает 503 вечно. Лечение вручную: `rm state/schema.lock`. Автоснятие устаревшего bootstrap-lock (только source=bootstrap, старше 60 с) — в портирование |
| 2026-07-03 | **Правило `sql_*`**: сборка SQL централизуется в семействе `sql_*` (helpers.php — направление вызовов позволяет звать его и record_*, и lookup_*). Закрытый словарь канонических форм (список/строка/insert/update/delete/словарь) под prepared statements; конструктор запросов общего назначения — табу §12. Миграция — при первом касании, не big-bang |
| 2026-07-03 | Статус смоука уточнён: «37 проверок» — среда разработки; целевой localhost подтверждён отдельно 2026-07-03. Журнал не утверждает больше, чем проверено на месте |
| 2026-07 | **Холодный старт** (проект): отсутствие `state/snapshot.php` при boot → автосборка штатным механизмом + атомарная запись + include. Санкционировано ARCHITECTURE.md §8 (интроспекция разрешена в bootstrap). Отклонено: динамическая сборка на каждый запрос (live introspection, табу §12) и тихий fallback в память при недоступном для записи `state/` — вместо него fail-fast с внятным сообщением. Второго пути сборки не появляется; триггер — состояние ФС, не действие пользователя. Dev-пересборка = удалить файл |
| 2026-07 | **ТЗ-2 (тампонаж) принято референсной задачей**; критерий беты — сборка из конфигурации без правки ядра. Решение по `rel_`/`dep_` поднято из отложенных в «Сейчас»: несущая конструкция задачи |
| 2026-07 | **[ПЕРЕСМОТРЕНО 2026-07-04 — словарная доктрина]** `ent_mlt` получает предметный контракт-тест: буфер и химреагенты — m2m с атрибутами связи через storage plan (§3). — Переклассифицировано: буфер и химреагенты — контекстные зависимые записи (rel_/dep_), не m2m. `ent_mlt` понижен в «Позже», настоящая m2m — только после классификации по доктрине (О_словарях.md) |
| 2026-07 | Отклонена инфляция композитных сущностей (plan_fact, start_end, unit_value, percent, money, workflow_state, history...) — нарушение §15.8 и табу на заготовки. Единица измерения — подпись в subscr; план/факт — группировка в отображении; interval — два поля до появления поведения |
| 2026-07 | Отклонён каталог «процессоров» впрок (list/item/filter/export_processor...) — слой, не ведущий к handler call, лишний (§12). Функции семейств record_*/render_* появляются по факту потребности |
| 2026-07 | Проектный префикс `gpdp_` упразднён: не нёс информации. Пространство имён формализовано: закрытый словарь семейств (entity/field/snapshot/action/record/lookup/render) + открытое множество `<id>_*`, где префикс функции сущности = префикс поля БД. Зарезервированные id — константа в ядре |
| 2026-07 | Тонкая труба: все проверки на границе подготовки, ровно один раз; из точки исполнения удалены whitelist режима, повторный разбор имени и охрана unknown — их двери не существуют по построению. `field_exec($data, $mode)` — имя поля из пакета |
| 2026-07 | Файловое пространство утверждено (см. раздел выше); state/ отделяет порождённое от написанного; направление вызовов фиксировано |
| 2026-07 | root_table временно в config.php до конфигуратора, затем в служебную таблицу модели |
| 2026-07 | PRG — стандарт завершения write-цикла: POST → save → redirect → GET |
| 2026-07 | index.php строит «подготовленное задание»: request → prepared task → pipeline (формулировка ChatGPT принята) |
| 2026-07 | `mysqli_set_charset($db, 'utf8mb4')` обязателен в точке подключения — без него кириллица задваивается (поймано живьём) |
| 2026-07 | Режима `save` у сущности нет: validate проверяет и нормализует, физическую запись делает pipeline (prepared statements). Рабочие режимы: `new/edit/read/validate` |
| 2026-07 | Контракт validate: `{valid, value, errors}`; сложное хранение в будущем — через storage plan, SQL всё равно у pipeline |
| 2026-07 | Результат записи честный: `{ok, operation, table, id, affected_rows, errors}` |
| 2026-07 | Ordinary pipeline: admin-режимы не рождаются картой действий. Создание структуры — декларативно по секции `db` паспорта; create-handler у базовых сущностей не нужен |
| 2026-07 | Первичный ключ новых таблиц — `id`; `index` — археология |
| 2026-07 | Неизвестный префикс — fail-fast (роняет сборку snapshot); старая БД под новым ядром не запускается |
| 2026-07 | subscr в snapshot как есть, колонку выбирает потребитель; расширение системы данными, а не API |
| 2026-07 | Формат snapshot — PHP `return [...]` (include + opcache); JSON только для экспорта/диагностики |
| 2026-07 | Статус legacy понижен: археология смыслов, не binding contract. Двоеточие не поддерживается даже на чтение; парсер без ветки modifier |
| 2026-07 | Сигнатура заморожена: `$handler(array $data, string $mode): array`; режимы `new/edit/read/validate/save` + `create` |
| 2026-07 | Handler возвращает структурированный массив; HTML — один из renderer'ов |
| 2026-07 | Snapshot двухслойный: structural (lock) / presentation (без lock) |
| 2026-07 | `dep_*`/`rel_main` не пишутся обычным save; reparent — отдельное действие |
| 2026-07 | mch стратегически = m2m/junction; CSV — только прототип |
| 2026-07 | root_table — параметр приложения в snapshot, не знание ядра |
| 2026-07 | N+1: request-level cache в `gpdp_lookup_options()`; read_many отложен |
| ранее | Entity registry: скан entities.php, без ручного массива |
| ранее | Паспорта: ноль аргументов, возвращают метаданные |
| ранее | Snapshot: два режима (locked event-driven / normal чтение файла), атомарная запись temp+rename |
| ранее | MySQL-only; короткие префиксы — гигиена, не кросс-СУБД |

## Статус спорных legacy-типов

| Тип | Решение |
|---|---|
| `enn_` | отложен; runtime SHOW COLUMNS запрещён; стратегически — частный случай choice/voc, не зависимость от MySQL ENUM |
| `footnotes` | **[ИСПРАВЛЕНО 2026-07-05]** отождествление с long_text было ошибочным. Разделено на две реализованные сущности: `ent_footnote` (краткая пометка, ≤50 симв., legacy-имя наследует по смыслу роли) и `ent_ltext` (длинный текст без потолка — то, что раньше ошибочно называлось «footnotes → long_text»). См. журнал |
| `timestamp` | статус открыт, уточнено 2026-07-05: нужен структурный префикс (`sys_`?) — имя колонки всё равно проходит field_parse, «ноль кода» было неточной формулировкой; см. открытые вопросы |
| `mlt_x:prm_y` | формат мёртв; роль/фильтр переедет в модельные элементы БД (слой «Позже») |
| `rel_`/`dep_` | **переведено из спорных в «Сейчас» п.2** — несущая конструкция ТЗ-2 |

## Носители контекста

- **ARCHITECTURE.md** — конституция (репозиторий + project knowledge)
- **GPDP-RNA_Architecture.pdf** — том 2, философская мотивировка
- **STATE.md** — этот файл: состояние, дорожная карта, журнал
- **2_Тех_задание.xlsx** — референсная задача (тампонаж, ТЗ-2)
- **О_словарях.md** — доктрина словарей, контекстных таблиц и классификации до m2m
- Репозиторий: github.com/Vladl-UA/gpdp
- Один источник истины для всех ИИ (Claude / ChatGPT / Gemini) — файлы в репо,
  не пересказы в диалогах
