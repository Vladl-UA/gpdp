## HANDOFF — 2026-07-14

task: DB call wrapper across core.php+configurator.php; AI Context
      Protocol adopted, trust mechanism corrected twice

decision:
  - STATE.md:805  trust mechanism: file-history commit, not self-reported field; scoped by source file, not whole-repo
  - STATE.md:807  DB wrapper session close; AI passports first verification pass
  - STATE.md:809  AI Context Protocol accepted (measured trigger: 27% session on one architectural message)
  - STATE.md:811  two DB-work levels separated (call wrapper vs selection-plan layer)

changed:
  - core.php::record_fetch        (mysqli_* → db_select)
  - core.php::record_children     (mysqli_* → db_select)
  - core.php::record_list         (mysqli_* → db_select)
  - core.php::snapshot_build_registry     (mysqli_* → db_select)
  - core.php::snapshot_build_presentation (mysqli_* → db_select)
  - core.php::snapshot_build_links        (mysqli_* → db_select)
  - core.php::model_diagnose      (mysqli_* → db_select)
  - core.php::record_save         (mysqli_* → db_execute)
  - core.php::record_delete       (mysqli_* → db_execute)
  - configurator.php::configurator_create_table    (mysqli_* → db_select/db_execute)
  - configurator.php::configurator_delete_table    (mysqli_* → db_execute)
  - configurator.php::configurator_adopt_table     (mysqli_* → db_execute)
  - configurator.php::configurator_adopt_field     (mysqli_* → db_select/db_execute)
  - configurator.php::configurator_add_field       (mysqli_* → db_select/db_execute)
  - configurator.php::configurator_drop_field      (mysqli_* → db_select/db_execute)
  - configurator.php::configurator_drop_column     (mysqli_* → db_select/db_execute)
  - configurator.php::configurator_drop_registry_row (mysqli_* → db_execute)
  - HANDOFF.md §0     (freshness check mechanism, twice today)
  - AI_CONTEXT_PROTOCOL.md §2,3 (freshness scope clarified for Chat)

added:
  - db.php::db_select   (SELECT/SHOW wrapper, prepared or direct)
  - db.php::db_execute  (INSERT/UPDATE/DELETE wrapper, honest result)

removed: none

contracts_changed:
  - configurator_register_link: bool → array
    (was: raw success flag, caller read mysqli_error() on failure.
     now: full db_execute() result {ok,error,...} — after the move to
     db_execute() the raw mysqli error was no longer the right one to
     read; return the real one instead of a stale/wrong string.)

contracts_unchanged:
  - record_save/record_delete public signature        [checked: read]
  - record_fetch/record_list/record_children signature [checked: read]
  - all entity handlers (entities.php untouched today) [checked: none — file not opened this session]
  - renderer contract (render.php untouched today)     [checked: none — file not opened this session]

docs_to_update:
  - function_passports:  already current for *.php as of 89010bb
                          (verified via `git log <last-touch>..HEAD -- '*.php'` — empty)
  - architecture_snapshot: NOT verified against today's changes — check before trusting
  - state_index: STALE — missing STATE.md:805,807,809,811 (verified —
    last touch 6f9a939, `git log 6f9a939..HEAD -- STATE.md` non-empty)

delegate_tasks:
  - file: labels.php, helpers.php, index.php
    operation: same db_select/db_execute migration, same pattern as
      core.php/configurator.php (18 remaining mysqli_* call sites total)
    constraints:
      - do not touch mysqli_connect/mysqli_close/mysqli_set_charset
      - do not change SQL text, only execution method
      - flag any failure-path behavior difference explicitly, do not
        stay silent (same rule as today's migration)

verification:
  - php -l: all touched files, clean
  - smoke: 10/10 sections pass, verified live on server after each step
    (not just locally — real MariaDB, real HTTP cycle)
